/* Generated by tools/build-runtime-bundle.mjs. Do not edit directly. */
/* source: mw4.js */
(function () {
  const STYLE_ID = "mw4-20260601-style";
  const revealTrailerId = "jLbst85USN8";
  const officialHero = "https://imgs.callofduty.com/content/dam/atvi/callofduty/cod-touchui/blog/body/mw4/reveal/MW4-REVEAL-TOUT.jpg";
  const officialImages = [
    "https://imgs.callofduty.com/content/dam/atvi/callofduty/cod-touchui/blog/body/mw4/reveal/MW4-REVEAL-001.webp",
    "https://imgs.callofduty.com/content/dam/atvi/callofduty/cod-touchui/blog/body/mw4/reveal/MW4-REVEAL-002.webp",
    "https://imgs.callofduty.com/content/dam/atvi/callofduty/cod-touchui/blog/body/mw4/reveal/MW4-REVEAL-003.webp",
    "https://imgs.callofduty.com/content/dam/atvi/callofduty/cod-touchui/blog/body/mw4/reveal/MW4-REVEAL-012.webp",
  ];

  const sources = [
    ["Offizieller Reveal-Blog", "https://www.callofduty.com/ca/en/blog/2026/05/call-of-duty-modern-warfare-4-announcement"],
    ["Offizieller MW4 Intel-Hub", "https://www.callofduty.com/blog/2026/05/modernwarfare4-fob"],
    ["Tokyo Games Show Campaign Trailer", "https://www.callofduty.com/blog/2026/09/call-of-duty-modern-warfare-4-tokyo-games-show-campaign-trailer"],
    ["Story so far", "https://www.callofduty.com/blog/2026/09/call-of-duty-modern-warfare-4-story-so-far"],
    ["Beta Patch Notes", "https://www.callofduty.com/patchnotes/2026/08/call-of-duty-modern-warfare-4-beta-patch-notes"],
    ["Beta Guide", "https://www.callofduty.com/blog/2026/08/call-of-duty-modern-warfare-4-open-beta-everything-you-need-to-know"],
    ["Multiplayer Deep Dive", "https://www.callofduty.com/blog/2026/08/call-of-duty-modern-warfare-4-next-highlights-multiplayer-gameplay-systems"],
    ["Pre-Order & Editionen", "https://www.callofduty.com/blog/2026/05/call-of-duty-modern-warfare-4-preorder-benefits-game-editions-details"],
    ["Offizielle MW4-Seite", "https://www.callofduty.com/modernwarfare4"],
  ];

  const facts = {
    title: "MW4",
    description: "Call of Duty: Modern Warfare 4: Release am 23. Oktober 2026, Campaign Early Access ab 16. Oktober. Der MW4-Tab fokussiert jetzt Launch-Daten, Season-1-Anbindung, Warzone-Start und die wichtigsten Season-1-Infos.",
    updateTime: "Aktualisiert: 24. September 2026",
    updateSummary: "Neu: Launch-Übersicht, Season-1-Status und Warzone-Anbindung sind prominenter einsortiert.",
    stats: [
      ["Release", "23. Okt. 2026"],
      ["Early Access", "Kampagne ab 16. Okt."],
      ["Season 1", "Datum noch offen"],
      ["Warzone", "Start mit Season 1"],
      ["Plattformen", "PS5, Xbox, PC, Switch 2"],
      ["Fokus", "Launch & Season 1"],
    ],
  };

  const sections = [
    {
      id: "launch",
      label: "Launch",
      eyebrow: "Launch-Fahrplan",
      title: "Wann startet Modern Warfare 4?",
      text: "Modern Warfare 4 erscheint offiziell am Freitag, 23. Oktober 2026. Wer digital vorbestellt, bekommt Campaign Early Access ab Freitag, 16. Oktober 2026. Ein konkreter globaler Uhrzeit-Plan wurde von Call of Duty noch nicht final veröffentlicht.",
      bullets: [
        "Release: Freitag, 23. Oktober 2026.",
        "Campaign Early Access: Freitag, 16. Oktober 2026 fuer digitale Vorbesteller.",
        "Studio: Infinity Ward fuehrt die Entwicklung.",
        "Plattformen: PlayStation 5, Xbox Series X|S, PC und Nintendo Switch 2.",
        "Globale Unlock-Zeiten sind noch nicht offiziell final bestaetigt.",
      ],
      cards: [
        ["Campaign Early Access", "16. Oktober 2026"],
        ["Full Launch", "23. Oktober 2026"],
        ["Unlock-Zeit", "noch offen"],
      ],
    },
    {
      id: "season1",
      label: "Season 1",
      eyebrow: "Season-1-Status",
      title: "Wann beginnt Season 1?",
      text: "Season 1 ist als gemeinsame Content-Phase fuer Modern Warfare 4 und Warzone eingeordnet. Ein exaktes Startdatum ist aber noch nicht final genannt.",
      bullets: [
        "Season-1-Start: offiziell noch offen; Call of Duty hat noch kein fixes Datum genannt.",
        "Warzone bekommt MW4-Inhalte und Progression mit Season 1.",
        "Die Signature Weapon Collection ist in MW4 zum Launch verfuegbar und in Warzone ab Season 01.",
        "Battle Pass, BlackCell und Tier Skips werden nutzbar, sobald der Season-01-Battle-Pass im Spiel verfuegbar ist.",
        "Warzone auf PS4 und Xbox One endet mit dem Start von MW4 Season 1.",
        "Das Season-1-Datum bleibt hier offen, bis ein fixes Datum da ist.",
      ],
      cards: [
        ["Season-1-Start", "offiziell noch offen"],
        ["Warzone-Integration", "ab Season 1"],
        ["Last-Gen Warzone", "endet mit Season 1"],
      ],
    },
    {
      id: "overview",
      label: "Übersicht",
      eyebrow: "Kurzüberblick",
      title: "Was ist bis zum Launch wichtig?",
      text: "Der aktuelle Stand buendelt Launch, Campaign Early Access, Multiplayer, DMZ, Warzone-Anbindung und die wichtigsten September-Updates.",
      bullets: [
        "Der MW4-Info-Hub wurde zuletzt am 9. September 2026 aktualisiert.",
        "Der Tokyo-Games-Show Campaign Trailer und der Story-Recap sind wichtige September-Updates.",
        "Open Beta, Maps/Modes, Waffen, Progression und Beta Patch Notes sind bereits eingeordnet.",
        "Warzone bindet MW4-Inhalte und Progression ab Season 1 ein.",
      ],
      cards: [
        ["Neuester Hub-Stand", "9. September"],
        ["Story-Recap", "16. September"],
        ["TGS Trailer", "17. September"],
      ],
    },
    {
      id: "campaign",
      label: "Kampagne",
      eyebrow: "No line holds forever",
      title: "Korea, Price und ein global eskalierender Krieg",
      text: "Die Kampagne startet auf der koreanischen Halbinsel. Der aktuelle Story-Recap und der Campaign-Trailer bereiten die Ereignisse vor, damit Spieler vor dem Early Access am 16. Oktober den wichtigsten Kontext haben.",
      bullets: [
        "Schauplätze: Korea, New York, Paris, Mumbai und weitere internationale Einsatzorte.",
        "Captain Price agiert nach den Ereignissen von MWIII als gejagter Outlaw mit persönlicher Agenda.",
        "Neue Squad-Mitglieder wie Park, Jay, Cho und Moon sollen die Korea-Perspektive stärker tragen.",
        "Der September-Story-Recap fasst die Vorgeschichte für Modern Warfare 4 offiziell zusammen.",
      ],
      cards: [
        ["Ton", "düsterer, persönlicher, näher an Frontsoldaten"],
        ["Setpieces", "Gräben, Stadtangriffe, Nacht-Raids, Verfolgungen"],
        ["Story-Fokus", "Price kollidiert mit den Kräften hinter der Invasion"],
      ],
    },
    {
      id: "multiplayer",
      label: "Multiplayer",
      eyebrow: "Gunplay & Progression",
      title: "Mehr Kontrolle, weniger Bloom, neue Build-Systeme",
      text: "MW4 setzt offiziell auf ein Waffen-zuerst-Technikpaket namens Ballistic Authority. Zusätzlich sind Open-Beta-Maps, Modi, Waffen, Progression, Rewards und Beta Patch Notes inzwischen offiziell dokumentiert.",
      bullets: [
        "Launch-Plan: 12 Core-Maps plus eigene Gunfight-Maps und Big-War-Maps.",
        "Ballistic Authority richtet Bullet Trajectory, Weapon Motion, Operator-Stance, Kamera, Audio, FOV und Sichtbarkeit stärker aufeinander aus.",
        "Gunsmith kehrt zurück; Gunny kann auf Basis deiner Unlocks schnelle Close-, Mid- oder Long-Range-Builds vorschlagen.",
        "Apex Attachments werden über voll gelevelte Waffen freigeschaltet und können Handling, Feuerverhalten, Utility, Stealth oder Rolle deutlich verändern.",
        "Zwei Prestige-Wege: Classic Prestige mit Reset und Regular Prestige ohne Create-a-Class-Reset.",
        "Die Beta umfasst unter anderem Core 6v6, neue Modi, Gunfight auf Kill Block, Ground War: Combat Outpost und Warzone Resurgence auf Zodiac.",
      ],
      cards: [
        ["Kill Block", "Trainingsanlage mit über 500 Konfigurationen"],
        ["Kompetitiv", "Settings für hohe FPS und Reaktionsfähigkeit"],
        ["Beta", "Maps, Modi, Waffen und Rewards offiziell"],
      ],
    },
    {
      id: "dmz",
      label: "DMZ",
      eyebrow: "Extraction kehrt zurück",
      title: "DMZ wird wieder ein eigener Schwerpunkt",
      text: "DMZ ist offiziell zurück und wird als lebendige Extraction-Sandbox beschrieben. Solo oder im Squad gehst du als inoffizieller Asset in umkämpfte Zonen, sicherst Technologie und entscheidest, wann du extrahierst.",
      bullets: [
        "Die Zone soll mit Wetter, militärischen Zielen und feindlichen Kräften dynamischer reagieren.",
        "Der Fokus liegt auf Looten, Kämpfen, Verhandeln, Verrat und Extraktion mit begrenztem Risiko-Budget.",
        "Vault-Edition-Besitzer bekommen einen DMZ Deployment Bonus zum Launch; genaue Inhalte folgen noch.",
        "Der erste offizielle DMZ-Blick ist für den 7. Juni angekündigt.",
      ],
      cards: [
        ["Spielstil", "Solo oder Squad"],
        ["Risiko", "je tiefer du gehst, desto härter schlägt die Zone zurück"],
        ["Status", "mehr Gameplay-Details stehen noch aus"],
      ],
    },
    {
      id: "warzone",
      label: "Warzone & Plattformen",
      eyebrow: "Season-1-Anbindung",
      title: "Warzone wird mit Season 1 umgestellt",
      text: "Modern Warfare 4 startet am 23. Oktober. Die Warzone-Anbindung ist fuer Season 1 bestaetigt: Ab dann werden MW4-Inhalte und Progression mit Warzone verbunden.",
      bullets: [
        "Warzone auf PS4 und Xbox One ist ab MW4 Season 1 nicht mehr spielbar.",
        "Neue Downloads für Warzone auf PS4/Xbox One enden am 4. Juni; der Ingame-Store dort wird am 25. Juni entfernt.",
        "MW4 erscheint für PS5, Xbox Series X|S, PC und Nintendo Switch 2.",
        "PC-Version: Battle.net, Steam und Xbox on PC; High-End-Features wie DLSS 4.5 und erweitertes Raytracing sind offiziell genannt.",
        "Switch 2 wird nativ mit Digital Legends entwickelt; Preorder-Details kommen später im Sommer.",
      ],
      cards: [
        ["Warzone Cutoff", "PS4/Xbox One endet mit Season 1"],
        ["PC", "Beenox optimiert die PC-Version"],
        ["Xbox", "Xbox Play Anywhere für Xbox/PC-Kauf"],
      ],
    },
    {
      id: "editions",
      label: "Editionen",
      eyebrow: "Preorder & Boni",
      title: "Standard, Vault und Loyalty Discount",
      text: "Zum Start der Preorders gibt es Digital Standard, Digital Vault und Physical Standard auf Konsolen. Alle Preorders sichern Open-Beta-Early-Access; digitale Preorders liefern zusätzlich sofort den Hunter Killer Skin für Black Ops 7 und Warzone.",
      bullets: [
        "Vault Edition: Hostile Alliance Operator Pack mit Price, Valeria, Ghost und Blix.",
        "Special Forces Operator Pack: COM SFT, KSK Amphibious, South Korean Special Forces und SAS.",
        "Signature Weapon Collection: fünf Waffen/Blueprints inklusive Apex-Attachment-Bezug.",
        "BlackCell für eine Season ist enthalten; erster möglicher Einsatz ist Season 1.",
        "Loyalty Discount: 10% auf die Vault Edition für berechtigte Besitzer/Spieler früherer Premium-CoDs, gültig bis 23. Oktober 2026 und nur für digitale Preorders/Prepurchases.",
      ],
      cards: [
        ["Digital Standard", "Spiel + Beta Early Access + Hunter Killer"],
        ["Digital Vault", "Premium-Packs + BlackCell + DMZ Bonus"],
        ["Upgrade", "$30 SRP regional angepasst"],
      ],
    },
  ];

  const groups = [
    { id: "launch", label: "Launch", sectionIds: ["launch", "season1", "warzone"] },
    { id: "gameplay", label: "Gameplay", sectionIds: ["campaign", "multiplayer", "dmz"] },
    { id: "more", label: "Extras", sectionIds: ["overview", "editions"] },
  ];

  let activeMw4Tab = "launch";

  function html(value) {
    return String(value || "")
      .replace(/&/g, "&amp;")
      .replace(/</g, "&lt;")
      .replace(/>/g, "&gt;")
      .replace(/"/g, "&quot;");
  }

  function installStyle() {
    let style = document.getElementById(STYLE_ID);
    if (!style) {
      style = document.createElement("style");
      style.id = STYLE_ID;
      document.head.append(style);
    }

    style.textContent = `
      body .mw4-watch-panel {
        --mw4-gold: #d8b457;
        --mw4-gold-rgb: 216, 180, 87;
        --mw4-ink: #070b0e;
      }

      body .mw4-watch-panel .mode-info-hero,
      body .mw4-watch-panel .mode-info-side,
      body .mw4-watch-panel .mode-update-box,
      body .mw4-watch-panel #modeInfoCards,
      body .mw4-watch-panel #modeInfoTips,
      body .mw4-watch-panel #modeInfoGallery,
      body .mw4-watch-panel .official-note,
      body .mw4-watch-panel #modeInfoTabs {
        display: none !important;
      }

      body .mw4-watch-panel .mode-info-layout {
        display: block !important;
      }

      body .mw4-watch-panel .mode-info-main {
        overflow: hidden !important;
        width: 100% !important;
        max-width: none !important;
        border-color: rgba(var(--mw4-gold-rgb), 0.28) !important;
        border-radius: 10px !important;
        background: #070b0e !important;
        box-shadow: 0 12px 30px rgba(0, 0, 0, 0.28) !important;
      }

      body .mw4-watch-panel #modeInfoImage {
        display: block !important;
        width: 100% !important;
        height: auto !important;
        max-height: none !important;
        aspect-ratio: auto !important;
        object-fit: contain !important;
        object-position: center !important;
        border: 0 !important;
        border-bottom: 1px solid rgba(var(--mw4-gold-rgb), 0.32) !important;
        border-radius: 10px 10px 0 0 !important;
        background: #05070a !important;
      }

      body .mw4-watch-panel .mode-info-body {
        padding: clamp(1rem, 1.8vw, 1.55rem) !important;
      }

      body .mw4-watch-panel .mw4-updated-pill {
        display: none !important;
      }

      body .mw4-watch-panel #modeInfoStats {
        display: grid !important;
        grid-template-columns: repeat(6, minmax(0, 1fr)) !important;
        gap: 0.55rem !important;
        margin-bottom: 0.8rem !important;
      }

      body .mw4-watch-panel #modeInfoStats > div,
      body .mw4-mini-card {
        border: 1px solid rgba(var(--mw4-gold-rgb), 0.24) !important;
        border-radius: 8px !important;
        padding: 0.72rem 0.8rem !important;
        background: linear-gradient(145deg, rgba(var(--mw4-gold-rgb), 0.12), rgba(8, 12, 16, 0.92)) !important;
        box-shadow: none !important;
      }

      body .mw4-watch-panel #modeInfoStats span,
      body .mw4-mini-card span,
      body .mw4-kicker {
        display: block !important;
        color: rgba(255, 244, 207, 0.72) !important;
        font-size: 0.72rem !important;
        font-weight: 950 !important;
        text-transform: uppercase !important;
        letter-spacing: 0.045em !important;
      }

      body .mw4-watch-panel #modeInfoStats strong,
      body .mw4-mini-card strong {
        color: #ffe08a !important;
        font-family: Rajdhani, Inter, sans-serif !important;
        font-size: 1.08rem !important;
        line-height: 1 !important;
      }

      body .mw4-dashboard {
        display: grid !important;
        gap: 1rem !important;
      }

      body .mw4-topline {
        display: flex !important;
        flex-wrap: wrap !important;
        align-items: center !important;
        justify-content: space-between !important;
        gap: 0.7rem !important;
        margin-bottom: 0.15rem !important;
      }

      body .mw4-updated-pill {
        display: inline-flex !important;
        padding: 0.34rem 0.62rem !important;
        border: 1px solid rgba(var(--mw4-gold-rgb), 0.3) !important;
        border-radius: 999px !important;
        color: rgba(255, 244, 207, 0.84) !important;
        background: rgba(5, 8, 11, 0.72) !important;
        font-size: 0.75rem !important;
        font-weight: 850 !important;
      }

      body .mw4-tabs {
        display: flex !important;
        flex-wrap: wrap !important;
        gap: 0.34rem !important;
        padding: 0.25rem !important;
        border: 1px solid rgba(255, 255, 255, 0.09) !important;
        border-radius: 0.5rem !important;
        background: rgba(5, 8, 11, 0.78) !important;
      }

      body .mw4-tab {
        min-height: 2.05rem !important;
        padding: 0.38rem 0.82rem !important;
        border: 1px solid transparent !important;
        border-radius: 0.34rem !important;
        background: transparent !important;
        color: rgba(238, 232, 214, 0.78) !important;
        font-weight: 900 !important;
        cursor: pointer !important;
      }

      body .mw4-tab.active,
      body .mw4-tab:hover,
      body .mw4-tab:focus-visible {
        border-color: rgba(var(--mw4-gold-rgb), 0.72) !important;
        background: linear-gradient(135deg, #ffe08a, #d8b457) !important;
        color: #120e04 !important;
        outline: none !important;
      }

      body .mw4-panel {
        display: none !important;
      }

      body .mw4-panel.active {
        display: grid !important;
        gap: 0.78rem !important;
        align-items: start !important;
      }

      body .mw4-section-card {
        display: grid !important;
        grid-template-columns: minmax(0, 1.08fr) minmax(17rem, 0.92fr) !important;
        gap: 0.78rem !important;
        align-items: start !important;
      }

      body .mw4-official-card {
        border: 1px solid rgba(var(--mw4-gold-rgb), 0.22) !important;
        border-radius: 10px !important;
        background: linear-gradient(145deg, rgba(var(--mw4-gold-rgb), 0.1), rgba(7, 11, 14, 0.96)) !important;
        padding: clamp(1rem, 1.65vw, 1.35rem) !important;
        box-shadow: none !important;
      }

      body .mw4-official-card h3 {
        margin: 0 0 0.7rem !important;
        color: #ffe08a !important;
        font-family: Rajdhani, Inter, sans-serif !important;
        font-size: clamp(1.55rem, 2.3vw, 2.25rem) !important;
        line-height: 0.98 !important;
      }

      body .mw4-official-card p {
        margin: 0 0 0.78rem !important;
        color: rgba(242, 236, 220, 0.9) !important;
        line-height: 1.62 !important;
      }

      body .mw4-official-card ul {
        display: grid !important;
        gap: 0.44rem !important;
        margin: 0.35rem 0 0 !important;
        padding: 0 !important;
        list-style: none !important;
      }

      body .mw4-official-card li {
        position: relative !important;
        padding-left: 1.05rem !important;
        color: rgba(242, 236, 220, 0.9) !important;
        line-height: 1.48 !important;
      }

      body .mw4-official-card li::before {
        content: "" !important;
        position: absolute !important;
        left: 0 !important;
        top: 0.62em !important;
        width: 0.36rem !important;
        height: 0.36rem !important;
        border-radius: 999px !important;
        background: #d8b457 !important;
      }

      body .mw4-side-stack {
        display: grid !important;
        gap: 0.65rem !important;
      }

      body .mw4-video-frame {
        position: relative !important;
        width: 100% !important;
        aspect-ratio: 16 / 9 !important;
        overflow: hidden !important;
        border: 1px solid rgba(var(--mw4-gold-rgb), 0.22) !important;
        border-radius: 10px !important;
        background: #05070a !important;
      }

      body .mw4-video-frame iframe {
        position: absolute !important;
        inset: 0 !important;
        width: 100% !important;
        height: 100% !important;
        border: 0 !important;
      }

      body .mw4-mini-grid {
        display: grid !important;
        grid-template-columns: repeat(3, minmax(0, 1fr)) !important;
        gap: 0.55rem !important;
      }

      body .mw4-gallery {
        display: grid !important;
        grid-template-columns: repeat(4, minmax(0, 1fr)) !important;
        gap: 0.55rem !important;
      }

      body .mw4-gallery img {
        display: block !important;
        width: 100% !important;
        aspect-ratio: 16 / 9 !important;
        object-fit: cover !important;
        border-radius: 8px !important;
        border: 1px solid rgba(var(--mw4-gold-rgb), 0.2) !important;
        background: #05070a !important;
      }

      body .mw4-source-list a {
        color: #ffe08a !important;
        text-decoration: none !important;
        font-weight: 850 !important;
      }

      @media (max-width: 1040px) {
        body .mw4-watch-panel #modeInfoStats {
          grid-template-columns: repeat(3, minmax(0, 1fr)) !important;
        }

        body .mw4-section-card,
        body .mw4-mini-grid {
          grid-template-columns: 1fr !important;
        }
      }

      @media (max-width: 640px) {
        body .mw4-watch-panel #modeInfoStats,
        body .mw4-gallery {
          grid-template-columns: 1fr !important;
        }

        body .mw4-tabs {
          border-radius: 10px !important;
        }

        body .mw4-tab {
          flex: 1 1 100% !important;
        }
      }
    `;
  }

  function getMw4Button() {
    return Array.from(document.querySelectorAll(".primary-mode-switch .mode-button, .primary-mode-switch button"))
      .find((button) => String(button.dataset.mode || "").toLowerCase() === "mw4-info" || (button.textContent || "").trim().toLowerCase() === "mw4") || null;
  }

  function prepareMw4Button() {
    const button = getMw4Button();
    if (!button) return null;
    button.classList.add("mode-button", "mw4-mode-button");
    button.dataset.mode = "mw4-info";
    button.type = "button";
    return button;
  }

  function setText(selector, value) {
    const element = document.querySelector(selector);
    if (element) element.textContent = value;
  }

  function renderStats() {
    const stats = document.querySelector(".mw4-watch-panel #modeInfoStats");
    if (!stats) return;
    stats.innerHTML = facts.stats.map(([label, value]) => `<div><span>${html(label)}</span><strong>${html(value)}</strong></div>`).join("");
  }

  function cleanupModeInfoBody() {
    document.querySelectorAll("#mw4LivePanel, .mw4-dashboard, .mw4-prose, .mw4-updated-pill, .season4-prose, .season4-updated-pill, #season4TrailerPanel, #season4KeyArtPanel").forEach((node) => node.remove());
    const cards = document.querySelector("#modeInfoCards");
    if (cards) {
      cards.innerHTML = "";
      cards.hidden = true;
      cards.style.display = "none";
    }
    const tips = document.querySelector("#modeInfoTips");
    if (tips) tips.innerHTML = "";
    const gallery = document.querySelector("#modeInfoGallery");
    if (gallery) {
      gallery.innerHTML = "";
      gallery.hidden = true;
      gallery.style.display = "none";
    }
  }

  function renderSectionCard(section) {
    const isOverview = section.id === "overview";
    return `
      <section class="mw4-section-card">
        <article class="mw4-official-card">
          <span class="mw4-kicker">${html(section.eyebrow)}</span>
          <h3>${html(section.title)}</h3>
          <p>${html(section.text)}</p>
          <ul>${section.bullets.map((item) => `<li>${html(item)}</li>`).join("")}</ul>
        </article>
        <aside class="mw4-side-stack">
          ${isOverview ? `<div class="mw4-video-frame"><iframe src="https://www.youtube-nocookie.com/embed/${revealTrailerId}" title="Call of Duty: Modern Warfare 4 Reveal Trailer" loading="lazy" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" allowfullscreen></iframe></div>` : ""}
          <div class="mw4-mini-grid">${section.cards.map(([label, value]) => `<div class="mw4-mini-card"><span>${html(label)}</span><strong>${html(value)}</strong></div>`).join("")}</div>
        </aside>
      </section>
    `;
  }

  function renderGroup(group) {
    const groupSections = group.sectionIds.map((id) => sections.find((section) => section.id === id)).filter(Boolean);
    return `
      <section class="mw4-panel${group.id === activeMw4Tab ? " active" : ""}" data-mw4-panel="${html(group.id)}">
        ${groupSections.map(renderSectionCard).join("")}
      </section>
    `;
  }

  function renderMw4Dashboard() {
    const body = document.querySelector(".mw4-watch-panel .mode-info-body");
    if (!body) return;

    const wrapper = document.createElement("div");
    wrapper.className = "mw4-dashboard";
    wrapper.id = "mw4LivePanel";
    wrapper.innerHTML = `
      <div class="mw4-topline">
        <span class="mw4-updated-pill">${html(facts.updateTime)}</span>
        <nav class="mw4-tabs" aria-label="MW4-Unterbereiche">
          ${groups.map((group) => `<button class="mw4-tab${group.id === activeMw4Tab ? " active" : ""}" data-mw4-tab="${html(group.id)}" type="button">${html(group.label)}</button>`).join("")}
        </nav>
      </div>
      ${groups.map(renderGroup).join("")}
      <section class="mw4-official-card">
        <h3>Bilder</h3>
        <div class="mw4-gallery">${officialImages.map((src, index) => `<img src="${src}" alt="Modern Warfare 4 offizielles Reveal-Bild ${index + 1}" loading="lazy" decoding="async">`).join("")}</div>
      </section>
    `;

    wrapper.addEventListener("click", (event) => {
      const button = event.target.closest("[data-mw4-tab]");
      if (!button) return;
      activeMw4Tab = button.dataset.mw4Tab;
      wrapper.querySelectorAll("[data-mw4-tab]").forEach((item) => item.classList.toggle("active", item === button));
      wrapper.querySelectorAll("[data-mw4-panel]").forEach((panel) => panel.classList.toggle("active", panel.dataset.mw4Panel === activeMw4Tab));
    });

    body.appendChild(wrapper);
  }

  function activateMw4(button = prepareMw4Button()) {
    if (!button) return;
    installStyle();

    document.querySelectorAll(".primary-mode-switch .mode-button").forEach((item) => item.classList.toggle("active", item === button));
    document.querySelectorAll(".secondary-mode-switch .mode-button").forEach((item) => item.classList.remove("active"));

    const contentTabs = document.querySelector("#contentTabs");
    if (contentTabs) contentTabs.hidden = true;
    const modeInfoTabs = document.querySelector("#modeInfoTabs");
    if (modeInfoTabs) {
      modeInfoTabs.hidden = true;
      modeInfoTabs.style.display = "none";
    }

    document.querySelectorAll(".tab-panel").forEach((panel) => {
      const active = panel.dataset.panel === "mode-info";
      panel.hidden = !active;
      panel.classList.toggle("active", active);
    });

    const panel = document.querySelector('[data-panel="mode-info"]');
    if (panel) {
      panel.classList.add("mw4-watch-panel");
      panel.classList.remove("season4-watch-panel");
    }

    setText("#tierTitle", facts.title);
    setText("#tierDescription", facts.description);
    setText("#modeInfoTitle", facts.title);
    setText("#modeInfoDescription", facts.description);
    setText("#modeInfoKicker", "Offiziell enthüllt");
    setText("#modeInfoUpdateTime", facts.updateTime);
    setText("#modeInfoUpdateSummary", facts.updateSummary);

    const image = document.querySelector("#modeInfoImage");
    if (image) {
      image.src = officialHero;
      image.alt = "Offizielles Modern Warfare 4 Reveal Key Art";
      image.loading = "eager";
      image.decoding = "async";
    }

    cleanupModeInfoBody();
    renderStats();
    renderMw4Dashboard();
    window.dispatchEvent(new CustomEvent("loadoutlab:lite-render", { detail: { panel: "mw4" } }));
  }

  function bindMw4() {
    installStyle();
    const button = prepareMw4Button();
    if (!button || button.dataset.mw4Bound === "true") return;
    button.dataset.mw4Bound = "true";
  }

  document.addEventListener("click", (event) => {
    const button = event.target.closest(".mw4-mode-button, [data-mode='mw4-info']");
    if (!button) return;
    event.preventDefault();
    event.stopPropagation();
    event.stopImmediatePropagation?.();
    activateMw4(button);
  }, true);

  function init() {
    bindMw4();
    const shouldOpenMw4 = ["#mw4", "#intel"].includes(window.location.hash) || getMw4Button()?.classList.contains("active");
    if (shouldOpenMw4) activateMw4();
  }

  if (document.readyState === "loading") document.addEventListener("DOMContentLoaded", init, { once: true });
  else init();

  window.setTimeout(bindMw4, 300);
}());

/* source: mw4-no-tabs.js */
(function () {
  const STYLE_ID = "mw4-no-tabs-20260601";

  function installStyle() {
    let style = document.getElementById(STYLE_ID);
    if (!style) {
      style = document.createElement("style");
      style.id = STYLE_ID;
      document.head.append(style);
    }

    style.textContent = `
      body .mw4-watch-panel .mw4-tabs {
        display: flex !important;
      }

      body .mw4-watch-panel .mw4-panel:not(.active),
      body .mw4-watch-panel .mw4-panel[hidden] {
        display: none !important;
      }

      body .mw4-watch-panel .mw4-panel.active {
        display: grid !important;
        gap: 0.78rem !important;
        align-items: start !important;
      }

      body .mw4-watch-panel .mw4-section-card {
        display: grid !important;
        grid-template-columns: minmax(0, 1.08fr) minmax(17rem, 0.92fr) !important;
        gap: 0.78rem !important;
        align-items: start !important;
      }

      body .mw4-watch-panel .mw4-panel + .mw4-panel {
        margin-top: 0.9rem !important;
      }

      body .mw4-watch-panel .mw4-topline {
        justify-content: flex-start !important;
        margin-bottom: 0 !important;
      }

      @media (max-width: 1040px) {
        body .mw4-watch-panel .mw4-section-card {
          grid-template-columns: 1fr !important;
        }
      }
    `;
  }

  function preserveMw4Tabs() {
    installStyle();

    const wrapper = document.querySelector("#mw4LivePanel");
    if (!wrapper) return;

    wrapper.querySelectorAll(".mw4-tabs").forEach((tabs) => {
      tabs.hidden = false;
      tabs.removeAttribute("aria-hidden");
    });

    const active = wrapper.querySelector("[data-mw4-tab].active")?.dataset.mw4Tab || "overview";
    wrapper.querySelectorAll("[data-mw4-panel]").forEach((panel) => {
      const isActive = panel.dataset.mw4Panel === active;
      panel.hidden = !isActive;
      panel.classList.toggle("active", isActive);
    });
  }

  function schedulePreserveTabs() {
    requestAnimationFrame(preserveMw4Tabs);
    window.setTimeout(preserveMw4Tabs, 80);
  }

  window.addEventListener("loadoutlab:lite-render", schedulePreserveTabs);
  document.addEventListener("click", (event) => {
    if (event.target.closest("[data-mw4-tab], .mw4-mode-button, [data-mode='mw4-info']")) schedulePreserveTabs();
  }, true);

  if (document.readyState === "loading") document.addEventListener("DOMContentLoaded", schedulePreserveTabs, { once: true });
  else schedulePreserveTabs();

  window.setTimeout(preserveMw4Tabs, 350);
  window.setTimeout(preserveMw4Tabs, 1000);
}());

/* source: season4.js */
(function () {
  const STYLE_ID = "season4-live-20260603-style";
  const DATA_URL = "data/season-watch.json";
  let seasonData = null;

  function html(value) {
    return String(value || "")
      .replace(/&/g, "&amp;")
      .replace(/</g, "&lt;")
      .replace(/>/g, "&gt;")
      .replace(/\"/g, "&quot;");
  }

  async function fetchSeasonData() {
    try {
      const response = await fetch(`${DATA_URL}?v=${Date.now()}`, { cache: "no-store" });
      if (!response.ok) return null;
      seasonData = await response.json();
      return seasonData;
    } catch {
      return null;
    }
  }

  function installStyle() {
    let style = document.getElementById(STYLE_ID);
    if (!style) {
      style = document.createElement("style");
      style.id = STYLE_ID;
      document.head.append(style);
    }

    style.textContent = `
      body .season4-watch-panel {
        --season-green: #b9ff3d;
        --season-green-rgb: 185, 255, 61;
      }

      body .season4-watch-panel .mode-info-main {
        width: 100% !important;
        max-width: none !important;
        border-color: rgba(var(--season-green-rgb), 0.28) !important;
        background:
          radial-gradient(circle at 16% 0%, rgba(var(--season-green-rgb), 0.12), transparent 22rem),
          linear-gradient(145deg, rgba(12, 18, 21, 0.96), rgba(6, 9, 12, 0.98)) !important;
      }

      body .season4-watch-panel #modeInfoTabs {
        display: none !important;
      }

      body .season4-watch-panel #modeInfoImage {
        display: block !important;
        width: 100% !important;
        height: auto !important;
        max-height: none !important;
        aspect-ratio: auto !important;
        object-fit: cover !important;
        object-position: center !important;
        border-bottom: 1px solid rgba(var(--season-green-rgb), 0.24) !important;
      }

      body .season4-watch-panel #modeInfoStats {
        display: grid !important;
        grid-template-columns: repeat(4, minmax(0, 1fr)) !important;
        gap: 0.7rem !important;
        margin-bottom: 0.95rem !important;
      }

      body .season4-watch-panel #modeInfoStats > div {
        border: 1px solid rgba(var(--season-green-rgb), 0.24) !important;
        border-radius: 8px !important;
        padding: 0.75rem 0.82rem !important;
        background: linear-gradient(145deg, rgba(var(--season-green-rgb), 0.11), rgba(9, 13, 16, 0.92)) !important;
      }

      body .season4-watch-panel #modeInfoStats span {
        display: block !important;
        color: rgba(240, 247, 228, 0.74) !important;
        font-size: 0.72rem !important;
        font-weight: 900 !important;
        text-transform: uppercase !important;
      }

      body .season4-watch-panel #modeInfoStats strong {
        color: #e5ff9f !important;
        font-family: Rajdhani, Inter, sans-serif !important;
        font-size: 1.1rem !important;
        line-height: 1 !important;
      }

      body .season4-watch-panel #modeInfoCards {
        display: grid !important;
        grid-template-columns: repeat(2, minmax(0, 1fr)) !important;
        gap: 0.8rem !important;
      }

      body .season4-watch-panel #modeInfoCards article {
        border: 1px solid rgba(var(--season-green-rgb), 0.2) !important;
        border-radius: 8px !important;
        padding: 0.85rem 0.9rem !important;
        background: rgba(8, 12, 15, 0.72) !important;
      }

      body .season4-watch-panel #modeInfoCards span {
        color: var(--season-green) !important;
        font-size: 0.72rem !important;
        font-weight: 950 !important;
        text-transform: uppercase !important;
      }

      body .season4-watch-panel #modeInfoCards h3 {
        margin: 0.22rem 0 0.4rem !important;
        color: #f6ffe2 !important;
        font-family: Rajdhani, Inter, sans-serif !important;
        font-size: 1.2rem !important;
        line-height: 1 !important;
      }

      body .season4-watch-panel #modeInfoCards p {
        margin: 0 !important;
        color: rgba(234, 240, 228, 0.88) !important;
        line-height: 1.58 !important;
      }

      body .season4-watch-panel #modeInfoGallery {
        display: grid !important;
        grid-template-columns: repeat(2, minmax(0, 1fr)) !important;
        gap: 0.8rem !important;
      }

      body .season4-watch-panel #modeInfoGallery figure {
        margin: 0 !important;
        overflow: hidden !important;
        border: 1px solid rgba(var(--season-green-rgb), 0.18) !important;
        border-radius: 8px !important;
        background: rgba(5, 8, 11, 0.82) !important;
      }

      body .season4-watch-panel #modeInfoGallery img {
        display: block !important;
        width: 100% !important;
        aspect-ratio: 16 / 9 !important;
        object-fit: cover !important;
      }

      body .season4-watch-panel #modeInfoGallery figcaption {
        padding: 0.62rem 0.74rem !important;
        color: #f3f8ea !important;
        font-size: 0.88rem !important;
        font-weight: 800 !important;
      }

      @media (max-width: 900px) {
        body .season4-watch-panel #modeInfoStats,
        body .season4-watch-panel #modeInfoCards,
        body .season4-watch-panel #modeInfoGallery {
          grid-template-columns: 1fr !important;
        }
      }
    `;
  }

  function getSeason4Button() {
    return Array.from(document.querySelectorAll(".primary-mode-switch .mode-button, .primary-mode-switch button"))
      .find((button) => {
        const label = (button.textContent || "").replace(/\s+/g, " ").trim().toLowerCase();
        const mode = String(button.dataset.mode || "").toLowerCase();
        return mode === "season4-info" || mode === "season-4-info" || label.includes("season 4");
      }) || null;
  }

  function prepareSeason4Button() {
    const button = getSeason4Button();
    if (!button) return null;
    button.classList.add("mode-button", "season4-mode-button", "season-mode-button");
    button.dataset.mode = "season4-info";
    button.type = "button";
    return button;
  }

  function setText(selector, value) {
    const element = document.querySelector(selector);
    if (element) element.textContent = value;
  }

  function renderSeason4() {
    const data = seasonData;
    if (!data) return;

    const featured = data.featuredPost || {};
    const seasonLabel = data.season || "Season 4";
    const button = prepareSeason4Button();
    if (button) button.textContent = seasonLabel.replace(/^Season 0/, "Season ");
    const panel = document.querySelector('[data-panel="mode-info"]');
    if (panel) {
      panel.classList.add("season4-watch-panel");
      panel.classList.remove("mw4-watch-panel");
    }

    setText("#tierTitle", seasonLabel);
    setText("#tierDescription", `Offizielle ${seasonLabel}-News, Live-Seasons, Events und Start-Hinweise aus Call-of-Duty-Quellen.`);
    setText("#modeInfoTitle", seasonLabel);
    setText("#modeInfoDescription", "Automatisch beobachtet: offizieller Call-of-Duty-Blog plus der offizielle X-Kanal CODLiveSeasons fuer Season-, Event- und Live-Updates.");
    setText("#modeInfoKicker", `${seasonLabel} Live`);
    setText("#modeInfoUpdateTime", data.generatedAtLabel || `${seasonLabel} Watch`);
    setText("#modeInfoUpdateSummary", data.officialSummary || `Offizielle ${seasonLabel}-Infos wurden aktualisiert.`);

    const modeInfoTabs = document.querySelector("#modeInfoTabs");
    if (modeInfoTabs) {
      modeInfoTabs.hidden = true;
      modeInfoTabs.style.display = "none";
    }

    const stats = document.querySelector("#modeInfoStats");
    if (stats) {
      stats.innerHTML = (data.stats || []).map(([label, value]) => `
        <div>
          <span>${html(label)}</span>
          <strong>${html(value)}</strong>
        </div>
      `).join("");
    }

    const cards = document.querySelector("#modeInfoCards");
    if (cards) {
      cards.innerHTML = (data.cards || []).map((card) => `
        <article>
          <span>${html(card.label || "Offiziell")}</span>
          <h3>${html(card.title || seasonLabel)}</h3>
          <p>${html(card.text || "")}</p>
        </article>
      `).join("");
      cards.hidden = false;
      cards.style.display = "grid";
    }

    const tips = document.querySelector("#modeInfoTips");
    if (tips) {
      tips.innerHTML = (data.tips || []).map((tip, index) => `<li><span>${index + 1}</span>${html(tip)}</li>`).join("");
    }

    const gallery = document.querySelector("#modeInfoGallery");
    if (gallery) {
      const items = (data.gallery || []).length ? data.gallery : [[featured.title || seasonLabel, featured.imageUrl || "assets/cod-loadout-hero.png"]];
      gallery.innerHTML = items.map(([label, src]) => `
        <figure>
          <img src="${html(src)}" alt="${html(label)}" loading="lazy">
          <figcaption>${html(label)}</figcaption>
        </figure>
      `).join("");
      gallery.hidden = false;
      gallery.style.display = "grid";
    }

    const image = document.querySelector("#modeInfoImage");
    if (image) {
      image.src = featured.imageUrl || "assets/cod-loadout-hero.png";
      image.alt = featured.title || seasonLabel;
      image.loading = "eager";
      image.decoding = "async";
    }
  }

  function activateSeason4(button = prepareSeason4Button()) {
    if (!button) return;
    installStyle();

    document.querySelectorAll(".primary-mode-switch .mode-button").forEach((item) => item.classList.toggle("active", item === button));
    document.querySelectorAll(".secondary-mode-switch .mode-button").forEach((item) => item.classList.remove("active"));

    const contentTabs = document.querySelector("#contentTabs");
    if (contentTabs) contentTabs.hidden = true;

    document.querySelectorAll(".tab-panel").forEach((panel) => {
      const active = panel.dataset.panel === "mode-info";
      panel.hidden = !active;
      panel.classList.toggle("active", active);
    });

    renderSeason4();
    window.dispatchEvent(new CustomEvent("loadoutlab:lite-render", { detail: { panel: "season4" } }));
  }

  document.addEventListener("click", async (event) => {
    const button = event.target.closest(".season4-mode-button, .season-mode-button, [data-mode='season4-info'], [data-mode='season-4-info']");
    if (!button) return;
    event.preventDefault();
    event.stopPropagation();
    event.stopImmediatePropagation?.();
    if (!seasonData) await fetchSeasonData();
    activateSeason4(button);
  }, true);

  async function init() {
    installStyle();
    prepareSeason4Button();
    await fetchSeasonData();
    if (getSeason4Button()?.classList.contains("active")) activateSeason4();
  }

  if (document.readyState === "loading") document.addEventListener("DOMContentLoaded", init, { once: true });
  else init();

  window.setTimeout(prepareSeason4Button, 300);
}());

/* source: site-ui.js */
(function () {
  const STYLE_ID = "site-ui-20260529-style";
  const tabOrder = ["updates", "mw4-info", "season4-info"];
  let latestUpdates = null;
  let latestUpdatesPromise = null;

  const css = `
    body .tier-first {
      max-width: min(98.5rem, calc(100vw - clamp(1rem, 2.4vw, 2.75rem))) !important;
      padding-top: clamp(0.65rem, 1.2vw, 1rem) !important;
      margin-inline: auto !important;
    }

    body .tier-first .section-heading {
      position: relative !important;
      display: grid !important;
      grid-template-columns: minmax(0, 1fr) auto !important;
      grid-template-areas:
        "kicker date"
        "title date" !important;
      align-items: center !important;
      gap: 0.45rem 1rem !important;
      max-width: min(78rem, 100%) !important;
      margin: 0 0 0.6rem !important;
      padding: 0.78rem 0.95rem !important;
      border: 1px solid rgba(255, 255, 255, 0.105) !important;
      border-left: 0.26rem solid rgba(185, 255, 61, 0.74) !important;
      border-radius: 0.46rem !important;
      background:
        radial-gradient(circle at 86% 14%, rgba(255, 216, 106, 0.08), transparent 18rem),
        linear-gradient(135deg, rgba(185, 255, 61, 0.07), rgba(10, 14, 19, 0.9) 40%, rgba(6, 9, 12, 0.96)) !important;
      box-shadow: 0 1rem 2.15rem rgba(0, 0, 0, 0.3), inset 0 1px 0 rgba(255, 255, 255, 0.045) !important;
    }

    body .tier-first .section-heading::before {
      content: "" !important;
      position: absolute !important;
      inset: 0 auto 0 0 !important;
      width: 0.18rem !important;
      background: linear-gradient(180deg, #b9ff3d, rgba(41, 230, 129, 0.45)) !important;
      pointer-events: none !important;
    }

    body .tier-first .section-heading::after {
      content: "" !important;
      position: absolute !important;
      inset: 0 !important;
      border-radius: inherit !important;
      background: linear-gradient(180deg, rgba(255, 255, 255, 0.055), transparent 54%) !important;
      pointer-events: none !important;
    }

    body .tier-first .section-heading .eyebrow {
      position: relative !important;
      grid-area: kicker !important;
      width: fit-content !important;
      margin: 0 !important;
      padding: 0.18rem 0.48rem !important;
      border: 1px solid rgba(185, 255, 61, 0.28) !important;
      border-radius: 999px !important;
      background: rgba(185, 255, 61, 0.08) !important;
      color: #b9ff3d !important;
      font-size: 0.62rem !important;
      letter-spacing: 0 !important;
      line-height: 1 !important;
      z-index: 1 !important;
    }

    body .tier-first .section-heading #tierDescription {
      display: none !important;
    }

    body .tier-first h1 {
      position: relative !important;
      grid-area: title !important;
      max-width: 62rem !important;
      margin: 0 !important;
      color: #f4f7fb !important;
      font-size: clamp(2rem, 3.05vw, 3.05rem) !important;
      line-height: 0.96 !important;
      letter-spacing: 0 !important;
      text-wrap: balance !important;
      text-shadow: 0 0.75rem 1.9rem rgba(0, 0, 0, 0.32) !important;
      z-index: 1 !important;
    }

    body .tier-first .updated-note {
      position: relative !important;
      grid-area: date !important;
      justify-self: end !important;
      align-self: center !important;
      margin: 0 !important;
      min-height: 1.75rem !important;
      padding: 0.32rem 0.62rem !important;
      border-color: rgba(255, 216, 106, 0.24) !important;
      background: rgba(9, 13, 18, 0.76) !important;
      color: #d7dee8 !important;
      white-space: nowrap !important;
      font-size: 0.72rem !important;
      z-index: 1 !important;
    }

    body .primary-mode-switch {
      grid-template-columns: repeat(3, minmax(0, 1fr)) !important;
      max-width: min(78rem, 100%) !important;
      gap: 0.32rem !important;
      margin: 0 0 0.42rem !important;
      padding: 0.25rem !important;
      border: 1px solid rgba(255, 255, 255, 0.095) !important;
      border-radius: 0.5rem !important;
      background: rgba(7, 10, 14, 0.62) !important;
      box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.04) !important;
    }

    body .primary-mode-switch .mode-button {
      min-height: 2.72rem !important;
      border-radius: 0.36rem !important;
      padding: 0.4rem 0.7rem !important;
      font-size: 0.98rem !important;
      letter-spacing: 0 !important;
    }

    body .primary-mode-switch .mode-button::before {
      content: attr(data-kicker) !important;
      display: block !important;
      margin-bottom: 0.12rem !important;
      color: #b9ff3d !important;
      font-size: 0.58rem !important;
      font-weight: 950 !important;
      line-height: 1 !important;
      opacity: 0.95 !important;
    }

    body .primary-mode-switch .mode-button.active,
    body .secondary-mode-switch .mode-button.active,
    body .content-tab.active,
    body .filter-button.active {
      border-color: rgba(185, 255, 61, 0.92) !important;
      background: linear-gradient(135deg, #b9ff3d 0%, #66f244 45%, #24e66f 100%) !important;
      color: #051007 !important;
      text-shadow: none !important;
      box-shadow:
        0 0 0 1px rgba(185, 255, 61, 0.24),
        0 0.55rem 1.35rem rgba(42, 239, 111, 0.24),
        inset 0 1px 0 rgba(255, 255, 255, 0.26) !important;
    }

    body .primary-mode-switch .mode-button.active::before {
      color: #07100a !important;
    }

    body .primary-mode-switch .mode-button.active *,
    body .secondary-mode-switch .mode-button.active *,
    body .content-tab.active *,
    body .filter-button.active * {
      color: #051007 !important;
      text-shadow: none !important;
    }

    body .secondary-mode-switch,
    body .content-tabs {
      min-height: 2.5rem !important;
      margin-top: 0 !important;
      margin-bottom: 0.42rem !important;
      padding: 0.2rem !important;
      border: 1px solid rgba(255, 255, 255, 0.09) !important;
      background: rgba(7, 10, 14, 0.62) !important;
      box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.035) !important;
      vertical-align: top !important;
    }

    body .secondary-mode-switch {
      display: inline-grid !important;
      grid-template-columns: repeat(2, minmax(10rem, 1fr)) !important;
      width: min(37rem, calc(100% - 13rem)) !important;
      max-width: min(37rem, 100%) !important;
      min-width: min(100%, 24rem) !important;
      margin-right: 0.45rem !important;
    }

    body .secondary-mode-switch .camo-mode-button {
      display: none !important;
    }

    body .secondary-mode-switch .mode-button,
    body .content-tab {
      min-height: 2.12rem !important;
      padding: 0.36rem 0.78rem !important;
      font-size: 0.9rem !important;
    }

    body .content-tabs {
      display: inline-flex !important;
      width: fit-content !important;
      margin-left: 0 !important;
      transform: translateY(0) !important;
    }

    body .controls {
      grid-template-columns: minmax(18rem, 1fr) minmax(12rem, 18rem) !important;
      max-width: min(82rem, 100%) !important;
      gap: 0.55rem !important;
      margin: 0.18rem 0 0.55rem !important;
      padding: 0.38rem !important;
      border: 1px solid rgba(255, 255, 255, 0.095) !important;
      border-radius: 0.5rem !important;
      background: rgba(7, 10, 14, 0.58) !important;
      box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.035) !important;
    }

    body .search-box,
    body .select-box {
      gap: 0.25rem !important;
    }

    body .search-box span,
    body .select-box span {
      padding-left: 0.12rem !important;
      color: #8e98a8 !important;
      font-size: 0.66rem !important;
    }

    body .search-box input,
    body .select-box select {
      min-height: 2.75rem !important;
      border-radius: 0.34rem !important;
      background:
        linear-gradient(180deg, rgba(255, 255, 255, 0.035), transparent 70%),
        rgba(12, 17, 24, 0.9) !important;
    }

    body .toolbar {
      max-width: min(82rem, 100%) !important;
      margin-top: 0 !important;
      margin-bottom: 0.55rem !important;
    }

    body .tab-panel.active {
      margin-top: 0.25rem !important;
    }

    body .site-header {
      min-height: 4.1rem !important;
      padding: 0.62rem max(clamp(1rem, 3vw, 2.25rem), calc((100vw - 82rem) / 2)) !important;
      border-bottom: 1px solid rgba(255, 255, 255, 0.08) !important;
      background:
        radial-gradient(circle at 12% 0%, rgba(185, 255, 61, 0.12), transparent 16rem),
        linear-gradient(180deg, rgba(9, 13, 17, 0.96), rgba(5, 8, 11, 0.92)) !important;
      box-shadow: 0 0.85rem 2rem rgba(0, 0, 0, 0.24), inset 0 -1px 0 rgba(185, 255, 61, 0.05) !important;
      backdrop-filter: blur(18px) saturate(1.12) !important;
    }

    body .brand {
      gap: 0.68rem !important;
      min-width: max-content !important;
      color: rgba(246, 250, 244, 0.94) !important;
      font-size: 1.05rem !important;
      letter-spacing: 0 !important;
    }

    body .brand-mark {
      width: 2.25rem !important;
      height: 2.25rem !important;
      border: 1px solid rgba(185, 255, 61, 0.36) !important;
      border-radius: 0.5rem !important;
      background: linear-gradient(135deg, #b9ff3d, #28e070) !important;
      color: #061008 !important;
      box-shadow: 0 0 0 0.25rem rgba(185, 255, 61, 0.07), 0 0 1.3rem rgba(65, 239, 113, 0.22) !important;
      font-family: Rajdhani, Inter, sans-serif !important;
      font-size: 0.98rem !important;
      font-weight: 950 !important;
    }

    body .top-nav {
      align-items: center !important;
      gap: 0.28rem !important;
      padding: 0.18rem !important;
      border: 1px solid rgba(255, 255, 255, 0.08) !important;
      border-radius: 999px !important;
      background: rgba(7, 10, 14, 0.58) !important;
      box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.04) !important;
    }

    body .top-nav a {
      min-height: 2rem !important;
      display: inline-flex !important;
      align-items: center !important;
      justify-content: center !important;
      padding: 0.42rem 0.78rem !important;
      border-radius: 999px !important;
      color: rgba(218, 226, 236, 0.72) !important;
      font-size: 0.82rem !important;
      font-weight: 800 !important;
      line-height: 1 !important;
      transition: color 160ms ease, background 160ms ease, border-color 160ms ease !important;
    }

    body .top-nav a:hover,
    body .top-nav a:focus-visible {
      border-color: rgba(185, 255, 61, 0.22) !important;
      background: rgba(185, 255, 61, 0.08) !important;
      color: #f6ffe2 !important;
    }

    @media (max-width: 820px) {
      body .tier-first {
        max-width: calc(100vw - 1rem) !important;
      }

      body .tier-first .section-heading {
        grid-template-columns: 1fr !important;
        grid-template-areas:
          "kicker"
          "title"
          "date" !important;
        padding: 0.95rem !important;
      }

      body .tier-first .updated-note {
        justify-self: start !important;
        white-space: normal !important;
      }

      body .primary-mode-switch,
      body .secondary-mode-switch,
      body .controls {
        grid-template-columns: 1fr !important;
        width: 100% !important;
      }

      body .content-tabs {
        width: 100% !important;
      }

      body .tier-first h1 {
        font-size: clamp(1.95rem, 12vw, 2.55rem) !important;
      }
    }

    @media (max-width: 720px) {
      body .site-header {
        min-height: auto !important;
        align-items: stretch !important;
        flex-direction: column !important;
        gap: 0.7rem !important;
        padding-inline: 0.75rem !important;
      }

      body .top-nav {
        width: 100% !important;
        display: grid !important;
        grid-template-columns: repeat(3, minmax(0, 1fr)) !important;
        border-radius: 0.55rem !important;
      }
    }
  `;

  function installStyle() {
    let style = document.getElementById(STYLE_ID);
    if (!style) {
      style = document.createElement("style");
      style.id = STYLE_ID;
      document.head.append(style);
    }
    style.textContent = css;
  }

  function reorderPrimaryTabs() {
    const switcher = document.querySelector(".primary-mode-switch");
    if (!switcher) return false;
    const buttons = new Map(Array.from(switcher.querySelectorAll(".mode-button[data-mode]")).map((button) => [button.dataset.mode, button]));
    tabOrder.forEach((mode, index) => {
      const button = buttons.get(mode);
      if (!button) return;
      const current = Array.from(switcher.children).filter((child) => child.classList?.contains("mode-button"))[index];
      if (current !== button) switcher.insertBefore(button, current || null);
    });
    return true;
  }

  function clearStuckLoaders() {
    const blockingText = ["loadoutlab wird geladen", "loadout lab wird geladen", "falls nichts passiert", "wird geladen"];
    document.querySelectorAll("body *").forEach((element) => {
      const text = (element.textContent || "").replace(/\s+/g, " ").trim().toLowerCase();
      if (!text || !blockingText.some((needle) => text.includes(needle))) return;

      const style = window.getComputedStyle(element);
      const rect = element.getBoundingClientRect();
      const coversScreen = rect.width > window.innerWidth * 0.45 && rect.height > window.innerHeight * 0.2;
      const canBlock = ["fixed", "absolute", "sticky"].includes(style.position) || Number(style.zIndex) > 10 || coversScreen;
      if (!canBlock) return;

      element.hidden = true;
      element.style.display = "none";
      element.style.pointerEvents = "none";
    });

    document.documentElement.classList.add("site-interaction-ready");
    document.body?.classList.add("site-interaction-ready");
  }

  function escapeHtml(value) {
    return String(value ?? "")
      .replace(/&/g, "&amp;")
      .replace(/</g, "&lt;")
      .replace(/>/g, "&gt;")
      .replace(/"/g, "&quot;");
  }

  function categoryLabel(category) {
    const key = String(category || "").toLowerCase();
    if (key === "warzone") return "Warzone";
    if (key === "multiplayer") return "Multiplayer";
    if (key === "zombies") return "Zombies";
    if (key === "endgame") return "Endgame";
    return "Update";
  }

  function sourceLabel(update) {
    const source = String(update.source || "Call of Duty");
    if (/treyarch/i.test(source)) return "Treyarch auf X";
    if (/\bx\b|twitter/i.test(source)) return `${source.replace(/twitter/ig, "X")} auf X`;
    return source;
  }

  function updateCardHtml(update, index) {
    const status = index < 2 ? "Neu" : "Update";
    const title = escapeHtml(update.title);
    const category = categoryLabel(update.category);
    return `
      <a class="update-card official" href="${escapeHtml(update.url)}" target="_blank" rel="noreferrer" data-update-category="${escapeHtml(update.category || "update")}">
        <img src="${escapeHtml(update.imageUrl || "assets/cod-loadout-hero.png")}" alt="${title}" loading="lazy">
        <div class="update-card-content">
          <div class="update-meta-row">
            <span class="status-pill">${escapeHtml(status)}</span>
            <span class="update-category-pill">${escapeHtml(category)}</span>
            <time datetime="${escapeHtml(update.date)}">${escapeHtml(update.dateLabel || update.date)}</time>
          </div>
          <h3>${title}</h3>
          <div class="update-detail">
            <p>${escapeHtml(update.summary)}</p>
            <strong>Mehr lesen</strong>
          </div>
        </div>
      </a>`;
  }

  function renderLatestUpdates(data) {
    if (!data || !Array.isArray(data.updates)) return;
    const stamp = data.generatedAtLabel || "";
    const highlights = Array.isArray(data.highlights) ? data.highlights : [];
    const signature = data.generatedAt || stamp || String(data.updates.length);
    if (document.documentElement.dataset.latestUpdatesRendered === signature) return;

    document.querySelectorAll(".updated-note").forEach((note) => {
      note.textContent = stamp ? `Aktualisiert: ${stamp}` : note.textContent;
    });

    document.querySelectorAll(".updates-hero span").forEach((span) => {
      if (/stand:/i.test(span.textContent || "")) span.textContent = stamp ? `Stand: ${stamp}` : span.textContent;
    });

    const hero = document.querySelector(".updates-hero");
    if (hero && !hero.querySelector(".update-sync-strip")) {
      const strip = document.createElement("div");
      strip.className = "update-sync-strip";
      hero.append(strip);
    }
    const strip = hero?.querySelector(".update-sync-strip");
    if (strip) {
      strip.innerHTML = `
        <span><strong>${escapeHtml(data.updates.length)}</strong> Updates</span>
        <span><strong>${stamp ? escapeHtml(stamp) : "Heute"}</strong></span>`;
    }

    const priority = document.querySelector(".update-priority-grid");
    if (priority && highlights.length) {
      priority.innerHTML = highlights.slice(0, 3).map((item) => `
        <article>
          <span>${escapeHtml(String(item.label || "").replace(/Geprueft|Geprüft|Quellen/gi, "Live"))}</span>
          <strong>${escapeHtml(item.title)}</strong>
          <p>${escapeHtml(String(item.text || "").replace(/Offizielle Quellen wurden am (.*?) geprueft\\.?/i, "Aktualisiert am $1.").replace(/Offizielle Quellen wurden am (.*?) geprüft\\.?/i, "Aktualisiert am $1."))}</p>
        </article>`).join("");
    }

    const board = document.querySelector(".update-board");
    if (board) {
      board.innerHTML = data.updates.map(updateCardHtml).join("");
    }

    const timeline = document.querySelector(".timeline");
    if (timeline) {
      timeline.innerHTML = data.updates.slice(0, 4).map((update) => `
        <article>
          <time datetime="${escapeHtml(update.date)}">${escapeHtml(update.dateLabel || update.date)}</time>
          <h3>${escapeHtml(update.title)}</h3>
          <p>${escapeHtml(update.summary)}</p>
        </article>`).join("");
    }
    document.documentElement.dataset.latestUpdatesRendered = signature;
  }

  function loadLatestUpdates() {
    if (latestUpdates) {
      renderLatestUpdates(latestUpdates);
      return latestUpdates;
    }
    if (latestUpdatesPromise) return latestUpdatesPromise;

    latestUpdatesPromise = fetch(`data/latest-updates.json?v=${Date.now()}`, { cache: "no-store" })
      .then((response) => {
        if (!response.ok) throw new Error(`Updates HTTP ${response.status}`);
        return response.json();
      })
      .then((data) => {
        latestUpdates = data;
        renderLatestUpdates(data);
        return data;
      })
      .catch((error) => {
        latestUpdatesPromise = null;
        console.warn("Loadout Lab updates konnten nicht aktualisiert werden", error);
        return null;
      });
    return latestUpdatesPromise;
  }

  function refreshUi() {
    installStyle();
    reorderPrimaryTabs();
    clearStuckLoaders();
    if (latestUpdates) renderLatestUpdates(latestUpdates);
    else loadLatestUpdates();
  }

  function bindEvents() {
    if (window.__loadoutLabSiteUiReady) return;
    window.__loadoutLabSiteUiReady = true;
    document.addEventListener("click", () => window.setTimeout(refreshUi, 30), true);
  }

  function init() {
    bindEvents();
    refreshUi();
    window.setTimeout(refreshUi, 300);
    window.setTimeout(loadLatestUpdates, 900);
    window.setTimeout(clearStuckLoaders, 500);
    window.setTimeout(clearStuckLoaders, 1500);
    window.setTimeout(clearStuckLoaders, 3500);
  }

  if (document.readyState === "loading") document.addEventListener("DOMContentLoaded", init, { once: true });
  else init();
}());

/* source: loadout-lab-upgrades.js */
(function () {
  const STYLE_ID = "loadout-lab-upgrades-style";
  const DATA_VERSION = () => window.__loadoutLabMetaVersion || String(Date.now());
  const state = {
    lang: localStorage.getItem("loadoutLabLang") || "de",
    smartFilter: "all",
    meta: null,
  };

  const copy = {
    de: {
      navTier: "Tierlist",
      navMw4: "MW4",
      navUpdates: "Updates",
      eyebrow: "Meta Tierlist",
      title: "Warzone Ranked Meta Tierlist",
      description: "Suche nach BO7-Waffen, Klasse oder Rolle. Die Liste gruppiert Builds nach Meta-Stufe und zeigt Score, Rolle und Aufsaetze.",
      updated: "Aktualisiert",
      search: "Suche",
      placeholder: "z. B. AK-27, Kogot-7, Ranked",
      sort: "Sortieren",
      score: "Meta Score",
      pick: "Pick-Rate",
      name: "Name",
      range: "Reichweite",
      weaponsTab: "Waffen",
      maps: "Maps",
      warzone: "Warzone",
      bo7: "BO7",
      updates: "Updates",
      mw4: "MW4",
      season: "Season 6",
      updateKicker: "News",
      mw4Kicker: "Geruechte",
      seasonKicker: "Neu",
      statusActive: "Auto-Update aktiv",
      lastCheck: "Letzter Check",
      source: "Quelle",
      weapons: "Waffen",
      sourceUpdated: "WZStats Stand",
      shown: "Meta-Waffen angezeigt",
      of: "von",
      role: "Rolle",
      attachments: "Aufsaetze",
      classCode: "Klassen-Code",
      close: "Schliessen",
      details: "Details",
      legendary: "Legendaer",
      emptyTitle: "Keine Meta-Waffe gefunden",
      emptyText: "Versuch einen anderen Suchbegriff oder wechsel den Filter.",
      filters: {
        all: "Alle",
        meta: "Absolute Meta",
        a: "A-Tier",
        long: "Long Range",
        close: "Close Range",
      },
    },
    en: {
      navTier: "Tier List",
      navMw4: "MW4",
      navUpdates: "Updates",
      eyebrow: "Meta Tier List",
      title: "Warzone Ranked Meta Tier List",
      description: "Search BO7 weapons, classes, or roles. The list groups builds by meta tier and shows score, role, and attachments.",
      updated: "Updated",
      search: "Search",
      placeholder: "e.g. AK-27, Kogot-7, Ranked",
      sort: "Sort",
      score: "Meta Score",
      pick: "Pick Rate",
      name: "Name",
      range: "Range",
      weaponsTab: "Weapons",
      maps: "Maps",
      warzone: "Warzone",
      bo7: "BO7",
      updates: "Updates",
      mw4: "MW4",
      season: "Season 6",
      updateKicker: "News",
      mw4Kicker: "Rumors",
      seasonKicker: "New",
      statusActive: "Auto update active",
      lastCheck: "Last check",
      source: "Source",
      weapons: "Weapons",
      sourceUpdated: "WZStats updated",
      shown: "meta weapons shown",
      of: "of",
      role: "Role",
      attachments: "Attachments",
      classCode: "Class Code",
      close: "Close",
      details: "Details",
      legendary: "Legendary",
      emptyTitle: "No meta weapon found",
      emptyText: "Try another search term or switch filters.",
      filters: {
        all: "All",
        meta: "Absolute Meta",
        a: "A-Tier",
        long: "Long Range",
        close: "Close Range",
      },
    },
  };

  const filterConfig = [
    { key: "all", native: "all", accent: "neutral" },
    { key: "meta", native: "all", accent: "gold" },
    { key: "a", native: "all", accent: "violet" },
    { key: "long", native: "long", accent: "neutral" },
    { key: "close", native: "close", accent: "neutral" },
  ];

  const css = `
    body .site-actions {
      display: inline-flex !important;
      align-items: center !important;
      gap: 0.45rem !important;
      margin-left: auto !important;
    }

    body .language-switch {
      display: inline-grid !important;
      grid-template-columns: repeat(2, minmax(2.3rem, 1fr)) !important;
      gap: 0.18rem !important;
      padding: 0.18rem !important;
      border: 1px solid rgba(255, 255, 255, 0.1) !important;
      border-radius: 999px !important;
      background: rgba(7, 10, 14, 0.64) !important;
    }

    body .language-switch button {
      min-height: 2rem !important;
      border: 0 !important;
      border-radius: 999px !important;
      padding: 0.25rem 0.62rem !important;
      background: transparent !important;
      color: rgba(220, 229, 239, 0.68) !important;
      font: 900 0.74rem Inter, system-ui, sans-serif !important;
      cursor: pointer !important;
    }

    body .language-switch button.active {
      background: linear-gradient(135deg, #b9ff3d, #25e66f) !important;
      color: #061008 !important;
      box-shadow: 0 0.55rem 1.2rem rgba(37, 230, 111, 0.22) !important;
    }

    body .auto-update-status {
      display: grid !important;
      grid-template-columns: auto 1fr auto !important;
      align-items: center !important;
      gap: 0.75rem !important;
      max-width: min(82rem, 100%) !important;
      margin: 0 0 0.65rem !important;
      padding: 0.62rem 0.8rem !important;
      border: 1px solid rgba(185, 255, 61, 0.22) !important;
      border-radius: 0.48rem !important;
      background: linear-gradient(135deg, rgba(185, 255, 61, 0.105), rgba(11, 17, 22, 0.88) 42%, rgba(8, 12, 17, 0.96)) !important;
      box-shadow: 0 0.8rem 1.65rem rgba(0, 0, 0, 0.22), inset 0 1px 0 rgba(255, 255, 255, 0.045) !important;
    }

    body .auto-update-status .status-main {
      display: inline-flex !important;
      align-items: center !important;
      gap: 0.45rem !important;
      color: #b9ff3d !important;
      font-weight: 950 !important;
      white-space: nowrap !important;
    }

    body .auto-update-status .status-dot {
      width: 0.55rem !important;
      height: 0.55rem !important;
      border-radius: 999px !important;
      background: #25e66f !important;
      box-shadow: 0 0 0 0.22rem rgba(37, 230, 111, 0.14), 0 0 1rem rgba(37, 230, 111, 0.38) !important;
    }

    body .auto-update-status .status-meta {
      display: flex !important;
      flex-wrap: wrap !important;
      gap: 0.35rem 0.75rem !important;
      min-width: 0 !important;
      color: rgba(222, 232, 242, 0.76) !important;
      font-size: 0.78rem !important;
      font-weight: 800 !important;
    }

    body .auto-update-status .status-meta strong,
    body .auto-update-status .status-count {
      color: #f8fbff !important;
      font-weight: 950 !important;
    }

    body .toolbar.enhanced-filter-toolbar {
      display: flex !important;
      flex-wrap: wrap !important;
      gap: 0.45rem !important;
      padding: 0.44rem !important;
      border: 1px solid rgba(255, 255, 255, 0.095) !important;
      border-radius: 0.5rem !important;
      background: rgba(7, 10, 14, 0.58) !important;
      box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.035) !important;
    }

    body .toolbar.enhanced-filter-toolbar .filter-button {
      min-height: 2.25rem !important;
      border: 1px solid rgba(255, 255, 255, 0.105) !important;
      border-radius: 999px !important;
      padding: 0.42rem 0.82rem !important;
      background: rgba(13, 19, 27, 0.78) !important;
      color: rgba(238, 244, 250, 0.9) !important;
      font-weight: 900 !important;
      cursor: pointer !important;
    }

    body .toolbar.enhanced-filter-toolbar .filter-button[data-accent="gold"] { border-color: rgba(255, 211, 90, 0.32) !important; color: #ffd35a !important; }
    body .toolbar.enhanced-filter-toolbar .filter-button[data-accent="violet"] { border-color: rgba(176, 140, 255, 0.35) !important; color: #c4a8ff !important; }
    body .toolbar.enhanced-filter-toolbar .filter-button[data-accent="blue"] { border-color: rgba(53, 215, 255, 0.35) !important; color: #6de4ff !important; }
    body .toolbar.enhanced-filter-toolbar .filter-button[data-accent="green"] { border-color: rgba(41, 230, 129, 0.35) !important; color: #7dffad !important; }
    body .toolbar.enhanced-filter-toolbar .filter-button[data-accent="muted"] { color: rgba(209, 218, 228, 0.72) !important; }

    body .toolbar.enhanced-filter-toolbar .filter-button.active {
      border-color: rgba(185, 255, 61, 0.94) !important;
      background: linear-gradient(135deg, #b9ff3d 0%, #66f244 45%, #24e66f 100%) !important;
      color: #051007 !important;
      box-shadow: 0 0.5rem 1.2rem rgba(42, 239, 111, 0.22) !important;
    }

    @media (max-width: 720px) {
      body .site-actions {
        width: 100% !important;
        justify-content: flex-end !important;
      }

      body .auto-update-status {
        grid-template-columns: 1fr !important;
        gap: 0.45rem !important;
      }

      body .auto-update-status .status-count {
        justify-self: start !important;
      }
    }
  `;

  function t() {
    return copy[state.lang] || copy.de;
  }

  function html(value) {
    return String(value ?? "")
      .replace(/&/g, "&amp;")
      .replace(/</g, "&lt;")
      .replace(/>/g, "&gt;")
      .replace(/"/g, "&quot;");
  }

  function installStyle() {
    let style = document.getElementById(STYLE_ID);
    if (!style) {
      style = document.createElement("style");
      style.id = STYLE_ID;
      document.head.append(style);
    }
    style.textContent = css;
  }

  function versionedUrl(path) {
    const url = new URL(path, window.location.href);
    url.searchParams.set("fresh", DATA_VERSION());
    url.searchParams.set("t", String(Date.now()));
    return url.toString();
  }

  async function loadMeta() {
    if (state.meta) return state.meta;
    const response = await fetch(versionedUrl("data/wzstats-meta.json"), { cache: "no-store" });
    if (!response.ok) throw new Error(`Meta HTTP ${response.status}`);
    state.meta = await response.json();
    return state.meta;
  }

  function currentList() {
    const meta = state.meta;
    if (!meta) return null;
    const active = document.querySelector(".secondary-mode-switch .mode-button.active")?.dataset.mode;
    return active === "bo7-ranked" ? meta.bo7Ranked : meta.warzoneRanked;
  }

  function installLanguageSwitch() {
    const header = document.querySelector(".site-header");
    if (!header) return;
    let actions = header.querySelector(".site-actions");
    if (!actions) {
      actions = document.createElement("div");
      actions.className = "site-actions";
      header.appendChild(actions);
    }
    if (!actions.querySelector(".language-switch")) {
      actions.insertAdjacentHTML("beforeend", `
        <div class="language-switch" aria-label="Sprache auswaehlen">
          <button type="button" data-language="de">DE</button>
          <button type="button" data-language="en">EN</button>
        </div>
      `);
    }
    actions.querySelectorAll("[data-language]").forEach((button) => {
      button.classList.toggle("active", button.dataset.language === state.lang);
      button.setAttribute("aria-pressed", button.dataset.language === state.lang ? "true" : "false");
    });
  }

  function renderStatus() {
    const section = document.querySelector("#loadouts .section-heading");
    const list = currentList();
    if (!section || !state.meta || !list) return;
    let status = document.getElementById("autoUpdateStatus");
    if (!status) {
      status = document.createElement("div");
      status.id = "autoUpdateStatus";
      status.className = "auto-update-status";
      section.insertAdjacentElement("afterend", status);
    }

    const labels = t();
    const generated = state.meta.generatedAtLabel || "-";
    const sourceUpdated = list.sourceUpdatedAt ? new Date(list.sourceUpdatedAt).toLocaleString(state.lang === "en" ? "en-US" : "de-DE", {
      day: "numeric",
      month: "short",
      year: "numeric",
      hour: "2-digit",
      minute: "2-digit",
      timeZone: "Europe/Berlin",
    }) : "-";

    status.innerHTML = `
      <span class="status-main"><i class="status-dot" aria-hidden="true"></i>${html(labels.statusActive)}</span>
      <span class="status-meta">
        <span>${html(labels.lastCheck)}: <strong>${html(generated)}</strong></span>
        <span>${html(labels.source)}: <strong>WZStats</strong></span>
        <span>${html(labels.sourceUpdated)}: <strong>${html(sourceUpdated)}</strong></span>
      </span>
      <strong class="status-count">${list.items.length} ${html(labels.weapons)}</strong>
    `;

    const note = document.querySelector(".updated-note");
    if (note) note.textContent = `${labels.updated}: ${generated}`;
  }

  function renderToolbar() {
    const toolbar = document.getElementById("filterToolbar");
    if (!toolbar) return;
    const labels = t().filters;
    toolbar.classList.add("enhanced-filter-toolbar");
    toolbar.innerHTML = filterConfig.map((filter) => `
      <button class="filter-button${state.smartFilter === filter.key ? " active" : ""}" type="button" data-filter="${filter.native}" data-smart-filter="${filter.key}" data-accent="${filter.accent}">
        ${html(labels[filter.key])}
      </button>
    `).join("");
  }

  function applyLanguage() {
    const labels = t();
    document.documentElement.lang = state.lang;
    document.querySelector('.top-nav a[href="#loadouts"]') && (document.querySelector('.top-nav a[href="#loadouts"]').textContent = labels.navTier);
    document.querySelector('.top-nav a[href="#intel"]') && (document.querySelector('.top-nav a[href="#intel"]').textContent = labels.navMw4);
    document.querySelector('.top-nav a[href="#updates"]') && (document.querySelector('.top-nav a[href="#updates"]').textContent = labels.navUpdates);
    document.querySelector(".section-heading .eyebrow") && (document.querySelector(".section-heading .eyebrow").textContent = labels.eyebrow);
    document.getElementById("tierTitle") && (document.getElementById("tierTitle").textContent = labels.title);
    document.getElementById("tierDescription") && (document.getElementById("tierDescription").textContent = labels.description);
    document.querySelector('.search-box span') && (document.querySelector('.search-box span').textContent = labels.search);
    document.getElementById("loadoutSearch") && (document.getElementById("loadoutSearch").placeholder = labels.placeholder);
    document.querySelector('.select-box span') && (document.querySelector('.select-box span').textContent = labels.sort);

    const options = document.querySelectorAll("#sortSelect option");
    if (options[0]) options[0].textContent = labels.score;
    if (options[1]) options[1].textContent = labels.pick;
    if (options[2]) options[2].textContent = labels.name;
    if (options[3]) options[3].textContent = labels.range;

    const primary = {
      updates: [labels.updates, labels.updateKicker],
      "mw4-info": [labels.mw4, labels.mw4Kicker],
      "season4-info": [labels.season, labels.seasonKicker],
    };
    Object.entries(primary).forEach(([mode, values]) => {
      const button = document.querySelector(`.primary-mode-switch .mode-button[data-mode="${mode}"]`);
      if (!button) return;
      button.textContent = values[0];
      button.dataset.kicker = values[1];
    });

    const wz = document.querySelector('.secondary-mode-switch .mode-button[data-mode="warzone-ranked"]');
    const bo7 = document.querySelector('.secondary-mode-switch .mode-button[data-mode="bo7-ranked"]');
    if (wz) wz.textContent = labels.warzone;
    if (bo7) bo7.textContent = labels.bo7;
    const weaponsTab = document.querySelector('.content-tab[data-tab="weapons"]');
    const mapsTab = document.querySelector('.content-tab[data-tab="maps"]');
    if (weaponsTab) weaponsTab.textContent = labels.weaponsTab;
    if (mapsTab) mapsTab.textContent = labels.maps;

    installLanguageSwitch();
    renderToolbar();
    renderStatus();
    translateRenderedCards();
    applySmartFilter();
  }

  function itemMap() {
    const list = currentList();
    if (!list) return new Map();
    return new Map(list.items.map((item) => [item.name, item]));
  }

  function cardHasBuild(card) {
    const text = (card.textContent || "").toLowerCase();
    if (text.includes("keine gepr") || text.includes("no verified")) return false;
    return card.querySelectorAll(".loadout-slot").length > 1;
  }

  function isVisibleForSmartFilter(item, card) {
    if (!item) return true;
    if (state.smartFilter === "top10") return Number(item.position) <= 10;
    if (state.smartFilter === "meta") return item.tier === "META";
    if (state.smartFilter === "a") return item.tier === "A";
    if (state.smartFilter === "b") return item.tier === "B";
    if (state.smartFilter === "hasBuild") return cardHasBuild(card);
    if (state.smartFilter === "noBuild") return !cardHasBuild(card);
    return true;
  }

  function applySmartFilter() {
    const grid = document.getElementById("loadoutGrid");
    const list = currentList();
    if (!grid || !list) return;
    const byName = itemMap();
    let visible = 0;

    grid.querySelectorAll(".loadout-card").forEach((card) => {
      const item = byName.get(card.dataset.loadoutCard || "");
      const show = isVisibleForSmartFilter(item, card);
      card.hidden = !show;
      card.style.display = show ? "" : "none";
      if (show) visible += 1;
    });

    grid.querySelectorAll(".meta-tier-heading").forEach((heading) => {
      let hasVisibleCard = false;
      let node = heading.nextElementSibling;
      while (node && !node.classList.contains("meta-tier-heading")) {
        if (node.classList.contains("loadout-card") && !node.hidden && node.style.display !== "none") {
          hasVisibleCard = true;
          break;
        }
        node = node.nextElementSibling;
      }
      heading.hidden = !hasVisibleCard;
      heading.style.display = hasVisibleCard ? "" : "none";
    });

    const count = document.getElementById("resultCount");
    if (count) count.textContent = `${visible} ${t().of} ${list.items.length} ${t().shown}`;

    const empty = grid.querySelector(".empty-state");
    if (empty) {
      const h3 = empty.querySelector("h3");
      const p = empty.querySelector("p");
      if (h3) h3.textContent = t().emptyTitle;
      if (p) p.textContent = t().emptyText;
    }
  }

  function translateRenderedCards() {
    const labels = t();
    document.querySelectorAll(".stat-row em").forEach((node) => { node.textContent = labels.role; });
    document.querySelectorAll(".detail-panel-title span").forEach((node) => {
      const text = (node.textContent || "").toLowerCase();
      if (text.includes("aufs") || text.includes("attach")) node.textContent = labels.attachments;
      if (text.includes("extra")) node.textContent = "Extras";
    });
    document.querySelectorAll(".build-code-box span").forEach((node) => { node.textContent = labels.classCode; });
    document.querySelectorAll(".expand-button span").forEach((node) => {
      const expanded = node.closest(".expand-button")?.getAttribute("aria-expanded") === "true";
      node.textContent = expanded ? labels.close : labels.details;
    });
    document.querySelectorAll(".rank-badge span").forEach((node) => {
      if (/legend/i.test(node.textContent || "")) node.textContent = labels.legendary;
    });
    document.querySelectorAll(".meta-tier-heading").forEach((heading) => {
      const small = heading.querySelector("small");
      if (small) {
        const count = (small.textContent || "").match(/\d+/)?.[0] || "";
        small.textContent = `${count} ${labels.weapons}`.trim();
      }
      if (state.lang === "en") {
        const span = heading.querySelector("span");
        if (span) span.textContent = span.textContent.replace("Waffen", "Weapons");
      }
    });
  }

  function bindEvents() {
    if (window.__loadoutLabUpgradesReady) return;
    window.__loadoutLabUpgradesReady = true;

    document.addEventListener("click", (event) => {
      const language = event.target.closest("[data-language]");
      if (language) {
        state.lang = language.dataset.language === "en" ? "en" : "de";
        localStorage.setItem("loadoutLabLang", state.lang);
        applyLanguage();
        return;
      }

      const filterButton = event.target.closest("#filterToolbar .filter-button[data-smart-filter]");
      if (!filterButton) return;
      state.smartFilter = filterButton.dataset.smartFilter || "all";
      document.querySelectorAll("#filterToolbar .filter-button").forEach((button) => button.classList.toggle("active", button === filterButton));
      window.setTimeout(() => { translateRenderedCards(); applySmartFilter(); }, 60);
      window.setTimeout(() => { translateRenderedCards(); applySmartFilter(); }, 220);
    }, true);

    document.addEventListener("click", (event) => {
      if (event.target.closest(".secondary-mode-switch .mode-button, .content-tab[data-tab='weapons']")) {
        window.setTimeout(() => { renderStatus(); renderToolbar(); translateRenderedCards(); applySmartFilter(); }, 120);
      }
    });

    const observer = new MutationObserver(() => {
      window.clearTimeout(window.__loadoutLabUpgradeRenderTimer);
      window.__loadoutLabUpgradeRenderTimer = window.setTimeout(() => {
        translateRenderedCards();
        applySmartFilter();
      }, 50);
    });
    const watchGrid = () => {
      const grid = document.getElementById("loadoutGrid");
      if (grid && !grid.dataset.upgradeObserved) {
        grid.dataset.upgradeObserved = "true";
        observer.observe(grid, { childList: true, subtree: true });
      }
    };
    watchGrid();
    window.setTimeout(watchGrid, 400);
  }

  async function init() {
    installStyle();
    installLanguageSwitch();
    bindEvents();
    renderToolbar();
    try { await loadMeta(); } catch (error) { console.warn("Loadout Lab meta status konnte nicht geladen werden", error); }
    applyLanguage();
    window.setTimeout(applyLanguage, 350);
    window.setTimeout(applyLanguage, 1200);
  }

  if (document.readyState === "loading") document.addEventListener("DOMContentLoaded", init, { once: true });
  else init();
}());

/* source: loadout-details-upgrades.js */
(function () {
  const STYLE_ID = "loadout-details-upgrades-style";
  const state = { meta: null, timer: 0 };

  const copy = {
    de: {
      source: "Quelle",
      sourceUpdated: "Stand",
      score: "Score",
      rank: "Rang",
      tier: "Tier",
      copy: "Kopieren",
      copied: "Kopiert",
      unavailable: "Kein Klassen-Code hinterlegt",
      noAttachments: "Keine geprueften Aufsaetze hinterlegt",
    },
    en: {
      source: "Source",
      sourceUpdated: "Updated",
      score: "Score",
      rank: "Rank",
      tier: "Tier",
      copy: "Copy",
      copied: "Copied",
      unavailable: "No class code available",
      noAttachments: "No verified attachments available",
    },
  };

  const css = `
    body #loadoutGrid .detail-source-row {
      display: flex !important;
      flex-wrap: wrap !important;
      gap: 0.42rem !important;
      margin: 0 0 0.72rem !important;
      padding: 0.64rem 0.72rem !important;
      border: 1px solid rgba(var(--tier-card-rgb, 185, 255, 61), 0.18) !important;
      border-radius: 0.42rem !important;
      background: rgba(5, 8, 12, 0.36) !important;
    }

    body #loadoutGrid .detail-source-chip {
      display: inline-flex !important;
      align-items: center !important;
      gap: 0.28rem !important;
      min-height: 1.65rem !important;
      padding: 0.2rem 0.5rem !important;
      border: 1px solid rgba(255, 255, 255, 0.075) !important;
      border-radius: 999px !important;
      background: rgba(255, 255, 255, 0.038) !important;
      color: rgba(234, 241, 248, 0.84) !important;
      font-size: 0.72rem !important;
      font-weight: 850 !important;
      line-height: 1 !important;
      white-space: nowrap !important;
    }

    body #loadoutGrid .detail-source-chip span {
      color: rgba(255, 255, 255, 0.54) !important;
      font-size: 0.66rem !important;
      font-weight: 950 !important;
      text-transform: uppercase !important;
    }

    body #loadoutGrid .detail-source-chip strong,
    body #loadoutGrid .detail-source-chip a {
      color: var(--tier-card-color, #b9ff3d) !important;
      font-weight: 950 !important;
      text-decoration: none !important;
    }

    body #loadoutGrid .build-code-box {
      position: relative !important;
      display: grid !important;
      grid-template-columns: minmax(0, 1fr) auto !important;
      gap: 0.48rem 0.65rem !important;
      align-items: center !important;
    }

    body #loadoutGrid .build-code-box span,
    body #loadoutGrid .build-code-box strong {
      grid-column: 1 !important;
    }

    body #loadoutGrid .copy-build-button {
      grid-column: 2 !important;
      grid-row: 1 / span 2 !important;
      align-self: stretch !important;
      min-width: 5.6rem !important;
      border: 1px solid rgba(var(--tier-card-rgb, 185, 255, 61), 0.46) !important;
      border-radius: 0.36rem !important;
      background: linear-gradient(135deg, rgba(var(--tier-card-rgb, 185, 255, 61), 0.22), rgba(var(--tier-card-rgb, 185, 255, 61), 0.08)), rgba(8, 12, 18, 0.94) !important;
      color: #fff !important;
      font: 950 0.78rem Inter, system-ui, sans-serif !important;
      cursor: pointer !important;
    }

    body #loadoutGrid .copy-build-button:hover,
    body #loadoutGrid .copy-build-button.copied {
      background: var(--tier-card-color, #b9ff3d) !important;
      color: #061008 !important;
    }

    body #loadoutGrid .build-code-box.no-code {
      grid-template-columns: 1fr !important;
      opacity: 0.82 !important;
    }

    body #loadoutGrid .build-code-box.no-code strong {
      color: rgba(255, 255, 255, 0.66) !important;
      font-size: 0.9rem !important;
    }

    body #loadoutGrid .loadout-slot.placeholder-slot {
      grid-template-columns: 1fr !important;
      border-style: dashed !important;
      color: rgba(255, 255, 255, 0.68) !important;
    }

    @media (max-width: 720px) {
      body #loadoutGrid .build-code-box {
        grid-template-columns: 1fr !important;
      }

      body #loadoutGrid .copy-build-button {
        grid-column: 1 !important;
        grid-row: auto !important;
        min-height: 2.35rem !important;
      }
    }
  `;

  function lang() {
    return localStorage.getItem("loadoutLabLang") === "en" || document.documentElement.lang === "en" ? "en" : "de";
  }

  function t() {
    return copy[lang()] || copy.de;
  }

  function html(value) {
    return String(value ?? "")
      .replace(/&/g, "&amp;")
      .replace(/</g, "&lt;")
      .replace(/>/g, "&gt;")
      .replace(/"/g, "&quot;");
  }

  function installStyle() {
    let style = document.getElementById(STYLE_ID);
    if (!style) {
      style = document.createElement("style");
      style.id = STYLE_ID;
      document.head.append(style);
    }
    style.textContent = css;
  }

  function versionedUrl(path) {
    const url = new URL(path, window.location.href);
    url.searchParams.set("fresh", window.__loadoutLabMetaVersion || String(Date.now()));
    url.searchParams.set("t", String(Date.now()));
    return url.toString();
  }

  async function loadMeta() {
    if (state.meta) return state.meta;
    const response = await fetch(versionedUrl("data/wzstats-meta.json"), { cache: "no-store" });
    if (!response.ok) throw new Error(`Meta HTTP ${response.status}`);
    state.meta = await response.json();
    return state.meta;
  }

  function currentList() {
    if (!state.meta) return null;
    const active = document.querySelector(".secondary-mode-switch .mode-button.active")?.dataset.mode;
    return active === "bo7-ranked" ? state.meta.bo7Ranked : state.meta.warzoneRanked;
  }

  function itemMap() {
    const list = currentList();
    if (!list?.items) return new Map();
    return new Map(list.items.map((item) => [item.name, item]));
  }

  function formatDate(value) {
    if (!value) return "-";
    const date = new Date(value);
    if (Number.isNaN(date.getTime())) return value;
    return date.toLocaleString(lang() === "en" ? "en-US" : "de-DE", {
      day: "numeric",
      month: "short",
      year: "numeric",
      hour: "2-digit",
      minute: "2-digit",
      timeZone: "Europe/Berlin",
    });
  }

  function sourceRow(item, list) {
    const labels = t();
    const sourceUrl = item.sourceUrl || list?.sourceUrl || state.meta?.sourceUrl || "https://wzstats.gg/";
    const sourceUpdated = item.sourceUpdatedAt || list?.sourceUpdatedAt || state.meta?.generatedAt;
    return `
      <div class="detail-source-row" data-detail-source-row>
        <span class="detail-source-chip"><span>${html(labels.source)}</span><a href="${html(sourceUrl)}" target="_blank" rel="noreferrer">WZStats</a></span>
        <span class="detail-source-chip"><span>${html(labels.sourceUpdated)}</span><strong>${html(formatDate(sourceUpdated))}</strong></span>
        <span class="detail-source-chip"><span>${html(labels.rank)}</span><strong>#${html(item.position)}</strong></span>
        <span class="detail-source-chip"><span>${html(labels.tier)}</span><strong>${html(item.tierLabel || item.tier)}</strong></span>
        <span class="detail-source-chip"><span>${html(labels.score)}</span><strong>${html(item.score || item.scoreLabel || "-")}/100</strong></span>
      </div>
    `;
  }

  async function copyText(text) {
    if (navigator.clipboard?.writeText) {
      await navigator.clipboard.writeText(text);
      return;
    }
    const area = document.createElement("textarea");
    area.value = text;
    area.setAttribute("readonly", "");
    area.style.position = "fixed";
    area.style.left = "-9999px";
    document.body.append(area);
    area.select();
    document.execCommand("copy");
    area.remove();
  }

  function enhanceBuildBox(box) {
    const labels = t();
    const codeNode = box.querySelector("strong");
    const code = (codeNode?.textContent || "").trim();
    const hasCode = code && !/^wzstats loadout$/i.test(code) && !/^wzstats$/i.test(code);

    if (!hasCode) {
      box.classList.add("no-code");
      if (codeNode) codeNode.textContent = labels.unavailable;
      box.querySelector(".copy-build-button")?.remove();
      return;
    }

    box.classList.remove("no-code");
    let button = box.querySelector(".copy-build-button");
    if (!button) {
      button = document.createElement("button");
      button.type = "button";
      button.className = "copy-build-button";
      box.append(button);
    }
    button.textContent = button.classList.contains("copied") ? labels.copied : labels.copy;
    button.dataset.copyValue = code;
  }

  function polishAttachmentPanel(panel) {
    const labels = t();
    const list = panel.querySelector(".premium-attachment-list");
    if (!list) return;
    const slots = [...list.querySelectorAll(".loadout-slot")];
    const onlyPlaceholder = slots.length <= 1 && /keine gepr|no verified/i.test(list.textContent || "");
    if (!onlyPlaceholder) return;
    list.innerHTML = `<li class="loadout-slot placeholder-slot"><strong>${html(labels.noAttachments)}</strong></li>`;
    const count = panel.querySelector(".detail-panel-title strong");
    if (count) count.textContent = "0/5";
  }

  function enhanceCards() {
    const list = currentList();
    if (!list) return;
    const byName = itemMap();
    document.querySelectorAll("#loadoutGrid .loadout-card").forEach((card) => {
      const item = byName.get(card.dataset.loadoutCard || "");
      const details = card.querySelector(".meta-card-details");
      if (!item || !details) return;

      details.querySelector("[data-detail-source-row]")?.remove();
      details.insertAdjacentHTML("afterbegin", sourceRow(item, list));

      card.querySelectorAll(".build-code-box").forEach(enhanceBuildBox);
      card.querySelectorAll(".attachments-panel").forEach(polishAttachmentPanel);
    });
  }

  function scheduleEnhance(delay = 60) {
    window.clearTimeout(state.timer);
    state.timer = window.setTimeout(enhanceCards, delay);
  }

  function bindEvents() {
    if (window.__loadoutDetailsUpgradesReady) return;
    window.__loadoutDetailsUpgradesReady = true;

    document.addEventListener("click", async (event) => {
      const button = event.target.closest(".copy-build-button");
      if (!button) return;
      event.preventDefault();
      try {
        await copyText(button.dataset.copyValue || "");
        button.classList.add("copied");
        button.textContent = t().copied;
        window.setTimeout(() => {
          button.classList.remove("copied");
          button.textContent = t().copy;
        }, 1400);
      } catch (error) {
        console.warn("Klassen-Code konnte nicht kopiert werden", error);
      }
    }, true);

    document.addEventListener("click", (event) => {
      if (event.target.closest(".expand-button, [data-language], #filterToolbar .filter-button, .secondary-mode-switch .mode-button")) {
        scheduleEnhance(120);
        scheduleEnhance(360);
      }
    }, true);

    const observer = new MutationObserver(() => scheduleEnhance(80));
    const watchGrid = () => {
      const grid = document.getElementById("loadoutGrid");
      if (!grid || grid.dataset.detailsUpgradeObserved) return;
      grid.dataset.detailsUpgradeObserved = "true";
      observer.observe(grid, { childList: true, subtree: true });
    };
    watchGrid();
    window.setTimeout(watchGrid, 500);
  }

  async function init() {
    installStyle();
    bindEvents();
    try { await loadMeta(); } catch (error) { console.warn("Loadout details meta konnte nicht geladen werden", error); }
    scheduleEnhance(100);
    window.setTimeout(enhanceCards, 600);
    window.setTimeout(enhanceCards, 1500);
  }

  if (document.readyState === "loading") document.addEventListener("DOMContentLoaded", init, { once: true });
  else init();
}());

/* source: loadout-build-supplements.js */
(function () {
  const REGISTRY_URL = "data/weapon-registry.json";
  const metaExtras = ["Drill Instructor", "Sprinter", "Tempered"];
  const longRangeExtras = ["Drill Instructor", "Sprinter", "Tempered"];
  const closeRangeExtras = ["Drill Instructor", "Sprinter", "Tempered"];
  const sniperExtras = ["Drill Instructor", "Sprinter", "Tempered"];
  const utilityExtras = ["Drill Instructor", "Sprinter", "Tempered"];
  const registryBuilds = new Map();

  const supplementalBuilds = {
    vmp: {
      code: "LL-VMP-META",
      attachments: ["Mündung: Compensator", "Lauf: Long Barrel", "Unterlauf: Lightweight Handstop", "Magazin: Extended Mag II", "Hinterer Griff: Ergonomic Grip"],
      extras: [...closeRangeExtras],
    },
    "tr51-para": {
      code: "LL-TR51-META",
      attachments: ["Mündung: Compensator", "Lauf: Reinforced Barrel", "Unterlauf: Vertical Foregrip", "Magazin: Extended Mag II", "Optik: Reflex Sight"],
      extras: [...longRangeExtras],
    },
    fg42: {
      code: "LL-FG42-A9",
      attachments: ["Mündung: Compensator", "Lauf: Long Barrel", "Unterlauf: Vertical Foregrip", "Magazin: Extended Mag II", "Optik: 2x Scope"],
      extras: [...longRangeExtras],
    },
    "an-94": {
      code: "LL-AN94-A14",
      attachments: ["Mündung: Compensator", "Lauf: Long Barrel", "Unterlauf: Stabilizer Foregrip", "Magazin: Extended Mag II", "Optik: Reflex Sight"],
      extras: [...longRangeExtras],
    },
    gremlin: {
      code: "LL-GREMLIN-A25",
      attachments: ["Mündung: Compensator", "Lauf: Reinforced Barrel", "Unterlauf: Lightweight Handstop", "Magazin: Extended Mag II", "Hinterer Griff: Quickdraw Grip"],
      extras: [...closeRangeExtras],
    },
    mammoth: {
      code: "LL-MAMMOTH-C45",
      attachments: ["Mündung: Compensator", "Lauf: Long Barrel", "Unterlauf: Vertical Foregrip", "Magazin: Fast Mag", "Optik: 2x Scope"],
      extras: [...longRangeExtras],
    },
    "executioner-s-duet": {
      code: "LL-EXECUTIONER-D57",
      attachments: ["Keine Waffenaufsätze verfügbar"],
      extras: [...utilityExtras],
    },
    mace: {
      code: "LL-MACE-D58",
      attachments: ["Keine Waffenaufsätze verfügbar"],
      extras: [...utilityExtras],
    },
    "roc-20mm": {
      code: "LL-ROC20-D59",
      attachments: ["Keine Waffenaufsätze verfügbar"],
      extras: [...utilityExtras],
    },
    "vx-compact": {
      code: "LL-VXCOMPACT-A13",
      attachments: [
        "Mündung: LTI Stentorian Brake",
        "Lauf: Long Barrel",
        "Unterlauf: Stabilizer Foregrip",
        "Magazin: Extended Mag II",
        "Hinterer Griff: Ergonomic Grip",
      ],
      extras: [...longRangeExtras],
    },
    "cbrs-3": {
      code: "LL-CBRS3-A10",
      extras: closeRangeExtras,
      attachments: [
        "Muzzle: Compensator (Stufe 4)",
        "Barrel: Long Barrel (Stufe 12)",
        "Underbarrel: Lightweight Handstop (Stufe 3)",
        "Magazine: Extended Mag II (Stufe 28)",
        "Rear Grip: Quickdraw Grip (Stufe 9)",
      ],
    },
    "krs-762": {
      code: "LL-KRS762-B7",
      extras: sniperExtras,
      attachments: [
        "Optic: Variable Zoom Scope (Stufe 8)",
        "Muzzle: Suppressor (Stufe 9)",
        "Barrel: Marksman Barrel (Stufe 18)",
        "Magazine: Fast Mag (Prestige)",
        "Fire Mods: High Velocity (Stufe 24)",
      ],
    },
    "krs-7-62": {
      code: "LL-KRS762-B7",
      extras: sniperExtras,
      attachments: [
        "Optic: Variable Zoom Scope (Stufe 8)",
        "Muzzle: Suppressor (Stufe 9)",
        "Barrel: Marksman Barrel (Stufe 18)",
        "Magazine: Fast Mag (Prestige)",
        "Fire Mods: High Velocity (Stufe 24)",
      ],
    },
    grimhawk: {
      code: "LL-GRIMHAWK-D11",
      extras: utilityExtras,
      attachments: ["Keine Waffenaufsätze verfügbar"],
    },
  };

  function slug(value) {
    return String(value || "")
      .toLowerCase()
      .normalize("NFD")
      .replace(/[\u0300-\u036f]/g, "")
      .replace(/mk\./g, "mk")
      .replace(/[^a-z0-9]+/g, "-")
      .replace(/^-+|-+$/g, "");
  }

  function extrasFor(item) {
    return [...metaExtras];
  }

  function loadRegistry() {
    window.__loadoutWeaponRegistryPromise ||= fetch(`${REGISTRY_URL}?t=${Date.now()}`, { cache: "no-store" })
      .then((response) => {
        if (!response.ok) throw new Error("weapon registry unavailable");
        return response.json();
      });

    window.__loadoutWeaponRegistryPromise.then((data) => {
      (data.weapons || []).forEach((item) => {
        registryBuilds.set(slug(item.name), {
          code: item.buildCode,
          attachments: item.attachments || [],
          extras: extrasFor(item),
        });
      });
      scheduleRun();
    }).catch(() => {});
  }

  function escapeHtml(value) {
    return String(value)
      .replace(/&/g, "&amp;")
      .replace(/</g, "&lt;")
      .replace(/>/g, "&gt;");
  }

  const extraLabels = new Map([
    ["Scavenger", "Plünderer"],
    ["Ghost", "Geist"],
    ["Drill Instructor", "Ausbilder"],
    ["Tempered", "Gehärtet"],
    ["Quick Fix", "Schnelle Heilung"],
    ["Survivor", "Überlebender"],
    ["Smoke Grenade", "Rauchgranate"],
    ["Fast Hands", "Fingerfertigkeit"],
    ["Mountaineer", "Bergsteiger"],
    ["Stim Shot", "Stimulans"],
    ["Dexterity", "Geschicklichkeit"],
    ["Tac Mask", "Taktikmaske"],
    ["Flak Jacket", "Splitterschutz"],
    ["Lightweight", "Leichtgewicht"],
    ["Sprinter", "Sprinter"],
  ]);

  function displayExtra(value) {
    return extraLabels.get(value) || value;
  }

  function parseAttachment(value) {
    const clean = String(value || "").replace(/\s+/g, " ").trim();
    const levelMatch = clean.match(/\((Stufe [^)]+|Prestige)\)\s*$/i);
    const level = levelMatch ? levelMatch[1] : "";
    const withoutLevel = levelMatch ? clean.slice(0, levelMatch.index).trim() : clean;
    const parts = withoutLevel.split(":");
    const slot = parts.length > 1 ? parts.shift().trim() : "Aufsatz";
    const name = parts.join(":").trim() || withoutLevel;
    return { slot, name, level };
  }

  function legacyItemsHtml(items) {
    return items.map((item) => `<li>${escapeHtml(item)}</li>`).join("");
  }

  function cleanAttachments(items) {
    return (Array.isArray(items) ? items : [])
      .map((item) => String(item || "").trim())
      .filter((item) => item && !/^(Pick-Rate|Quelle|Rolle):/i.test(item));
  }

  function hasPendingAttachments(items) {
    return !items.length || items.some((item) => /Aufsätze werden aktualisiert/i.test(item));
  }

  function premiumAttachmentHtml(items) {
    const cleanItems = cleanAttachments(items);
    if (hasPendingAttachments(cleanItems)) {
      return `<li class="loadout-slot pending-slot"><span class="slot-type">Status</span><strong>Aufsätze werden aktualisiert</strong></li>`;
    }
    return cleanItems.map((item) => {
      const attachment = parseAttachment(item);
      return `<li class="loadout-slot"><span class="slot-type">${escapeHtml(attachment.slot)}</span><strong>${escapeHtml(attachment.name)}</strong>${attachment.level ? `<em>${escapeHtml(attachment.level)}</em>` : ""}</li>`;
    }).join("");
  }

  function premiumPerkHtml(items) {
    return items.map((item, index) => `<li class="perk-chip"><span>${index + 1}</span><strong>${escapeHtml(displayExtra(item))}</strong></li>`).join("");
  }

  function ensureBuildCode(card, build) {
    const setupPanel = card.querySelector(".setup-panel");
    if (!setupPanel || !build.code) return;

    let codeBox = setupPanel.querySelector(".build-code-box");
    if (!codeBox) {
      setupPanel.insertAdjacentHTML("afterbegin", `<div class="build-code-box"><span>Klassen-Code</span><strong></strong></div>`);
      codeBox = setupPanel.querySelector(".build-code-box");
    }

    const value = codeBox.querySelector("strong");
    if (value) value.textContent = build.code;
  }

  function updatePanelCounters(card, build) {
    const attachmentCount = card.querySelector(".attachments-panel .detail-panel-title strong");
    const extrasCount = card.querySelector(".setup-panel .detail-panel-title strong");
    const attachments = cleanAttachments(build.attachments);
    if (attachmentCount) attachmentCount.textContent = hasPendingAttachments(attachments) ? "Update" : `${attachments.length}/5`;
    if (extrasCount) extrasCount.textContent = String(build.extras.length);
  }

  function applyBuild(card) {
    const key = slug(card.dataset.loadoutCard || card.querySelector(".weapon-name")?.textContent);
    const build = registryBuilds.get(key) || supplementalBuilds[key];
    if (!build) return;

    const premiumAttachments = card.querySelector(".premium-attachment-list");
    const premiumPerks = card.querySelector(".premium-perk-list");
    const legacyAttachments = card.querySelector(".attachment-list");
    const legacyPerks = card.querySelector(".perk-list");

    const attachments = cleanAttachments(build.attachments);
    if (premiumAttachments) premiumAttachments.innerHTML = premiumAttachmentHtml(attachments);
    else if (legacyAttachments) legacyAttachments.innerHTML = legacyItemsHtml(hasPendingAttachments(attachments) ? ["Aufsätze werden aktualisiert"] : attachments);

    if (premiumPerks) premiumPerks.innerHTML = premiumPerkHtml(build.extras);
    else if (legacyPerks) {
      legacyPerks.innerHTML = legacyItemsHtml([
        `Code: ${build.code}`,
        ...build.extras.map((extra, index) => `Extra ${index + 1}: ${extra}`),
      ]);
    }

    ensureBuildCode(card, build);
    updatePanelCounters(card, build);
    card.dataset.supplementalBuildSignature = `supplement:${build.code}|${build.attachments.join("|")}|${build.extras.join("|")}`;
  }

  let scheduled = false;

  function run() {
    scheduled = false;
    document.querySelectorAll("#loadoutGrid .loadout-card").forEach(applyBuild);
  }

  function scheduleRun() {
    if (scheduled) return;
    scheduled = true;
    window.requestAnimationFrame(run);
  }

  function watchGrid() {
    const grid = document.querySelector("#loadoutGrid");
    if (!grid || grid.dataset.supplementalBuildsWatched === "true") return;
    grid.dataset.supplementalBuildsWatched = "true";
    new MutationObserver(scheduleRun).observe(grid, { childList: true, subtree: true });
  }

  function init() {
    loadRegistry();
    scheduleRun();
    watchGrid();
    window.__loadoutSupplementalBuilds = supplementalBuilds;
    window.__loadoutBuildSupplementsReady = true;
  }

  if (document.readyState === "loading") document.addEventListener("DOMContentLoaded", init, { once: true });
  else init();

  window.setTimeout(init, 120);
  window.setTimeout(init, 900);
}());

/* source: loadout-filter-fix.js */
(function () {
  const STYLE_ID = "loadout-filter-fix-style";
  const HIDDEN_CLASS = "filter-hidden";
  const state = { meta: null, timer: 0, reinforce: 0, filter: "all" };

  const labels = {
    de: { of: "von", shown: "Meta-Waffen angezeigt" },
    en: { of: "of", shown: "meta weapons shown" },
  };

  function lang() {
    return localStorage.getItem("loadoutLabLang") === "en" || document.documentElement.lang === "en" ? "en" : "de";
  }

  function text(value) {
    return String(value || "").toLowerCase();
  }

  function installStyle() {
    let style = document.getElementById(STYLE_ID);
    if (!style) {
      style = document.createElement("style");
      style.id = STYLE_ID;
      document.head.append(style);
    }
    style.textContent = `
      body #loadoutGrid .loadout-card.${HIDDEN_CLASS},
      body #loadoutGrid .tier-group.${HIDDEN_CLASS},
      body #loadoutGrid .meta-tier-heading.${HIDDEN_CLASS} {
        display: none !important;
      }

      body #resultCount[data-filter-count] {
        font-size: 0 !important;
      }

      body #resultCount[data-filter-count]::after {
        content: attr(data-filter-count);
        font-size: 0.95rem !important;
      }
    `;
  }

  function versionedUrl(path) {
    const url = new URL(path, window.location.href);
    url.searchParams.set("fresh", window.__loadoutLabMetaVersion || String(Date.now()));
    url.searchParams.set("t", String(Date.now()));
    return url.toString();
  }

  async function loadMeta() {
    if (state.meta) return state.meta;
    const response = await fetch(versionedUrl("data/wzstats-meta.json"), { cache: "no-store" });
    if (!response.ok) throw new Error(`Meta HTTP ${response.status}`);
    state.meta = await response.json();
    return state.meta;
  }

  function currentList() {
    if (!state.meta) return null;
    const active = document.querySelector(".secondary-mode-switch .mode-button.active")?.dataset.mode;
    return active === "bo7-ranked" ? state.meta.bo7Ranked : state.meta.warzoneRanked;
  }

  function itemMap(list) {
    return new Map((list?.items || []).map((item) => [text(item.name), item]));
  }

  function activeFilter() {
    const active = document.querySelector("#filterToolbar .filter-button.active[data-smart-filter]");
    if (active?.dataset.smartFilter) state.filter = active.dataset.smartFilter;
    return state.filter;
  }

  function rankFromCard(card) {
    const ownText = card.querySelector(".rank-badge")?.childNodes?.[0]?.textContent || card.querySelector(".rank-badge")?.textContent || "";
    return Number((ownText.match(/\d+/) || [])[0]) || 9999;
  }

  function tierFromCard(card) {
    const badge = text(card.querySelector(".rank-badge span")?.textContent || "");
    const classes = text(card.className);
    if (badge.includes("a-tier") || classes.includes("tier-card-a")) return "A";
    if (badge.includes("b-tier") || classes.includes("tier-card-b")) return "B";
    if (badge.includes("c-tier") || classes.includes("tier-card-c")) return "C";
    if (badge.includes("d-tier") || classes.includes("tier-card-d")) return "D";
    if (badge.includes("meta") || badge.includes("legendaer") || badge.includes("legendary") || classes.includes("tier-card-meta")) return "META";
    return "";
  }

  function cardRoleFallback(card) {
    const content = text(card?.textContent);
    const match = content.match(/rolle\s*(long range|close range|langstrecke|kurzstrecke|sniper|scharfschuetze|scharfschutze)/);
    return match ? match[1] : "";
  }

  function roleFromCard(card) {
    return card.querySelector(".stat-row strong")?.textContent || cardRoleFallback(card);
  }

  function domItem(card) {
    return {
      position: rankFromCard(card),
      tier: tierFromCard(card),
      role: roleFromCard(card),
      weaponClass: card.querySelector(".mode-pill")?.textContent || "",
      attachments: card.querySelectorAll(".loadout-slot:not(.placeholder-slot), .attachment-list li").length ? ["dom"] : [],
    };
  }

  function hasVerifiedAttachments(item, card) {
    if (Array.isArray(item?.attachments) && item.attachments.length) return true;
    const content = text(card?.textContent);
    if (content.includes("keine gepr") || content.includes("no verified")) return false;
    return (card?.querySelectorAll(".loadout-slot:not(.placeholder-slot), .attachment-list li") || []).length > 0;
  }

  function roleBucket(item, card) {
    const direct = [item?.role, item?.roleLabel, item?.category, item?.rangeLabel, card?.dataset?.role]
      .filter(Boolean)
      .join(" ");
    return text(direct || cardRoleFallback(card));
  }

  function matchesFilter(filter, item, card) {
    if (!item) return true;
    const role = roleBucket(item, card);
    const weaponClass = text(item.weaponClass);
    if (filter === "top10") return Number(item.position) <= 10;
    if (filter === "meta") return item.tier === "META";
    if (filter === "a") return item.tier === "A";
    if (filter === "b") return item.tier === "B";
    if (filter === "long") return role.includes("long range") || role.includes("langstrecke");
    if (filter === "close") return role.includes("close range") || role.includes("kurzstrecke");
    if (filter === "sniper") return /sniper|scharfsch|marksman/.test(role) || /sniper|marksman/.test(weaponClass);
    if (filter === "hasBuild") return hasVerifiedAttachments(item, card);
    if (filter === "noBuild") return !hasVerifiedAttachments(item, card);
    return true;
  }

  function isCardVisible(card) {
    return !card.classList.contains(HIDDEN_CLASS);
  }

  function syncTierVisibility(grid) {
    grid.querySelectorAll(".tier-group").forEach((group) => {
      const hasVisible = [...group.querySelectorAll(".loadout-card")].some(isCardVisible);
      group.classList.toggle(HIDDEN_CLASS, !hasVisible);
    });

    grid.querySelectorAll(".meta-tier-heading").forEach((heading) => {
      let hasVisible = false;
      let node = heading.nextElementSibling;
      while (node && !node.classList.contains("meta-tier-heading")) {
        if (node.classList.contains("loadout-card") && isCardVisible(node)) {
          hasVisible = true;
          break;
        }
        node = node.nextElementSibling;
      }
      heading.classList.toggle(HIDDEN_CLASS, !hasVisible);
    });
  }

  function updateCount(visible, total) {
    const count = document.getElementById("resultCount");
    if (!count) return;
    const copy = labels[lang()] || labels.de;
    const value = `${visible} ${copy.of} ${total} ${copy.shown}`;
    count.dataset.filterCount = value;
    count.textContent = value;
  }

  function applyFilter() {
    const grid = document.getElementById("loadoutGrid");
    if (!grid) return;

    const cards = [...grid.querySelectorAll(".loadout-card")];
    const list = currentList();
    const filter = activeFilter();
    const query = text(document.getElementById("loadoutSearch")?.value).trim();
    const byName = itemMap(list);
    const total = list?.items?.length || cards.length;
    let visible = 0;

    cards.forEach((card) => {
      const item = byName.get(text(card.dataset.loadoutCard || "")) || domItem(card);
      const searchable = text([
        card.dataset.loadoutCard,
        item.weaponClass,
        item.role,
        item.tier,
        card.textContent,
      ].filter(Boolean).join(" "));
      const show = matchesFilter(filter, item, card) && (!query || searchable.includes(query));
      card.classList.toggle(HIDDEN_CLASS, !show);
      card.hidden = false;
      card.style.display = "";
      if (show) visible += 1;
    });

    syncTierVisibility(grid);
    document.querySelectorAll("#filterToolbar .filter-button[data-smart-filter]").forEach((button) => {
      button.classList.toggle("active", button.dataset.smartFilter === filter);
    });
    updateCount(visible, total);
    window.__loadoutFilterFixLast = { filter, visible, total, at: Date.now() };
  }

  function safeApply() {
    try {
      applyFilter();
    } catch (error) {
      window.__loadoutFilterFixError = String(error && (error.stack || error.message) || error);
      console.warn("Loadout Filter konnte nicht angewendet werden", error);
    }
  }

  function reinforce(duration = 2200) {
    window.clearInterval(state.reinforce);
    const end = Date.now() + duration;
    state.reinforce = window.setInterval(() => {
      safeApply();
      if (Date.now() >= end) window.clearInterval(state.reinforce);
    }, 80);
  }

  function schedule(delay = 80) {
    window.clearTimeout(state.timer);
    state.timer = window.setTimeout(safeApply, delay);
  }

  function handleFilterClick(event) {
    const button = event.target.closest("#filterToolbar .filter-button[data-smart-filter]");
    if (!button) return;
    event.preventDefault();
    event.stopImmediatePropagation();
    state.filter = button.dataset.smartFilter || "all";
    document.querySelectorAll("#filterToolbar .filter-button").forEach((item) => {
      item.classList.toggle("active", item === button);
    });
    window.dispatchEvent(new CustomEvent("loadoutlab:filter-change", {
      detail: { filter: state.filter },
    }));
    safeApply();
    queueMicrotask(safeApply);
    requestAnimationFrame(safeApply);
    schedule(20);
    reinforce();
  }

  function bindEvents() {
    if (window.__loadoutFilterFixReady) return;
    window.__loadoutFilterFixReady = true;
    window.__loadoutFilterFixApply = safeApply;

    window.addEventListener("click", handleFilterClick, true);
    document.addEventListener("click", handleFilterClick, true);

    window.addEventListener("input", (event) => {
      if (!event.target.closest("#loadoutSearch")) return;
      event.stopImmediatePropagation();
      safeApply();
      queueMicrotask(safeApply);
      reinforce(1200);
    }, true);

    document.addEventListener("input", (event) => {
      if (event.target.closest("#loadoutSearch")) { window.setTimeout(safeApply, 120); reinforce(1200); }
    }, true);

    document.addEventListener("change", (event) => {
      if (event.target.closest("#sortSelect")) { window.setTimeout(safeApply, 120); reinforce(1200); }
    }, true);

    document.addEventListener("click", (event) => {
      if (event.target.closest(".secondary-mode-switch .mode-button, .content-tab[data-tab='weapons'], [data-language]")) {
        window.setTimeout(safeApply, 180);
        window.setTimeout(safeApply, 480);
      }
    }, true);

    const observer = new MutationObserver(() => schedule(100));
    const watch = () => {
      const grid = document.getElementById("loadoutGrid");
      if (!grid || grid.dataset.filterFixObserved) return;
      grid.dataset.filterFixObserved = "true";
      observer.observe(grid, { childList: true, subtree: true });
    };
    watch();
    window.setTimeout(watch, 500);
  }

  async function init() {
    installStyle();
    bindEvents();
    try { await loadMeta(); } catch (error) { console.warn("Loadout filter meta konnte nicht geladen werden", error); }
    schedule(180);
    window.setTimeout(safeApply, 700);
    window.setTimeout(safeApply, 1600);
  }

  if (document.readyState === "loading") document.addEventListener("DOMContentLoaded", init, { once: true });
  else init();
}());

/* source: subnav-cleanup.js */
(function () {
  const STYLE_ID = "subnav-cleanup-20260601";
  let refreshFrame = 0;
  const css = `
    body .tier-first > .primary-mode-switch,
    body .tier-first > .secondary-mode-switch,
    body .tier-first > .content-tabs {
      position: absolute !important;
      left: -9999px !important;
      top: auto !important;
      width: 1px !important;
      height: 1px !important;
      overflow: hidden !important;
      opacity: 0 !important;
      pointer-events: none !important;
    }

    body .tier-first > .loadout-subnav-row {
      display: none !important;
    }

    body .minimal-control-panel .loadout-subnav-row {
      display: contents !important;
      margin: 0 !important;
      padding: 0 !important;
      border: 0 !important;
      background: transparent !important;
      box-shadow: none !important;
    }

    body .minimal-control-panel .minimal-control-group:has(> [hidden]) {
      display: none !important;
    }

    body .minimal-control-panel .secondary-mode-switch,
    body .minimal-control-panel .minimal-control-group .secondary-mode-switch,
    body .minimal-control-panel .loadout-subnav-row .secondary-mode-switch {
      display: grid !important;
      grid-template-columns: repeat(3, minmax(0, 1fr)) !important;
      gap: 0.34rem !important;
      width: 100% !important;
      max-width: none !important;
      min-width: 0 !important;
      min-height: 0 !important;
    }

    body .minimal-control-panel .secondary-mode-switch .mode-button,
    body .minimal-control-panel .loadout-subnav-row .mode-button {
      display: inline-flex !important;
      width: auto !important;
      min-width: 0 !important;
      min-height: 2.12rem !important;
      align-items: center !important;
      justify-content: center !important;
      white-space: nowrap !important;
    }

    body .weapon-art,
    body .weapon-thumb,
    body .camo-swatch,
    body .mode-image-gallery figure,
    body #loadoutGrid .weapon-art,
    body #loadoutGrid .loadout-card .weapon-art,
    body .loadout-grid .loadout-card .weapon-art,
    body #loadoutGrid > .loadout-card.tier-absolute-meta .weapon-art {
      border: 0 !important;
      background: transparent !important;
      box-shadow: none !important;
    }

    body .mode-info-main > img,
    body .mode-image-gallery img,
    body .update-card img,
    body .ranked-map-card img,
    body .camo-swatch img,
    body .weapon-art img,
    body .weapon-thumb img,
    body #loadoutGrid .weapon-art img,
    body #loadoutGrid .loadout-card .weapon-art img,
    body .loadout-grid .loadout-card .weapon-art img {
      border: 0 !important;
      outline: 0 !important;
      background: transparent !important;
      box-shadow: none !important;
    }

    body .mode-info-main > img,
    body .update-card img {
      border-bottom: 0 !important;
    }

    @media (max-width: 620px) {
      body .minimal-control-panel .secondary-mode-switch,
      body .minimal-control-panel .minimal-control-group .secondary-mode-switch,
      body .minimal-control-panel .loadout-subnav-row .secondary-mode-switch {
        grid-template-columns: repeat(3, minmax(0, 1fr)) !important;
      }
    }
  `;

  function installStyle() {
    if (!document.head) return;
    let style = document.getElementById(STYLE_ID);
    if (!style) {
      style = document.createElement("style");
      style.id = STYLE_ID;
    }
    style.textContent = css;
    document.head.append(style);
  }

  function unwrapSubnavRows() {
    document.querySelectorAll(".loadout-subnav-row").forEach((row) => {
      const parent = row.parentElement;
      const secondary = row.querySelector(".secondary-mode-switch");
      const tabs = row.querySelector("#contentTabs");
      if (parent) {
        if (secondary) parent.insertBefore(secondary, row);
        if (tabs) parent.insertBefore(tabs, row);
      }
      row.remove();
    });
  }

  function refresh() {
    installStyle();
    unwrapSubnavRows();
  }

  function getClosestTopControl(target) {
    const element = target?.nodeType === 1 ? target : target?.parentElement;
    return element?.closest?.(".minimal-control-panel, .primary-mode-switch, .secondary-mode-switch, .content-tabs");
  }

  function setButtonLabel(selector, label) {
    const button = document.querySelector(selector);
    if (button && button.textContent !== label) button.textContent = label;
  }

  function orderButtons(switcher, modes) {
    if (!switcher) return;
    const buttons = modes
      .map((mode) => switcher.querySelector(`.mode-button[data-mode="${mode}"]`))
      .filter(Boolean);

    buttons.forEach((button, index) => {
      const visibleButtons = Array.from(switcher.children).filter((child) => child.classList?.contains("mode-button"));
      const current = visibleButtons[index];
      if (current !== button) switcher.insertBefore(button, current || null);
    });
  }

  function normalizeTopButtons() {
    orderButtons(document.querySelector(".primary-mode-switch"), ["updates", "mw4-info", "season4-info"]);
  }

  function ensureControlGroup(panel, key, label, element, className = "") {
    if (!panel || !element) return null;
    let group = panel.querySelector(`.minimal-control-group[data-control-group="${key}"]`);
    if (!group) {
      group = document.createElement("div");
      group.className = "minimal-control-group";
      group.dataset.controlGroup = key;
    }

    group.classList.toggle("compact", className === "compact");
    const shouldHide = Boolean(element.hidden);
    if (group.hidden !== shouldHide) group.hidden = shouldHide;

    let groupLabel = Array.from(group.children).find((child) => child.classList?.contains("minimal-control-label"));
    if (!groupLabel) {
      groupLabel = document.createElement("span");
      groupLabel.className = "minimal-control-label";
      group.prepend(groupLabel);
    }
    if (groupLabel.textContent !== label) groupLabel.textContent = label;

    if (element.parentElement !== group) group.append(element);
    return group;
  }

  function syncControlGroups(panel, groups) {
    const ordered = groups.filter(Boolean);
    ordered.forEach((group, index) => {
      if (group.parentElement !== panel) panel.append(group);
      const current = Array.from(panel.children).filter((child) => child.classList?.contains("minimal-control-group"))[index];
      if (current !== group) panel.insertBefore(group, current || null);
    });

    panel.querySelectorAll(".minimal-control-group").forEach((group) => {
      if (!ordered.includes(group)) group.remove();
    });
  }

  function syncMinimalPanel() {
    const section = document.querySelector(".tier-first");
    if (!section) return;

    normalizeTopButtons();
    const primarySwitch = document.querySelector(".primary-mode-switch");
    const secondarySwitch = document.querySelector(".secondary-mode-switch");
    const contentTabs = document.querySelector("#contentTabs");
    if (!primarySwitch && !secondarySwitch && !contentTabs) return;

    let panel = Array.from(section.children).find((child) => child.classList?.contains("minimal-control-panel"));
    if (!panel) {
      panel = document.createElement("div");
      panel.className = "minimal-control-panel";
      panel.setAttribute("aria-label", "Schnellauswahl");
      const heading = section.querySelector(".section-heading");
      const anchor = primarySwitch || secondarySwitch || contentTabs || section.querySelector(".controls");
      if (anchor?.parentElement === section) section.insertBefore(panel, anchor);
      else if (heading) heading.after(panel);
      else section.prepend(panel);
    }

    syncControlGroups(panel, [
      ensureControlGroup(panel, "info", "Info", primarySwitch),
      ensureControlGroup(panel, "meta", "Meta", secondarySwitch),
      ensureControlGroup(panel, "view", "Ansicht", contentTabs, "compact"),
    ]);
  }

  function scheduleRefresh() {
    if (refreshFrame) return;
    refreshFrame = window.requestAnimationFrame(() => {
      refreshFrame = 0;
      refresh();
      syncMinimalPanel();
    });
  }

  function init() {
    refresh();
    syncMinimalPanel();
    window.setTimeout(scheduleRefresh, 0);
    document.addEventListener("click", (event) => {
      if (getClosestTopControl(event.target)) scheduleRefresh();
    }, true);
    window.addEventListener("loadoutlab:lite-render", scheduleRefresh);
  }

  if (document.readyState === "loading") document.addEventListener("DOMContentLoaded", init, { once: true });
  else init();
}());

/* source: performance.js */
(function () {
  const STYLE_ID = "loadoutlab-performance-20260529";

  const css = `
    :root {
      --premium-green: #9cff35;
      --premium-green-2: #25e873;
      --premium-gold: #ffd35a;
      --premium-line: rgba(255, 255, 255, 0.10);
      --premium-panel: rgba(12, 16, 21, 0.94);
      --premium-card-shadow: 0 8px 20px rgba(0, 0, 0, 0.24);
    }

    html {
      scroll-padding-top: 5.25rem;
      scroll-behavior: auto !important;
    }

    body {
      background:
        linear-gradient(180deg, rgba(156, 255, 53, 0.03), transparent 15rem),
        radial-gradient(circle at 18% 8%, rgba(37, 232, 115, 0.055), transparent 26rem),
        #05070a !important;
      text-rendering: optimizeLegibility;
    }

    body::before,
    .weapon-compare-panel,
    #weaponComparePanel,
    .meta-patch-summary:empty {
      display: none !important;
      content: none !important;
    }

    .site-header {
      transform: none !important;
      backface-visibility: visible !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: 0 8px 22px rgba(0, 0, 0, 0.22) !important;
    }

    .brand-mark {
      border: 1px solid rgba(255, 255, 255, 0.16);
      box-shadow: 0 0 0 2px rgba(156, 255, 53, 0.11) !important;
    }

    .tier-first {
      max-width: 92rem;
      margin-inline: auto;
      padding-inline: clamp(0.9rem, 2.35vw, 2rem) !important;
    }

    .tier-first .section-heading {
      max-width: 100% !important;
      border: 1px solid rgba(255, 255, 255, 0.10);
      border-left: 5px solid var(--premium-green);
      border-radius: 0.55rem;
      padding: clamp(1rem, 2.2vw, 1.55rem) clamp(1rem, 2.4vw, 1.7rem) !important;
      background:
        linear-gradient(120deg, rgba(156, 255, 53, 0.10), transparent 38%),
        rgba(10, 14, 19, 0.88) !important;
      box-shadow: 0 10px 26px rgba(0, 0, 0, 0.22) !important;
      contain: layout paint;
    }

    .tier-first .section-heading::after {
      content: none !important;
    }

    .tier-first h1 {
      line-height: 0.95 !important;
      text-wrap: balance;
    }

    .updated-note {
      border-color: rgba(255, 212, 92, 0.30) !important;
      background: rgba(9, 12, 17, 0.88) !important;
      box-shadow: none !important;
    }

    .primary-mode-switch,
    .secondary-mode-switch,
    .content-tabs,
    .controls,
    .toolbar {
      max-width: 100% !important;
      contain: layout paint;
      box-shadow: none !important;
    }

    .primary-mode-switch .mode-button,
    .secondary-mode-switch .mode-button,
    .content-tab,
    .filter-button,
    .expand-button,
    .copy-button {
      transform: none !important;
      will-change: auto !important;
      transition: background-color 80ms ease, border-color 80ms ease, color 80ms ease !important;
    }

    .primary-mode-switch .mode-button.active,
    .secondary-mode-switch .mode-button.active,
    .content-tab.active,
    .filter-button.active {
      border-color: rgba(156, 255, 53, 0.58) !important;
      box-shadow: 0 0 0 1px rgba(156, 255, 53, 0.18) !important;
    }

    iframe[data-deferred-src] {
      background: #05070a !important;
    }

    #loadoutGrid {
      contain: none;
    }

    #loadoutGrid .meta-tier-heading,
    .loadout-grid .meta-tier-heading,
    #loadoutGrid .tier-header,
    .loadout-grid .tier-header {
      position: relative;
      overflow: hidden;
      border-radius: 0.45rem !important;
      box-shadow: none !important;
      contain: none;
      content-visibility: visible;
      contain-intrinsic-size: auto;
    }

    #loadoutGrid .loadout-card,
    .loadout-grid .loadout-card {
      isolation: isolate;
      contain: none;
      content-visibility: visible !important;
      contain-intrinsic-size: auto;
      box-shadow: var(--premium-card-shadow) !important;
      transform: none !important;
      transition: border-color 80ms ease, background-color 80ms ease !important;
      will-change: auto !important;
    }

    #loadoutGrid .loadout-card.expanded,
    .loadout-grid .loadout-card.expanded {
      content-visibility: visible !important;
      contain-intrinsic-size: auto;
    }

    #loadoutGrid .loadout-card:hover,
    .loadout-grid .loadout-card:hover {
      transform: none !important;
      box-shadow: var(--premium-card-shadow) !important;
    }

    #loadoutGrid .loadout-card .weapon-art,
    .loadout-grid .loadout-card .weapon-art {
      flex-shrink: 0;
      border-radius: 0.45rem !important;
      box-shadow: none !important;
      transform: none !important;
      will-change: auto !important;
      contain: layout paint !important;
    }

    #loadoutGrid .loadout-card .weapon-art img,
    .loadout-grid .loadout-card .weapon-art img,
    #loadoutGrid .loadout-card:hover .weapon-art img,
    .loadout-grid .loadout-card:hover .weapon-art img {
      transform: none !important;
      transition: none !important;
      animation: none !important;
      will-change: auto !important;
      object-fit: contain !important;
      filter: none !important;
    }

    #loadoutGrid .loadout-card.details-flash::after,
    #loadoutGrid .loadout-card.details-flash .expand-button::after,
    #loadoutGrid .loadout-card.expanded .attachment-columns,
    #loadoutGrid .loadout-card .meta-card-details,
    #loadoutGrid .loadout-card .card-details {
      animation: none !important;
      transition: none !important;
    }

    #loadoutGrid .loadout-card .meta-card-details,
    #loadoutGrid .loadout-card .card-details {
      contain: layout paint style !important;
    }

    #loadoutGrid .loadout-card:not(.expanded) .meta-card-details,
    #loadoutGrid .loadout-card:not(.expanded) .card-details,
    #loadoutGrid .loadout-card .meta-card-details[hidden],
    #loadoutGrid .loadout-card .card-details[hidden] {
      display: none !important;
    }

    #loadoutGrid .loadout-card.expand-animating {
      z-index: auto !important;
    }

    .weapon-name {
      text-wrap: balance;
    }

    .attachment-list,
    .perk-list,
    .premium-attachment-list,
    .premium-perk-list {
      box-shadow: none !important;
      background: rgba(10, 12, 10, 0.44) !important;
    }

    .mode-info-main,
    .mode-info-side,
    .update-card,
    .map-panel,
    .map-board,
    .camo-card,
    .timeline article {
      box-shadow: 0 8px 22px rgba(0, 0, 0, 0.22) !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }

    #loadoutGrid .meta-tier-heading.tier-heading-meta,
    #loadoutGrid .tier-heading-meta,
    #loadoutGrid .tier-group.tier-absolute-meta .tier-header {
      --tier-color: #ffd35a !important;
      --tier-rgb: 255, 211, 90 !important;
      --heading-tier-color: #ffd35a !important;
      --heading-tier-rgb: 255, 211, 90 !important;
      border-color: rgba(255, 211, 90, 0.56) !important;
      border-left-color: #ffd35a !important;
      background:
        linear-gradient(90deg, rgba(255, 211, 90, 0.34), rgba(255, 184, 46, 0.13) 42%, rgba(255, 255, 255, 0.035)),
        #171409 !important;
      box-shadow:
        0 0.85rem 1.8rem rgba(0, 0, 0, 0.30),
        0 0 1.65rem rgba(255, 211, 90, 0.18) !important;
    }

    #loadoutGrid .meta-tier-heading.tier-heading-meta span,
    #loadoutGrid .meta-tier-heading.tier-heading-meta small,
    #loadoutGrid .tier-heading-meta span,
    #loadoutGrid .tier-heading-meta small,
    #loadoutGrid .tier-group.tier-absolute-meta .tier-header h3,
    #loadoutGrid .tier-group.tier-absolute-meta .tier-header span {
      color: #ffd35a !important;
      text-shadow: 0 0 1rem rgba(255, 211, 90, 0.30) !important;
    }

    #loadoutGrid .loadout-card.tier-card-meta,
    #loadoutGrid .loadout-card.tier-absolute-meta,
    #loadoutGrid .tier-group.tier-absolute-meta .loadout-card {
      --tier-card-color: #ffd35a !important;
      --tier-card-rgb: 255, 211, 90 !important;
      border-color: rgba(255, 211, 90, 0.48) !important;
      background:
        linear-gradient(135deg, rgba(255, 211, 90, 0.22), rgba(255, 184, 46, 0.09) 44%, rgba(255, 255, 255, 0.025)),
        #141208 !important;
      box-shadow:
        inset 0.48rem 0 0 rgba(255, 211, 90, 0.94),
        0 0 0 1px rgba(255, 211, 90, 0.14),
        0 0.75rem 1.65rem rgba(0, 0, 0, 0.28),
        0 0 1.35rem rgba(255, 211, 90, 0.13) !important;
    }

    #loadoutGrid .loadout-card.tier-card-meta .rank-badge,
    #loadoutGrid .loadout-card.tier-absolute-meta .rank-badge,
    #loadoutGrid .tier-group.tier-absolute-meta .rank-badge {
      background: linear-gradient(135deg, #ffb82e, #ffe58d 58%, #ffd35a) !important;
      color: #1a1200 !important;
    }

    #loadoutGrid .loadout-card.tier-card-meta .mode-pill,
    #loadoutGrid .loadout-card.tier-absolute-meta .mode-pill,
    #loadoutGrid .tier-group.tier-absolute-meta .mode-pill,
    #loadoutGrid .loadout-card.tier-card-meta .meta-score-pill span,
    #loadoutGrid .loadout-card.tier-card-meta .meta-score-pill em,
    #loadoutGrid .loadout-card.tier-absolute-meta .meta-score-pill span,
    #loadoutGrid .loadout-card.tier-absolute-meta .meta-score-pill em,
    #loadoutGrid .tier-group.tier-absolute-meta .meta-score-pill span,
    #loadoutGrid .tier-group.tier-absolute-meta .meta-score-pill em,
    #loadoutGrid .loadout-card.tier-card-meta .slot-type,
    #loadoutGrid .loadout-card.tier-absolute-meta .slot-type,
    #loadoutGrid .tier-group.tier-absolute-meta .slot-type {
      color: #ffd35a !important;
    }

    #loadoutGrid .loadout-card.tier-card-meta .perk-chip span,
    #loadoutGrid .loadout-card.tier-absolute-meta .perk-chip span,
    #loadoutGrid .tier-group.tier-absolute-meta .perk-chip span,
    #loadoutGrid .loadout-card.tier-card-meta .expand-button:hover,
    #loadoutGrid .loadout-card.tier-card-meta.expanded .expand-button,
    #loadoutGrid .loadout-card.tier-absolute-meta .expand-button:hover,
    #loadoutGrid .loadout-card.tier-absolute-meta.expanded .expand-button {
      background: #ffd35a !important;
      color: #071008 !important;
    }

    @media (max-width: 760px) {
      .tier-first {
        padding-inline: 0.85rem !important;
      }

      .primary-mode-switch,
      .secondary-mode-switch {
        grid-template-columns: 1fr !important;
      }
    }

    @media (prefers-reduced-motion: reduce) {
      *,
      *::before,
      *::after {
        animation-duration: 0.001ms !important;
        animation-iteration-count: 1 !important;
        scroll-behavior: auto !important;
        transition-duration: 0.001ms !important;
      }
    }
  `;

  let mediaQueued = false;
  let renderQueued = false;
  let gridObserver = null;

  function installStyle() {
    let style = document.getElementById(STYLE_ID);
    if (!style) {
      style = document.createElement("style");
      style.id = STYLE_ID;
      document.head.append(style);
    }
    style.textContent = css;
  }

  function closestPanel(element) {
    return element.closest(".tab-panel, .mode-info-section, .updates-section, .maps-section, .camos-section");
  }

  function panelIsInactive(panel) {
    return !!panel && (panel.hidden || panel.getAttribute("aria-hidden") === "true" || (panel.classList.contains("tab-panel") && !panel.classList.contains("active")));
  }

  function visibleSoon(element) {
    const rect = element.getBoundingClientRect();
    const height = window.innerHeight || document.documentElement.clientHeight || 900;
    return rect.top < height + 220 && rect.bottom > -120;
  }

  function tuneImage(image, index = 10) {
    if (!image || image.dataset.llMediaTuned === "true") return;
    image.dataset.llMediaTuned = "true";
    image.decoding = "async";
    image.style.transform = "none";
    image.style.willChange = "auto";

    const inactive = panelIsInactive(closestPanel(image));
    const priorityImage = !inactive && (index < 4 || visibleSoon(image));
    image.loading = priorityImage ? "eager" : "lazy";
    image.fetchPriority = priorityImage ? "high" : "low";

    if (!image.getAttribute("width") && image.closest(".weapon-art")) image.setAttribute("width", "320");
    if (!image.getAttribute("height") && image.closest(".weapon-art")) image.setAttribute("height", "180");
  }

  function tuneImages(root = document) {
    const images = root.matches?.("img") ? [root] : [...root.querySelectorAll?.("img") || []];
    images.forEach(tuneImage);
  }

  function deferInactiveIframe(frame) {
    if (!frame) return;
    frame.loading = "lazy";
    frame.style.contain = "layout paint";

    const inactive = panelIsInactive(closestPanel(frame));
    const src = frame.getAttribute("src");
    const deferred = frame.dataset.deferredSrc;

    if (inactive && src) {
      frame.dataset.deferredSrc = src;
      frame.removeAttribute("src");
      return;
    }

    if (!inactive && !src && deferred) {
      frame.setAttribute("src", deferred);
    }
  }

  function tuneIframes(root = document) {
    const frames = root.matches?.("iframe") ? [root] : [...root.querySelectorAll?.("iframe") || []];
    frames.forEach(deferInactiveIframe);
  }

  function stripLegacyBlocks() {
    document.querySelectorAll("#weaponComparePanel, .weapon-compare-panel").forEach((element) => element.remove());
  }

  function optimizeMedia(root = document) {
    tuneImages(root);
    tuneIframes(root);
    stripLegacyBlocks();
  }

  function scheduleMediaPass(delay = 0) {
    if (mediaQueued) return;
    mediaQueued = true;
    window.setTimeout(() => {
      const run = () => {
        mediaQueued = false;
        optimizeMedia();
      };
      if (delay > 120 && "requestIdleCallback" in window) window.requestIdleCallback(run, { timeout: 800 });
      else window.requestAnimationFrame(run);
    }, delay);
  }

  function hasDetailsContent(details) {
    return !!details && !!details.querySelector(".premium-details-grid, .attachment-columns, .premium-attachment-list, .detail-panel");
  }

  function detailsTemplate(card) {
    return [...card.children].find((child) => child.matches?.("template.meta-details-template"));
  }

  function parkCardDetails(card) {
    if (!card || card.classList.contains("expanded")) return false;
    const details = card.querySelector(".meta-card-details, .card-details");
    if (!details || !hasDetailsContent(details)) return false;

    let template = detailsTemplate(card);
    if (!template) {
      template = document.createElement("template");
      template.className = "meta-details-template";
      details.after(template);
    }

    while (details.firstChild) template.content.append(details.firstChild);
    details.hidden = true;
    details.dataset.lazyDetails = "parked";
    return true;
  }

  function parkDetails(root = document) {
    const cards = root.matches?.("#loadoutGrid .loadout-card") ? [root] : [...root.querySelectorAll?.("#loadoutGrid .loadout-card:not(.expanded)") || []];
    let parked = 0;
    cards.forEach((card) => {
      if (parkCardDetails(card)) parked += 1;
    });
    window.__loadoutLabParkedDetails = parked;
  }

  function hydrateDetails(card) {
    const details = card.querySelector(".meta-card-details, .card-details");
    if (!details) return null;

    const template = detailsTemplate(card);
    if (template && !hasDetailsContent(details)) {
      details.append(template.content);
      template.remove();
    }

    details.hidden = false;
    details.dataset.lazyDetails = "hydrated";
    return details;
  }

  function setButtonState(card, open) {
    const button = card.querySelector(".expand-button");
    if (!button) return;
    button.setAttribute("aria-expanded", String(open));
    const label = button.querySelector("span");
    if (label) label.textContent = open ? "Schließen" : "Details";
  }

  function ignoredClick(event, card) {
    if (event.target.closest(".card-details, .meta-card-details, .attachment-list, .perk-list, a, input, select, textarea")) return true;
    const button = event.target.closest("button");
    return button && !button.classList.contains("expand-button") && card.contains(button);
  }

  function toggleDetails(card, open) {
    const details = open ? hydrateDetails(card) : card.querySelector(".meta-card-details, .card-details");
    if (!details) return;

    details.getAnimations?.().forEach((animation) => animation.cancel());
    card.classList.remove("details-flash", "expand-animating");
    card.classList.toggle("expanded", open);
    setButtonState(card, open);

    details.hidden = !open;
    details.style.display = open ? "block" : "";
    details.style.height = "";
    details.style.opacity = "";
    details.style.overflow = "";

    if (!open) parkCardDetails(card);
    scheduleMediaPass(40);
  }

  function activePanel() {
    return document.querySelector(".tab-panel.active")?.dataset.panel || "weapons";
  }

  function call(name) {
    if (typeof window[name] === "function") window[name]();
  }

  function renderActivePanel() {
    const panel = activePanel();
    if (panel === "maps") call("renderMaps");
    else if (panel === "camos") call("renderCamos");
    else if (panel === "mode-info") call("renderModeInfo");
    else if (panel === "updates") call("renderUpdateMode");

    stripLegacyBlocks();
    parkDetails();
    scheduleMediaPass(80);
    window.dispatchEvent(new CustomEvent("loadoutlab:lite-render", { detail: { panel } }));
  }

  function patchRenderLoadouts() {
    if (window.__loadoutLabLiteRenderReady || typeof window.renderLoadouts !== "function") return;
    window.__loadoutLabLiteRenderReady = true;
    window.__loadoutLabOriginalRenderLoadouts = window.renderLoadouts;
    window.renderLoadouts = function liteRenderLoadouts() {
      call("renderMode");
      renderActivePanel();
    };
  }

  function scheduleActiveRender() {
    if (renderQueued) return;
    renderQueued = true;
    window.requestAnimationFrame(() => {
      renderQueued = false;
      renderActivePanel();
    });
  }

  function cardsFromMutations(mutations) {
    const cards = new Set();
    for (const mutation of mutations) {
      mutation.addedNodes.forEach((node) => {
        if (!(node instanceof HTMLElement)) return;
        if (node.matches("#loadoutGrid .loadout-card")) cards.add(node);
        node.querySelectorAll?.(".loadout-card").forEach((card) => cards.add(card));
      });
    }
    return [...cards];
  }

  function observeGrid() {
    const grid = document.querySelector("#loadoutGrid");
    if (!grid || grid.dataset.llPerformanceObserver === "true") return;
    grid.dataset.llPerformanceObserver = "true";

    gridObserver = new MutationObserver((mutations) => {
      const cards = cardsFromMutations(mutations);
      if (!cards.length) return;
      cards.forEach(parkCardDetails);
      cards.forEach(tuneImages);
      stripLegacyBlocks();
    });

    gridObserver.observe(grid, { childList: true });
  }

  function bindEvents() {
    if (window.__loadoutLabPerformanceReady) return;
    window.__loadoutLabPerformanceReady = true;

    document.addEventListener("click", (event) => {
      const card = event.target.closest("#loadoutGrid .loadout-card");
      if (card && !ignoredClick(event, card)) {
        event.preventDefault();
        event.stopPropagation();
        event.stopImmediatePropagation?.();
        toggleDetails(card, !card.classList.contains("expanded"));
        return;
      }

      if (event.target.closest(".content-tab, .primary-mode-switch .mode-button, .secondary-mode-switch .mode-button, #filterToolbar .filter-button")) {
        scheduleActiveRender();
        scheduleMediaPass(100);
      }
    }, true);

    document.addEventListener("input", (event) => {
      if (event.target.matches("#loadoutSearch, #weaponSearch")) {
        window.setTimeout(() => {
          parkDetails();
          scheduleMediaPass(60);
        }, 140);
      }
    });

    document.addEventListener("change", (event) => {
      if (event.target.matches("#sortSelect")) {
        scheduleActiveRender();
        window.setTimeout(parkDetails, 80);
      }
    });

    window.addEventListener("loadoutlab:lite-render", () => {
      parkDetails();
      scheduleMediaPass(80);
    });
  }

  function init() {
    installStyle();
    patchRenderLoadouts();
    bindEvents();
    observeGrid();
    stripLegacyBlocks();
    parkDetails();
    optimizeMedia();
  }

  if (document.readyState === "loading") document.addEventListener("DOMContentLoaded", init, { once: true });
  else init();

  window.setTimeout(init, 120);
  window.setTimeout(init, 700);
  window.addEventListener("pagehide", () => gridObserver?.disconnect(), { once: true });
}());

/* source: loadout-card-polish.js */
(function () {
  const STYLE_ID = "loadout-card-polish-style";
  const state = { timer: 0 };

  const css = `
    body #loadouts.section,
    body section#loadouts {
      padding-left: clamp(1.35rem, 3vw, 3.75rem) !important;
      padding-right: clamp(1.35rem, 3vw, 3.75rem) !important;
    }

    body .weapon-dashboard .controls {
      display: grid !important;
      grid-template-columns: minmax(0, 1fr) minmax(12rem, 16rem) !important;
      gap: 0.8rem !important;
      align-items: end !important;
      margin-bottom: 0.72rem !important;
      padding: 0.72rem !important;
      border: 1px solid rgba(255,255,255,0.075) !important;
      border-radius: 0.48rem !important;
      background: linear-gradient(180deg, rgba(255,255,255,0.035), rgba(255,255,255,0.012)), rgba(7,10,14,0.72) !important;
      box-shadow: inset 0 1px 0 rgba(255,255,255,0.035) !important;
    }

    body .weapon-dashboard .search-box,
    body .weapon-dashboard .select-box {
      display: grid !important;
      gap: 0.35rem !important;
      min-width: 0 !important;
    }

    body .weapon-dashboard .search-box span,
    body .weapon-dashboard .select-box span {
      color: rgba(184, 198, 216, 0.76) !important;
      font-size: 0.72rem !important;
      font-weight: 950 !important;
      line-height: 1 !important;
      text-transform: uppercase !important;
    }

    body .weapon-dashboard input,
    body .weapon-dashboard select {
      min-height: 2.85rem !important;
      border: 1px solid rgba(120, 143, 170, 0.2) !important;
      border-radius: 0.4rem !important;
      background: #0d131c !important;
      color: #f7fbff !important;
      font-weight: 850 !important;
      box-shadow: inset 0 1px 0 rgba(255,255,255,0.035) !important;
    }

    body .toolbar.enhanced-filter-toolbar,
    body #filterToolbar {
      margin-bottom: 0.86rem !important;
      padding: 0.52rem !important;
      border-radius: 0.5rem !important;
      border-color: rgba(120,143,170,0.16) !important;
      background: rgba(8, 12, 18, 0.78) !important;
    }

    body #resultCount[data-filter-count],
    body #resultCount {
      display: block !important;
      min-height: 1.25rem !important;
      margin: 0.15rem 0 0.72rem !important;
      color: #b9ff3d !important;
      font-weight: 950 !important;
    }

    body #loadoutGrid {
      gap: 1.12rem !important;
      contain: layout paint !important;
    }

    body #loadoutGrid .meta-tier-heading {
      --tier-heading-color: #ffd35a;
      --tier-heading-rgb: 255, 211, 90;
      display: flex !important;
      align-items: center !important;
      justify-content: space-between !important;
      gap: 1rem !important;
      min-height: 4.8rem !important;
      margin: 1.25rem 0 0.52rem !important;
      padding: 1.05rem 1.25rem !important;
      border: 1px solid rgba(var(--tier-heading-rgb), 0.48) !important;
      border-left: 1.15rem solid var(--tier-heading-color) !important;
      border-radius: 0.52rem !important;
      background:
        linear-gradient(90deg, rgba(var(--tier-heading-rgb), 0.34), rgba(var(--tier-heading-rgb), 0.13) 38%, rgba(255,255,255,0.03)),
        #0d131c !important;
      box-shadow:
        0 1rem 2rem rgba(0,0,0,0.34),
        0 0 2.35rem rgba(var(--tier-heading-rgb), 0.18),
        inset 0 0 0 1px rgba(var(--tier-heading-rgb), 0.12) !important;
    }

    body #loadoutGrid .meta-tier-heading span {
      color: var(--tier-heading-color) !important;
      font-family: Rajdhani, Inter, system-ui, sans-serif !important;
      font-size: 1.72rem !important;
      font-weight: 950 !important;
      letter-spacing: 0 !important;
      line-height: 1 !important;
      text-transform: uppercase !important;
      text-shadow: 0 0 1.35rem rgba(var(--tier-heading-rgb), 0.5) !important;
    }

    body #loadoutGrid .meta-tier-heading small {
      color: var(--tier-heading-color) !important;
      font-size: 0.88rem !important;
      font-weight: 950 !important;
      text-transform: uppercase !important;
    }

    body #loadoutGrid .meta-tier-heading.tier-heading-meta {
      --tier-heading-color: #ffd35a;
      --tier-heading-rgb: 255, 211, 90;
      min-height: 5.15rem !important;
      border-left-width: 1.35rem !important;
      background:
        linear-gradient(90deg, rgba(255,211,90,0.46), rgba(255,173,55,0.18) 42%, rgba(255,255,255,0.04)),
        #14120b !important;
      box-shadow:
        0 1.25rem 2.4rem rgba(0,0,0,0.38),
        0 0 3.1rem rgba(255,211,90,0.24),
        inset 0 0 0 1px rgba(255,211,90,0.18) !important;
    }

    body #loadoutGrid .meta-tier-heading.tier-heading-a { --tier-heading-color: #35d7ff; --tier-heading-rgb: 53, 215, 255; }
    body #loadoutGrid .meta-tier-heading.tier-heading-b { --tier-heading-color: #ffcf4a; --tier-heading-rgb: 255, 207, 74; }
    body #loadoutGrid .meta-tier-heading.tier-heading-c { --tier-heading-color: #b08cff; --tier-heading-rgb: 176, 140, 255; }
    body #loadoutGrid .meta-tier-heading.tier-heading-d { --tier-heading-color: #ff6f91; --tier-heading-rgb: 255, 111, 145; }

    body #loadoutGrid .loadout-card {
      --tier-card-color: #9aa4b4;
      --tier-card-rgb: 154, 164, 180;
      position: relative !important;
      display: grid !important;
      grid-template-columns: clamp(12rem, 16vw, 14.8rem) minmax(0, 1fr) clamp(5.6rem, 7vw, 6.7rem) !important;
      grid-template-rows: auto auto auto !important;
      align-items: start !important;
      gap: 1rem 1.25rem !important;
      min-height: 10.8rem !important;
      padding: 1.15rem 1.35rem 1rem !important;
      overflow: clip !important;
      border: 1px solid rgba(var(--tier-card-rgb), 0.34) !important;
      border-left: 0.68rem solid var(--tier-card-color) !important;
      border-radius: 0.56rem !important;
      outline: 0 !important;
      background:
        linear-gradient(120deg, rgba(var(--tier-card-rgb), 0.14), rgba(13,19,28,0.92) 24%, rgba(8,12,18,0.98)),
        #101620 !important;
      box-shadow:
        0 0.95rem 2rem rgba(0,0,0,0.32),
        0 0 1.65rem rgba(var(--tier-card-rgb), 0.11),
        inset 0 1px 0 rgba(255,255,255,0.045) !important;
      transform: none !important;
      transition: border-color 120ms ease, box-shadow 120ms ease, background-color 120ms ease !important;
    }

    body #loadoutGrid .loadout-card:hover {
      border-color: rgba(var(--tier-card-rgb), 0.58) !important;
      box-shadow:
        0 1.1rem 2.3rem rgba(0,0,0,0.36),
        0 0 2.2rem rgba(var(--tier-card-rgb), 0.16),
        inset 0 1px 0 rgba(255,255,255,0.055) !important;
    }

    body #loadoutGrid .loadout-card.tier-card-meta,
    body #loadoutGrid .loadout-card.tier-absolute-meta {
      --tier-card-color: #ffd35a;
      --tier-card-rgb: 255, 211, 90;
      background:
        linear-gradient(120deg, rgba(255,211,90,0.18), rgba(21,25,17,0.9) 24%, rgba(9,13,18,0.98)),
        #111720 !important;
    }

    body #loadoutGrid .loadout-card.tier-card-a { --tier-card-color: #35d7ff; --tier-card-rgb: 53, 215, 255; }
    body #loadoutGrid .loadout-card.tier-card-b { --tier-card-color: #ffcf4a; --tier-card-rgb: 255, 207, 74; }
    body #loadoutGrid .loadout-card.tier-card-c { --tier-card-color: #b08cff; --tier-card-rgb: 176, 140, 255; }
    body #loadoutGrid .loadout-card.tier-card-d { --tier-card-color: #ff6f91; --tier-card-rgb: 255, 111, 145; }

    body #loadoutGrid > .loadout-card:nth-child(-n + 3),
    body #loadoutGrid > .loadout-card:nth-child(1),
    body #loadoutGrid > .loadout-card:nth-child(2) {
      transform: none !important;
      outline: 0 !important;
      border-left-color: var(--tier-card-color) !important;
    }

    body #loadoutGrid .weapon-art,
    body .loadout-grid .loadout-card .weapon-art,
    body #loadoutGrid > .loadout-card.tier-absolute-meta .weapon-art {
      grid-column: 1 !important;
      grid-row: 1 !important;
      width: 100% !important;
      max-width: none !important;
      height: 7.05rem !important;
      margin: 0 !important;
      border: 1px solid rgba(var(--tier-card-rgb), 0.18) !important;
      border-radius: 0.46rem !important;
      overflow: hidden !important;
      background:
        radial-gradient(circle at 50% 82%, rgba(var(--tier-card-rgb), 0.12), transparent 58%),
        rgba(3,5,9,0.82) !important;
      box-shadow: inset 0 1px 0 rgba(255,255,255,0.04) !important;
      transform: none !important;
    }

    body #loadoutGrid .weapon-art img,
    body .loadout-grid .loadout-card .weapon-art img,
    body #loadoutGrid .loadout-card:hover .weapon-art img {
      display: block !important;
      width: 100% !important;
      height: 100% !important;
      max-width: none !important;
      max-height: none !important;
      object-fit: contain !important;
      object-position: center center !important;
      padding: 0.08rem !important;
      transform: none !important;
      filter: brightness(1.08) contrast(1.06) saturate(1.08) drop-shadow(0 0.45rem 0.55rem rgba(0,0,0,0.38)) !important;
      transition: none !important;
      animation: none !important;
    }

    body #loadoutGrid .card-body {
      grid-column: 2 !important;
      grid-row: 1 !important;
      min-width: 0 !important;
      padding-right: 0 !important;
      align-self: start !important;
    }

    body #loadoutGrid .card-title-row {
      min-width: 0 !important;
    }

    body #loadoutGrid .mode-pill {
      display: inline-flex !important;
      width: fit-content !important;
      max-width: 100% !important;
      min-height: 1.55rem !important;
      align-items: center !important;
      margin: 0 0 0.45rem !important;
      padding: 0.22rem 0.62rem !important;
      border: 1px solid rgba(var(--tier-card-rgb), 0.22) !important;
      border-radius: 999px !important;
      background: rgba(var(--tier-card-rgb), 0.08) !important;
      color: var(--tier-card-color) !important;
      font-size: 0.76rem !important;
      font-weight: 950 !important;
      line-height: 1 !important;
      letter-spacing: 0 !important;
      text-transform: uppercase !important;
      white-space: nowrap !important;
    }

    body #loadoutGrid .weapon-name {
      max-width: min(100%, 48rem) !important;
      margin: 0 0 0.52rem !important;
      color: #f8fbff !important;
      font-size: 2.35rem !important;
      font-weight: 950 !important;
      letter-spacing: 0 !important;
      line-height: 0.95 !important;
      overflow-wrap: anywhere !important;
      text-shadow: 0 1px 0 rgba(0,0,0,0.5) !important;
    }

    body #loadoutGrid .meta-score-pill {
      display: inline-grid !important;
      grid-template-columns: auto auto auto !important;
      width: fit-content !important;
      min-height: 2.75rem !important;
      align-items: stretch !important;
      margin: 0 0 0.58rem !important;
      overflow: hidden !important;
      border: 1px solid rgba(var(--tier-card-rgb), 0.7) !important;
      border-radius: 0.48rem !important;
      background: rgba(5,8,12,0.82) !important;
      box-shadow: 0 0 0 1px rgba(var(--tier-card-rgb),0.12), 0 0.75rem 1.35rem rgba(var(--tier-card-rgb),0.12) !important;
    }

    body #loadoutGrid .meta-score-pill span {
      display: inline-grid !important;
      place-items: center !important;
      padding: 0.32rem 0.62rem !important;
      background: rgba(var(--tier-card-rgb), 0.18) !important;
      color: var(--tier-card-color) !important;
      font-size: 0.7rem !important;
      font-weight: 950 !important;
      text-transform: uppercase !important;
    }

    body #loadoutGrid .meta-score-pill strong {
      display: inline-grid !important;
      place-items: center !important;
      padding: 0.18rem 0.12rem 0.16rem 0.58rem !important;
      color: #fff !important;
      font-family: Rajdhani, Inter, system-ui, sans-serif !important;
      font-size: 2rem !important;
      font-weight: 950 !important;
      line-height: 0.9 !important;
    }

    body #loadoutGrid .meta-score-pill em {
      display: inline-grid !important;
      place-items: end center !important;
      padding: 0.18rem 0.62rem 0.43rem 0.08rem !important;
      color: var(--tier-card-color) !important;
      font-size: 0.74rem !important;
      font-style: normal !important;
      font-weight: 950 !important;
    }

    body #loadoutGrid .stat-row {
      display: flex !important;
      flex-wrap: wrap !important;
      gap: 0.45rem !important;
      margin: 0 !important;
    }

    body #loadoutGrid .stat-row span {
      display: inline-flex !important;
      width: fit-content !important;
      min-height: 2rem !important;
      align-items: center !important;
      gap: 0.42rem !important;
      padding: 0.24rem 0.62rem !important;
      border: 1px solid rgba(255,255,255,0.075) !important;
      border-radius: 999px !important;
      background: rgba(4,7,10,0.52) !important;
      white-space: nowrap !important;
    }

    body #loadoutGrid .stat-row em {
      color: rgba(255,255,255,0.58) !important;
      font-size: 0.68rem !important;
      font-style: normal !important;
      font-weight: 950 !important;
      line-height: 1 !important;
      text-transform: uppercase !important;
    }

    body #loadoutGrid .stat-row strong {
      color: #f8fbff !important;
      font-size: 0.94rem !important;
      font-weight: 950 !important;
      line-height: 1 !important;
    }

    body #loadoutGrid .rank-badge,
    body .loadout-grid .loadout-card .rank-badge,
    body .tier-group .loadout-card .rank-badge {
      position: static !important;
      grid-column: 3 !important;
      grid-row: 1 !important;
      justify-self: end !important;
      align-self: start !important;
      display: grid !important;
      width: 6.35rem !important;
      min-width: 6.35rem !important;
      height: 6.35rem !important;
      min-height: 6.35rem !important;
      place-items: center !important;
      padding: 0.66rem !important;
      border: 2px solid rgba(var(--tier-card-rgb), 0.78) !important;
      border-radius: 0.48rem !important;
      background: linear-gradient(135deg, var(--tier-card-color), rgba(255,255,255,0.72)) !important;
      color: #080b08 !important;
      font-family: Rajdhani, Inter, system-ui, sans-serif !important;
      font-size: 2.55rem !important;
      font-weight: 950 !important;
      letter-spacing: 0 !important;
      line-height: 0.82 !important;
      text-align: center !important;
      box-shadow: 0 0.95rem 1.8rem rgba(var(--tier-card-rgb), 0.18), 0 0 1.45rem rgba(var(--tier-card-rgb), 0.2) !important;
    }

    body #loadoutGrid .rank-badge span,
    body .loadout-grid .loadout-card .rank-badge span,
    body .tier-group .loadout-card .rank-badge span {
      display: block !important;
      margin: 0.36rem 0 0 !important;
      border: 0 !important;
      padding: 0 !important;
      color: rgba(8,11,8,0.78) !important;
      font-family: Inter, system-ui, sans-serif !important;
      font-size: 0.68rem !important;
      font-weight: 950 !important;
      letter-spacing: 0 !important;
      line-height: 1 !important;
      text-transform: none !important;
    }

    body #loadoutGrid .rank-badge::after,
    body .loadout-grid .loadout-card .rank-badge::after,
    body .tier-group .loadout-card .rank-badge::after {
      content: none !important;
      display: none !important;
    }

    body #loadoutGrid .card-footer {
      grid-column: 1 / -1 !important;
      grid-row: 3 !important;
      display: flex !important;
      align-items: center !important;
      gap: 0.72rem !important;
      margin: 0 !important;
      padding-top: 0.92rem !important;
      border-top: 1px solid rgba(255,255,255,0.075) !important;
    }

    body #loadoutGrid .card-footer .range,
    body #loadoutGrid .tag-list {
      display: none !important;
    }

    body #loadoutGrid .expand-button {
      min-width: 7.8rem !important;
      min-height: 2.75rem !important;
      border: 1px solid rgba(var(--tier-card-rgb), 0.48) !important;
      border-radius: 0.4rem !important;
      background: linear-gradient(135deg, rgba(var(--tier-card-rgb),0.16), rgba(var(--tier-card-rgb),0.07)), #101620 !important;
      color: #fff !important;
      font-weight: 950 !important;
      transition: background-color 120ms ease, border-color 120ms ease, color 120ms ease !important;
    }

    body #loadoutGrid .expand-button:hover,
    body #loadoutGrid .expand-button:focus-visible,
    body #loadoutGrid .loadout-card.expanded .expand-button {
      border-color: rgba(var(--tier-card-rgb), 0.88) !important;
      background: var(--tier-card-color) !important;
      color: #071008 !important;
      outline: none !important;
    }

    body #loadoutGrid .expand-button svg {
      width: 1.18rem !important;
      height: 1.18rem !important;
      fill: currentColor !important;
    }

    body #loadoutGrid .card-details,
    body #loadoutGrid .meta-card-details {
      grid-column: 1 / -1 !important;
      grid-row: 2 !important;
      width: 100% !important;
      max-height: 0 !important;
      margin: 0 !important;
      padding: 0 !important;
      overflow: hidden !important;
      opacity: 0 !important;
      transition: opacity 120ms ease !important;
    }

    body #loadoutGrid .loadout-card.expanded .card-details,
    body #loadoutGrid .loadout-card.expanded .meta-card-details {
      display: block !important;
      max-height: none !important;
      padding-top: 0.75rem !important;
      overflow: visible !important;
      opacity: 1 !important;
    }

    body #loadoutGrid .premium-details-grid {
      display: grid !important;
      grid-template-columns: minmax(0, 1.25fr) minmax(15rem, 0.75fr) !important;
      gap: 0.85rem !important;
    }

    body #loadoutGrid .detail-panel,
    body #loadoutGrid .detail-source-row,
    body #loadoutGrid .build-code-box {
      border-color: rgba(var(--tier-card-rgb), 0.24) !important;
    }

    @media (max-width: 980px) {
      body #loadoutGrid .loadout-card {
        grid-template-columns: minmax(10.5rem, 13rem) minmax(0, 1fr) auto !important;
      }

      body #loadoutGrid .weapon-name {
        font-size: 2.05rem !important;
      }

      body #loadoutGrid .premium-details-grid {
        grid-template-columns: 1fr !important;
      }
    }

    @media (max-width: 760px) {
      body #loadouts.section,
      body section#loadouts {
        padding-left: 1rem !important;
        padding-right: 1rem !important;
      }

      body .weapon-dashboard .controls {
        grid-template-columns: 1fr !important;
      }

      body #loadoutGrid .meta-tier-heading {
        min-height: 4rem !important;
        padding: 0.9rem 1rem !important;
        border-left-width: 0.82rem !important;
      }

      body #loadoutGrid .meta-tier-heading span {
        font-size: 1.32rem !important;
      }

      body #loadoutGrid .meta-tier-heading small {
        font-size: 0.76rem !important;
      }

      body #loadoutGrid .loadout-card {
        grid-template-columns: minmax(0, 1fr) !important;
        gap: 0.85rem !important;
        min-height: 0 !important;
        padding: 0.95rem !important;
        border-left-width: 0.55rem !important;
      }

      body #loadoutGrid .weapon-art,
      body .loadout-grid .loadout-card .weapon-art,
      body #loadoutGrid > .loadout-card.tier-absolute-meta .weapon-art {
        grid-column: 1 !important;
        width: 100% !important;
        height: 8.2rem !important;
        padding-right: 5.25rem !important;
      }

      body #loadoutGrid .card-body,
      body #loadoutGrid .card-details,
      body #loadoutGrid .meta-card-details,
      body #loadoutGrid .card-footer {
        grid-column: 1 !important;
      }

      body #loadoutGrid .rank-badge,
      body .loadout-grid .loadout-card .rank-badge,
      body .tier-group .loadout-card .rank-badge {
        position: absolute !important;
        top: 1rem !important;
        right: 1rem !important;
        width: 4.75rem !important;
        min-width: 4.75rem !important;
        height: 4.75rem !important;
        min-height: 4.75rem !important;
        font-size: 2rem !important;
      }

      body #loadoutGrid .weapon-name {
        font-size: 1.9rem !important;
        padding-right: 0 !important;
      }

      body #loadoutGrid .meta-score-pill strong {
        font-size: 1.72rem !important;
      }

      body #loadoutGrid .card-footer {
        align-items: stretch !important;
      }

      body #loadoutGrid .expand-button {
        width: 100% !important;
      }
    }
  `;

  function installStyle() {
    let style = document.getElementById(STYLE_ID);
    if (!style) {
      style = document.createElement("style");
      style.id = STYLE_ID;
      document.head.append(style);
    }
    style.textContent = css;
  }

  function buttonLabel(expanded) {
    const isEnglish = localStorage.getItem("loadoutLabLang") === "en" || document.documentElement.lang === "en";
    return expanded ? (isEnglish ? "Close" : "Schliessen") : "Details";
  }

  function setExpanded(card, expanded) {
    if (!card) return;
    const button = card.querySelector(".expand-button");
    const panel = card.querySelector(".meta-card-details, .card-details");
    card.classList.toggle("expanded", expanded);
    if (button) {
      button.setAttribute("aria-expanded", expanded ? "true" : "false");
      const label = button.querySelector("span");
      if (label) label.textContent = buttonLabel(expanded);
    }
    if (panel) {
      panel.hidden = false;
      panel.style.display = expanded ? "block" : "";
    }
  }

  function fixDetailsButton(button, intended) {
    const card = button && button.closest(".loadout-card");
    if (!card) return;
    window.setTimeout(() => {
      const handled = card.classList.contains("expanded") === intended || button.getAttribute("aria-expanded") === String(intended);
      const finalState = handled ? intended : !card.classList.contains("expanded");
      setExpanded(card, finalState);
      window.setTimeout(() => setExpanded(card, finalState), 80);
    }, 45);
  }

  function polishCards() {
    document.querySelectorAll("#loadoutGrid .loadout-card").forEach((card) => {
      const rankText = card.querySelector(".rank-badge")?.childNodes?.[0]?.textContent || "";
      const rank = (rankText.match(/\d+/) || [""])[0];
      if (rank) card.dataset.rank = rank;
      card.querySelectorAll(".weapon-name, .mode-pill, .stat-row em, .stat-row strong, .rank-badge, .rank-badge span").forEach((node) => {
        node.style.letterSpacing = "0";
      });
      const panel = card.querySelector(".meta-card-details, .card-details");
      if (panel) panel.hidden = false;
      const expanded = card.classList.contains("expanded") || card.querySelector(".expand-button")?.getAttribute("aria-expanded") === "true";
      setExpanded(card, expanded);
    });
  }

  function schedule(delay = 80) {
    window.clearTimeout(state.timer);
    state.timer = window.setTimeout(polishCards, delay);
  }

  function bindEvents() {
    if (window.__loadoutCardPolishReady) return;
    window.__loadoutCardPolishReady = true;

    document.addEventListener("click", (event) => {
      const button = event.target.closest("#loadoutGrid .expand-button");
      if (button) {
        const card = button.closest(".loadout-card");
        const intended = !(card?.classList.contains("expanded") || button.getAttribute("aria-expanded") === "true");
        fixDetailsButton(button, intended);
        return;
      }
      if (event.target.closest("#filterToolbar, .secondary-mode-switch, .content-tab[data-tab='weapons'], [data-language]")) {
        schedule(90);
        window.setTimeout(polishCards, 280);
        window.setTimeout(polishCards, 760);
      }
    }, true);

    document.addEventListener("input", (event) => {
      if (event.target.closest("#loadoutSearch")) schedule(130);
    }, true);

    document.addEventListener("change", (event) => {
      if (event.target.closest("#sortSelect")) schedule(120);
    }, true);
  }

  function watchGrid() {
    const grid = document.getElementById("loadoutGrid");
    if (!grid || grid.dataset.cardPolishObserved === "true") return;
    grid.dataset.cardPolishObserved = "true";
    new MutationObserver(() => schedule(60)).observe(grid, { childList: true, subtree: true });
  }

  function init() {
    installStyle();
    bindEvents();
    watchGrid();
    polishCards();
    window.setTimeout(watchGrid, 450);
    window.setTimeout(polishCards, 450);
    window.setTimeout(polishCards, 1250);
  }

  if (document.readyState === "loading") document.addEventListener("DOMContentLoaded", init, { once: true });
  else init();
}());

/* source: loadout-card-finalizer.js */
(function () {
  const STYLE_ID = "loadout-card-finalizer-style";

  const css = `
    body #loadoutGrid .loadout-card {
      display: grid !important;
      grid-template-columns: clamp(12rem, 16vw, 14.8rem) minmax(0, 1fr) clamp(5.6rem, 7vw, 6.7rem) !important;
      grid-template-rows: auto auto auto !important;
      align-items: start !important;
      gap: 1rem 1.25rem !important;
      min-height: 10.8rem !important;
      padding: 1.15rem 1.35rem 1rem !important;
      border-radius: 0.56rem !important;
      overflow: clip !important;
    }

    body #loadoutGrid .loadout-card .weapon-art,
    body .loadout-grid .loadout-card .weapon-art,
    body #loadoutGrid > .loadout-card.tier-absolute-meta .weapon-art {
      grid-column: 1 !important;
      grid-row: 1 !important;
      width: 100% !important;
      max-width: none !important;
      height: 7.05rem !important;
      margin: 0 !important;
      border: 1px solid rgba(var(--tier-card-rgb, 255, 211, 90), 0.18) !important;
      border-radius: 0.46rem !important;
      background: rgba(3, 5, 9, 0.82) !important;
      transform: none !important;
      overflow: hidden !important;
    }

    body #loadoutGrid .loadout-card .weapon-art img,
    body .loadout-grid .loadout-card .weapon-art img,
    body #loadoutGrid .loadout-card:hover .weapon-art img {
      display: block !important;
      width: 100% !important;
      height: 100% !important;
      max-width: none !important;
      max-height: none !important;
      object-fit: contain !important;
      object-position: center center !important;
      padding: 0.08rem !important;
      transform: none !important;
      filter: brightness(1.08) contrast(1.06) saturate(1.08) drop-shadow(0 0.45rem 0.55rem rgba(0,0,0,0.38)) !important;
      transition: none !important;
      animation: none !important;
    }

    body #loadoutGrid .loadout-card .card-body {
      grid-column: 2 !important;
      grid-row: 1 !important;
      min-width: 0 !important;
      padding-right: 0 !important;
      align-self: start !important;
    }

    body #loadoutGrid .loadout-card .rank-badge,
    body .loadout-grid .loadout-card .rank-badge,
    body .tier-group .loadout-card .rank-badge,
    body #loadoutGrid > .loadout-card:nth-child(-n + 3) .rank-badge {
      position: static !important;
      top: auto !important;
      right: auto !important;
      grid-column: 3 !important;
      grid-row: 1 !important;
      justify-self: end !important;
      align-self: start !important;
      width: 6.35rem !important;
      min-width: 6.35rem !important;
      height: 6.35rem !important;
      min-height: 6.35rem !important;
      border-radius: 0.48rem !important;
      font-size: 2.55rem !important;
    }

    body #loadoutGrid .loadout-card .card-details,
    body #loadoutGrid .loadout-card .meta-card-details {
      grid-column: 1 / -1 !important;
      grid-row: 2 !important;
      width: 100% !important;
    }

    body #loadoutGrid .loadout-card .card-footer {
      grid-column: 1 / -1 !important;
      grid-row: 3 !important;
      display: flex !important;
      align-items: center !important;
      gap: 0.72rem !important;
      margin: 0 !important;
      padding-top: 0.92rem !important;
      border-top: 1px solid rgba(255,255,255,0.075) !important;
    }

    body #loadoutGrid .loadout-card .weapon-name {
      font-size: 2.35rem !important;
      line-height: 0.95 !important;
      letter-spacing: 0 !important;
      overflow-wrap: anywhere !important;
    }

    body #loadoutGrid .loadout-card.tier-card-a {
      --tier-card-color: #35d7ff;
      --tier-card-rgb: 53, 215, 255;
    }

    body #loadoutGrid .loadout-card.tier-card-b {
      --tier-card-color: #ffcf4a;
      --tier-card-rgb: 255, 207, 74;
    }

    body #loadoutGrid .loadout-card.tier-card-c {
      --tier-card-color: #b08cff;
      --tier-card-rgb: 176, 140, 255;
    }

    body #loadoutGrid .loadout-card.tier-card-d {
      --tier-card-color: #ff6f91;
      --tier-card-rgb: 255, 111, 145;
    }

    @media (max-width: 980px) {
      body #loadoutGrid .loadout-card {
        grid-template-columns: minmax(10.5rem, 13rem) minmax(0, 1fr) auto !important;
      }

      body #loadoutGrid .loadout-card .weapon-name {
        font-size: 2.05rem !important;
      }
    }

    @media (max-width: 760px) {
      body #loadoutGrid .loadout-card {
        grid-template-columns: minmax(0, 1fr) !important;
        grid-template-rows: auto auto auto auto !important;
        height: auto !important;
        block-size: auto !important;
        min-height: max-content !important;
        max-height: none !important;
        aspect-ratio: auto !important;
        overflow: hidden !important;
        padding: 0.95rem !important;
      }

      body #loadoutGrid .loadout-card .weapon-art,
      body .loadout-grid .loadout-card .weapon-art,
      body #loadoutGrid > .loadout-card.tier-absolute-meta .weapon-art {
        grid-column: 1 !important;
        grid-row: 1 !important;
        width: 100% !important;
        height: 8.2rem !important;
        padding-right: 5.25rem !important;
      }

      body #loadoutGrid .loadout-card .card-body {
        grid-column: 1 !important;
        grid-row: 2 !important;
      }

      body #loadoutGrid .loadout-card .card-details,
      body #loadoutGrid .loadout-card .meta-card-details {
        grid-column: 1 !important;
        grid-row: 3 !important;
      }

      body #loadoutGrid .loadout-card .card-footer {
        grid-column: 1 !important;
        grid-row: 4 !important;
      }

      body #loadoutGrid .loadout-card .rank-badge,
      body .loadout-grid .loadout-card .rank-badge,
      body .tier-group .loadout-card .rank-badge,
      body #loadoutGrid > .loadout-card:nth-child(-n + 3) .rank-badge {
        position: absolute !important;
        top: 1rem !important;
        right: 1rem !important;
        width: 4.75rem !important;
        min-width: 4.75rem !important;
        height: 4.75rem !important;
        min-height: 4.75rem !important;
        font-size: 2rem !important;
      }

      body #loadoutGrid .loadout-card .weapon-name {
        font-size: 1.9rem !important;
      }
    }
  `;

  function installStyle() {
    let style = document.getElementById(STYLE_ID);
    if (!style) {
      style = document.createElement("style");
      style.id = STYLE_ID;
      document.head.append(style);
    }
    style.textContent = css;
    window.__loadoutCardFinalizerReady = true;
    window.__loadoutCardPolishReady = true;
  }

  function setExpanded(card, expanded) {
    const button = card && card.querySelector(".expand-button");
    if (!card || !button) return;
    const label = button.querySelector("span");
    card.classList.toggle("expanded", expanded);
    button.setAttribute("aria-expanded", expanded ? "true" : "false");
    if (label) label.textContent = expanded ? "Schliessen" : "Details";
  }

  function bindDetailsFallback() {
    if (window.__loadoutCardFinalizerClickReady) return;
    window.__loadoutCardFinalizerClickReady = true;
    document.addEventListener("click", (event) => {
      const button = event.target.closest("#loadoutGrid .expand-button");
      if (!button) return;
      const card = button.closest(".loadout-card");
      const intended = !(card?.classList.contains("expanded") || button.getAttribute("aria-expanded") === "true");
      window.setTimeout(() => setExpanded(card, intended), 70);
    }, true);
  }

  function init() {
    installStyle();
    bindDetailsFallback();
  }

  if (document.readyState === "loading") document.addEventListener("DOMContentLoaded", init, { once: true });
  else init();
}());

/* source: loadout-image-fix.js */
(function () {
  const fixedImages = {
    "vx-compact": "https://imgs.callofduty.com/content/dam/atvi/callofduty/cod-touchui/blog/body/bo7/bo7-s04-announcement/BO7-SEASON-04-ANNOUNCEMENT-070.webp",
    vmp: "https://imgs.callofduty.com/content/dam/atvi/callofduty/cod-touchui/blog/body/bo7/season-06/BO7-SEASON-06-ANNOUNCEMENT-030.webp",
    "tr51-para": "https://imgs.callofduty.com/content/dam/atvi/callofduty/cod-touchui/blog/body/bo7/season-06/BO7-SEASON-06-ANNOUNCEMENT-033.webp",
    "an-94": "https://imgs.callofduty.com/content/dam/atvi/callofduty/cod-touchui/blog/body/bo7/bo7-s04-announcement/BO7-SEASON-04-ANNOUNCEMENT-075.webp",
    "executioner-s-duet": "https://imgs.callofduty.com/content/dam/atvi/callofduty/cod-touchui/blog/body/bo7/bo7-s04-announcement/BO7-SEASON-04-ANNOUNCEMENT-073.webp",
    "roc-20mm": "https://imgs.callofduty.com/content/dam/atvi/callofduty/cod-touchui/blog/body/bo7/season-06/BO7-SEASON-06-ANNOUNCEMENT-038.webp",
    "swordfish-a1": "https://imgs.callofduty.com/content/dam/atvi/callofduty/cod-touchui/guides/games/blackops7/weapons-matrix/weapons/BO7-S02-SWORDFISH-A1.webp",
    grimhawk: "https://imgs.callofduty.com/content/dam/atvi/callofduty/cod-touchui/blog/seasons/blackops7/season-04/weapons/Seasonal-LP_S4_weapons_grimhawk.webp",
  };
  const IMAGE_STYLE_ID = "loadout-image-reliability-style";
  const IMAGE_TIMEOUT = 9000;
  const watchedImages = new WeakSet();

  function slugify(value) {
    return String(value || "")
      .normalize("NFD")
      .replace(/[\u0300-\u036f]/g, "")
      .toLowerCase()
      .replace(/&/g, " and ")
      .replace(/[^a-z0-9]+/g, "-")
      .replace(/^-+|-+$/g, "");
  }

  function applyImageFixes(root = document) {
    root.querySelectorAll?.("#loadoutGrid .loadout-card").forEach((card) => {
      const name = card.querySelector(".weapon-name, h2, h3")?.textContent?.trim();
      const src = fixedImages[slugify(name)];
      const img = card.querySelector(".weapon-art img");
      if (!src || !img || img.getAttribute("src") === src) return;
      img.dataset.fallback = img.dataset.fallback || img.getAttribute("src") || "";
      img.src = src;
    });
    hardenWeaponImages(root);
  }

  function installImageStateStyles() {
    if (document.getElementById(IMAGE_STYLE_ID)) return;
    const style = document.createElement("style");
    style.id = IMAGE_STYLE_ID;
    style.textContent = `
      body #loadoutGrid .weapon-art {
        position: relative !important;
        isolation: isolate !important;
        min-height: 4.2rem !important;
      }

      body #loadoutGrid .weapon-art::before {
        content: "" !important;
        position: absolute !important;
        inset: 0 !important;
        z-index: 0 !important;
        opacity: 0 !important;
        background:
          linear-gradient(90deg, transparent, rgba(255,255,255,0.06), transparent),
          rgba(5,8,12,0.72) !important;
        background-size: 210% 100%, 100% 100% !important;
        transition: opacity 120ms ease !important;
        pointer-events: none !important;
      }

      body #loadoutGrid .weapon-art img {
        position: relative !important;
        z-index: 1 !important;
        opacity: 1 !important;
      }

      body #loadoutGrid .weapon-art.is-loading::before {
        opacity: 1 !important;
        animation: loadoutImageSweep 1.1s linear infinite !important;
      }

      body #loadoutGrid .weapon-art.is-loading img {
        opacity: 0.28 !important;
      }

      body #loadoutGrid .weapon-art.is-fallback {
        border-style: dashed !important;
      }

      body #loadoutGrid .weapon-art.is-fallback::after {
        content: "Fallback" !important;
        position: absolute !important;
        right: 0.38rem !important;
        bottom: 0.34rem !important;
        z-index: 2 !important;
        padding: 0.16rem 0.34rem !important;
        border: 1px solid rgba(var(--tier-card-rgb, 255,211,90), 0.32) !important;
        border-radius: 999px !important;
        background: rgba(3,5,8,0.78) !important;
        color: var(--tier-card-color, #ffd35a) !important;
        font-size: 0.56rem !important;
        font-weight: 950 !important;
        line-height: 1 !important;
        text-transform: uppercase !important;
      }

      @keyframes loadoutImageSweep {
        from { background-position: 180% 0, 0 0; }
        to { background-position: -80% 0, 0 0; }
      }

      @media (prefers-reduced-motion: reduce) {
        body #loadoutGrid .weapon-art.is-loading::before {
          animation: none !important;
        }
      }
    `;
    document.head.append(style);
  }

  function setImageState(img, state) {
    const art = img.closest(".weapon-art");
    if (!art) return;
    art.classList.toggle("is-loading", state === "loading");
    art.classList.toggle("is-loaded", state === "loaded");
    art.classList.toggle("is-fallback", state === "fallback");
    art.dataset.imageState = state;
  }

  function useFallback(img) {
    const fallback = img.dataset.fallback;
    if (!fallback || img.dataset.fallbackApplied === "true" || img.src === fallback) {
      if (fallback && img.src === fallback) img.dataset.fallbackApplied = "true";
      setImageState(img, "fallback");
      return;
    }
    img.dataset.fallbackApplied = "true";
    img.src = fallback;
    setImageState(img, "fallback");
  }

  function watchImage(img, index) {
    if (watchedImages.has(img)) return;
    watchedImages.add(img);
    if (index < 3) {
      img.loading = "eager";
      img.setAttribute("fetchpriority", "high");
    } else {
      img.loading = "lazy";
      img.removeAttribute("fetchpriority");
    }
    img.decoding = "async";
    const done = () => {
      window.clearTimeout(Number(img.dataset.loadoutImageTimer || 0));
      const fallback = img.dataset.fallback || "";
      const fallbackLoaded = fallback && (img.src === fallback || img.currentSrc === fallback);
      setImageState(img, img.dataset.fallbackApplied === "true" || fallbackLoaded ? "fallback" : "loaded");
    };
    const fail = () => {
      window.clearTimeout(Number(img.dataset.loadoutImageTimer || 0));
      useFallback(img);
    };
    img.addEventListener("load", done);
    img.addEventListener("error", fail);
    if (img.complete) {
      if (img.naturalWidth > 0) done();
      else fail();
      return;
    }
    setImageState(img, "loading");
    img.dataset.loadoutImageTimer = String(window.setTimeout(() => {
      if (!img.complete || img.naturalWidth < 8) useFallback(img);
    }, IMAGE_TIMEOUT));
  }

  function hardenWeaponImages(root = document) {
    installImageStateStyles();
    root.querySelectorAll?.("#loadoutGrid .weapon-art img").forEach(watchImage);
  }

  const minimalTopCss = `
    body .today-strip,
    body .commercial-disclosure,
    body .ad-disclosure,
    body .affiliate-disclosure {
      display: none !important;
    }

    body .site-header {
      display: grid !important;
      grid-template-columns: auto minmax(0, 1fr) auto !important;
      align-items: center !important;
      gap: 0.55rem !important;
      min-height: 0 !important;
      padding: 0.55rem clamp(0.72rem, 2vw, 1rem) !important;
      background: #05080b !important;
      box-shadow: inset 0 -1px 0 rgba(255, 255, 255, 0.08) !important;
      backdrop-filter: none !important;
    }

    body .site-header .brand {
      min-width: 0 !important;
    }

    body .top-nav {
      display: grid !important;
      grid-template-columns: repeat(auto-fit, minmax(4.8rem, 1fr)) !important;
      align-items: center !important;
      justify-self: center !important;
      width: min(31rem, 100%) !important;
      min-width: 0 !important;
      min-height: 0 !important;
      gap: 0.2rem !important;
      padding: 0.18rem !important;
      border-radius: 0.62rem !important;
      background: #090f16 !important;
    }

    body .site-header .top-nav a {
      min-width: 0 !important;
      min-height: 2rem !important;
      padding: 0.42rem 0.52rem !important;
      text-align: center !important;
      white-space: nowrap !important;
    }

    body .site-header .site-actions {
      justify-self: end !important;
      width: auto !important;
      min-width: 0 !important;
    }

    body .site-header .language-switch {
      width: auto !important;
      min-width: 0 !important;
    }

    body #autoUpdateStatus {
      display: none !important;
    }

    body .tier-first .section-heading.minimal-hero {
      display: grid !important;
      grid-template-columns: 1fr !important;
      grid-template-areas: none !important;
      gap: 0.35rem !important;
      max-width: min(62rem, 100%) !important;
      margin: 0 0 0.65rem !important;
      padding: 0 !important;
      border: 0 !important;
      border-left: 0 !important;
      border-radius: 0 !important;
      background: transparent !important;
      box-shadow: none !important;
    }

    body .tier-first .section-heading.minimal-hero::before,
    body .tier-first .section-heading.minimal-hero::after {
      display: none !important;
      content: none !important;
    }

    body .minimal-hero-meta {
      display: flex !important;
      flex-wrap: wrap !important;
      align-items: center !important;
      gap: 0.42rem 0.65rem !important;
    }

    body .tier-first .minimal-hero .eyebrow,
    body .tier-first .minimal-hero .updated-note {
      grid-area: unset !important;
      margin: 0 !important;
      white-space: normal !important;
    }

    body .tier-first .minimal-hero .eyebrow {
      width: fit-content !important;
      padding: 0.2rem 0.52rem !important;
      border: 1px solid rgba(185, 255, 61, 0.28) !important;
      border-radius: 999px !important;
      background: #101821 !important;
      color: #b9ff3d !important;
      font-size: 0.62rem !important;
      letter-spacing: 0 !important;
      line-height: 1 !important;
    }

    body .tier-first .minimal-hero .updated-note {
      justify-self: start !important;
      min-height: 0 !important;
      padding: 0 !important;
      border: 0 !important;
      background: transparent !important;
      color: #8e98a8 !important;
      font-size: 0.76rem !important;
    }

    body .tier-first .minimal-hero h1 {
      grid-area: unset !important;
      max-width: 56rem !important;
      margin: 0 !important;
      color: #f4f7fb !important;
      font-size: clamp(2.05rem, 4.6vw, 3.65rem) !important;
      line-height: 0.92 !important;
      letter-spacing: -0.04em !important;
      text-shadow: none !important;
    }

    body .tier-first .minimal-hero #tierDescription {
      display: none !important;
    }

    body .minimal-control-panel {
      display: grid !important;
      grid-template-columns: minmax(13rem, 1fr) minmax(16rem, 1.2fr) minmax(9rem, 0.72fr) !important;
      align-items: end !important;
      gap: 0.55rem !important;
      max-width: min(78rem, 100%) !important;
      margin: 0 0 0.72rem !important;
      padding: 0.58rem !important;
      border: 1px solid rgba(255, 255, 255, 0.1) !important;
      border-radius: 0.72rem !important;
      background: #080d13 !important;
      box-shadow: none !important;
    }

    body .minimal-control-group {
      display: grid !important;
      min-width: 0 !important;
      gap: 0.32rem !important;
    }

    body .minimal-control-panel .loadout-subnav-row {
      display: contents !important;
      margin: 0 !important;
      padding: 0 !important;
      border: 0 !important;
      background: transparent !important;
      box-shadow: none !important;
    }

    body .minimal-control-panel .minimal-control-group[hidden],
    body .minimal-control-panel [hidden] {
      display: none !important;
    }

    body .minimal-control-label {
      color: #788393 !important;
      font-size: 0.66rem !important;
      font-weight: 950 !important;
      letter-spacing: 0.08em !important;
      line-height: 1 !important;
      text-transform: uppercase !important;
    }

    body .minimal-control-panel .mode-switch,
    body .minimal-control-panel .content-tabs {
      display: grid !important;
      width: 100% !important;
      max-width: none !important;
      min-width: 0 !important;
      min-height: 0 !important;
      margin: 0 !important;
      padding: 0 !important;
      border: 0 !important;
      border-radius: 0 !important;
      background: transparent !important;
      box-shadow: none !important;
      transform: none !important;
      vertical-align: baseline !important;
      gap: 0.34rem !important;
    }

    body .minimal-control-panel .primary-mode-switch,
    body .minimal-control-panel .secondary-mode-switch {
      grid-template-columns: repeat(3, minmax(0, 1fr)) !important;
    }

    body .minimal-control-panel .content-tabs {
      grid-template-columns: repeat(2, minmax(0, 1fr)) !important;
    }

    body .minimal-control-panel .content-tabs[hidden] {
      display: none !important;
    }

    body .minimal-control-panel .secondary-mode-switch .camo-mode-button {
      display: inline-flex !important;
    }

    body .minimal-control-panel .mode-button,
    body .minimal-control-panel .content-tab {
      display: inline-flex !important;
      min-width: 0 !important;
      min-height: 2.45rem !important;
      align-items: center !important;
      justify-content: center !important;
      border: 1px solid rgba(255, 255, 255, 0.09) !important;
      border-radius: 0.5rem !important;
      background: #111924 !important;
      color: #dce5ef !important;
      font-size: 0.88rem !important;
      font-weight: 900 !important;
      letter-spacing: 0 !important;
      line-height: 1 !important;
      padding: 0.52rem 0.58rem !important;
      text-align: center !important;
      white-space: nowrap !important;
      box-shadow: none !important;
    }

    body .minimal-control-panel .mode-button::before,
    body .minimal-control-panel .primary-mode-switch .mode-button::before {
      display: none !important;
      content: none !important;
    }

    body .minimal-control-panel .mode-button.active,
    body .minimal-control-panel .content-tab.active {
      border-color: transparent !important;
      background: #b9ff3d !important;
      color: #061008 !important;
      box-shadow: none !important;
      text-shadow: none !important;
    }

    @media (max-width: 1040px) {
      body .minimal-control-panel {
        grid-template-columns: 1fr !important;
      }
    }

    @media (max-width: 620px) {
      body .site-header {
        grid-template-columns: minmax(0, 1fr) auto !important;
        gap: 0.42rem !important;
        padding: 0.5rem 0.65rem !important;
      }

      body .site-header .brand {
        grid-column: 1 !important;
      }

      body .site-header .site-actions {
        grid-column: 2 !important;
      }

      body .site-header .top-nav {
        grid-column: 1 / -1 !important;
        grid-row: 2 !important;
        width: 100% !important;
      }

      body .site-header .top-nav a {
        min-height: 1.95rem !important;
        padding: 0.38rem 0.28rem !important;
        font-size: 0.78rem !important;
      }

      body .minimal-control-panel {
        gap: 0.45rem !important;
        margin-bottom: 0.55rem !important;
        padding: 0.5rem !important;
      }

      body .minimal-control-panel .primary-mode-switch,
      body .minimal-control-panel .secondary-mode-switch {
        grid-template-columns: repeat(3, minmax(0, 1fr)) !important;
      }

      body .minimal-control-panel .content-tabs {
        grid-template-columns: repeat(2, minmax(0, 1fr)) !important;
      }

      body .minimal-control-panel .mode-button,
      body .minimal-control-panel .content-tab {
        min-height: 2.12rem !important;
        padding: 0.42rem 0.22rem !important;
        font-size: 0.78rem !important;
      }

      body .tier-first .minimal-hero .updated-note {
        display: none !important;
      }

      body .tier-first .minimal-hero h1 {
        font-size: clamp(1.65rem, 8vw, 2.35rem) !important;
      }
    }
  `;

  function installMinimalTopStyle() {
    let style = document.getElementById("minimal-top-final-20260606");
    if (!style) {
      style = document.createElement("style");
      style.id = "minimal-top-final-20260606";
      document.head.append(style);
    }
    style.textContent = minimalTopCss;
    document.head.append(style);
  }

  function setButtonLabel(selector, label) {
    const button = document.querySelector(selector);
    if (button) button.textContent = label;
  }

  function createControlGroup(label, element, className = "") {
    if (!element) return null;
    const group = document.createElement("div");
    group.className = ["minimal-control-group", className].filter(Boolean).join(" ");
    group.hidden = Boolean(element.hidden);

    const groupLabel = document.createElement("span");
    groupLabel.className = "minimal-control-label";
    groupLabel.textContent = label;

    group.append(groupLabel, element);
    return group;
  }

  function reorderModes() {
    const switcher = document.querySelector(".primary-mode-switch");
    if (!switcher) return;
    ["updates", "mw4-info", "season4-info"].forEach((mode) => {
      const button = switcher.querySelector(`.mode-button[data-mode="${mode}"]`);
      if (button) switcher.appendChild(button);
    });
  }

  function applyMinimalTop() {
    installMinimalTopStyle();
    const section = document.querySelector(".tier-first");
    if (!section) return;

    section.querySelectorAll(".today-strip, #autoUpdateStatus, .commercial-disclosure, .ad-disclosure, .affiliate-disclosure").forEach((element) => element.remove());

    const heading = section.querySelector(".section-heading");
    if (heading) {
      heading.classList.add("minimal-hero");
      const eyebrow = heading.querySelector(".eyebrow");
      const updatedNote = heading.querySelector(".updated-note");
      const description = heading.querySelector("#tierDescription");
      let heroMeta = heading.querySelector(".minimal-hero-meta");

      if (!heroMeta) {
        heroMeta = document.createElement("div");
        heroMeta.className = "minimal-hero-meta";
        heading.prepend(heroMeta);
      }

      if (eyebrow) {
        eyebrow.textContent = "Loadout Lab";
        if (eyebrow.parentElement !== heroMeta) heroMeta.appendChild(eyebrow);
      }
      if (updatedNote && updatedNote.parentElement !== heroMeta) heroMeta.appendChild(updatedNote);
      if (description) description.textContent = "Schnelle Meta. Klare Loadouts.";
    }

    reorderModes();
    setButtonLabel('.primary-mode-switch .mode-button[data-mode="updates"]', "Updates");
    setButtonLabel('.primary-mode-switch .mode-button[data-mode="mw4-info"]', "MW4");
    setButtonLabel('.primary-mode-switch .mode-button[data-mode="season4-info"]', "Season 6");
    setButtonLabel('.secondary-mode-switch .mode-button[data-mode="warzone-ranked"]', "Warzone");
    setButtonLabel('.secondary-mode-switch .mode-button[data-mode="bo7-ranked"]', "BO7");
    setButtonLabel('.secondary-mode-switch .mode-button[data-mode="camos"]', "Camos");
    setButtonLabel('.content-tabs .content-tab[data-tab="weapons"]', "Waffen");
    setButtonLabel('.content-tabs .content-tab[data-tab="maps"]', "Maps");

    const primarySwitch = document.querySelector(".primary-mode-switch");
    const secondarySwitch = document.querySelector(".secondary-mode-switch");
    const contentTabs = document.querySelector("#contentTabs");
    if (!primarySwitch && !secondarySwitch && !contentTabs) return;

    let panel = Array.from(section.children).find((child) => child.classList?.contains("minimal-control-panel"));
    if (!panel) {
      panel = document.createElement("div");
      panel.className = "minimal-control-panel";
      panel.setAttribute("aria-label", "Schnellauswahl");

      const anchor = primarySwitch || secondarySwitch || contentTabs || section.querySelector(".controls");
      if (anchor?.parentElement === section) section.insertBefore(panel, anchor);
      else if (heading) heading.after(panel);
      else section.prepend(panel);
    }

    panel.replaceChildren(
      ...[
        createControlGroup("Info", primarySwitch),
        createControlGroup("Meta", secondarySwitch),
        createControlGroup("Ansicht", contentTabs, "compact"),
      ].filter(Boolean)
    );

    document.querySelectorAll(".loadout-subnav-row").forEach((row) => {
      const parent = row.parentElement;
      const wrappedSecondary = row.querySelector(".secondary-mode-switch");
      const wrappedTabs = row.querySelector("#contentTabs");
      if (parent) {
        if (wrappedSecondary) parent.insertBefore(wrappedSecondary, row);
        if (wrappedTabs) parent.insertBefore(wrappedTabs, row);
      }
      row.remove();
    });
  }

  function init() {
    applyMinimalTop();
    applyImageFixes();
    window.setTimeout(applyImageFixes, 250);
    window.setTimeout(applyImageFixes, 1000);
    const grid = document.getElementById("loadoutGrid");
    if (grid) {
      new MutationObserver(() => applyImageFixes(grid)).observe(grid, { childList: true, subtree: true });
    }
    window.__loadoutImageFixReady = true;
  }

  if (document.readyState === "loading") document.addEventListener("DOMContentLoaded", init, { once: true });
  else init();
}());

/* source: loadout-mobile-details-polish.js */
(function () {
  const STYLE_ID = "loadout-mobile-details-polish-style";
  const FILTER_SHELL_CLASS = "mobile-filter-shell";
  const FILTER_TOOLBAR_CLASS = "mobile-filter-toolbar";

  const css = `
    body .${FILTER_SHELL_CLASS} {
      max-width: min(82rem, 100%) !important;
      margin: 0 0 0.85rem !important;
    }

    body .mobile-filter-toggle {
      display: none !important;
    }

    body #filterToolbar.${FILTER_TOOLBAR_CLASS} {
      display: flex !important;
      flex-wrap: wrap !important;
      align-items: center !important;
      gap: 0.45rem !important;
      margin: 0 !important;
      padding: 0 !important;
      border: 0 !important;
      background: transparent !important;
      box-shadow: none !important;
    }

    body #filterToolbar.${FILTER_TOOLBAR_CLASS} .filter-button {
      min-height: 2.25rem !important;
      padding: 0.48rem 0.72rem !important;
      border-radius: 999px !important;
      border: 1px solid rgba(255, 255, 255, 0.1) !important;
      background: #101821 !important;
      color: #dce6f2 !important;
      font-size: 0.82rem !important;
      font-weight: 900 !important;
      letter-spacing: 0 !important;
      line-height: 1 !important;
      box-shadow: none !important;
      transition: border-color 160ms ease, background 160ms ease, color 160ms ease, transform 160ms ease !important;
    }

    body #filterToolbar.${FILTER_TOOLBAR_CLASS} .filter-button:hover {
      border-color: rgba(185, 255, 61, 0.35) !important;
      transform: translateY(-1px) !important;
    }

    body #filterToolbar.${FILTER_TOOLBAR_CLASS} .filter-button.active {
      border-color: transparent !important;
      background: linear-gradient(135deg, #b9ff3d, #24e574) !important;
      color: #061008 !important;
      box-shadow: 0 0.7rem 1.55rem rgba(86, 255, 86, 0.16) !important;
      text-shadow: none !important;
    }

    body #loadoutGrid .loadout-card.expanded .meta-card-details,
    body #loadoutGrid .loadout-card.expanded .card-details {
      padding: 0.95rem !important;
      border: 1px solid rgba(var(--tier-card-rgb, 185, 255, 61), 0.2) !important;
      border-radius: 0.72rem !important;
      background: linear-gradient(180deg, rgba(12, 18, 26, 0.96), rgba(6, 10, 15, 0.94)) !important;
      box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.055), 0 1rem 2rem rgba(0, 0, 0, 0.28) !important;
      overflow: visible !important;
    }

    body #loadoutGrid .loadout-card.expanded .expand-button {
      border-color: rgba(var(--tier-card-rgb, 185, 255, 61), 0.42) !important;
      background: rgba(var(--tier-card-rgb, 185, 255, 61), 0.14) !important;
      color: #ffffff !important;
    }

    body #loadoutGrid .detail-source-row {
      display: grid !important;
      grid-template-columns: repeat(auto-fit, minmax(8.8rem, 1fr)) !important;
      gap: 0.48rem !important;
      margin: 0 0 0.82rem !important;
      padding: 0 !important;
      border: 0 !important;
      background: transparent !important;
    }

    body #loadoutGrid .detail-source-chip {
      display: grid !important;
      grid-template-columns: 1fr !important;
      align-content: center !important;
      gap: 0.18rem !important;
      min-height: 3.1rem !important;
      padding: 0.55rem 0.65rem !important;
      border: 1px solid rgba(var(--tier-card-rgb, 185, 255, 61), 0.17) !important;
      border-left: 3px solid var(--tier-card-color, #b9ff3d) !important;
      border-radius: 0.5rem !important;
      background: rgba(255, 255, 255, 0.035) !important;
      white-space: normal !important;
    }

    body #loadoutGrid .detail-source-chip span {
      color: #7d8896 !important;
      font-size: 0.63rem !important;
      font-weight: 950 !important;
      letter-spacing: 0.06em !important;
      line-height: 1 !important;
      text-transform: uppercase !important;
    }

    body #loadoutGrid .detail-source-chip strong,
    body #loadoutGrid .detail-source-chip a {
      color: #f5f8fc !important;
      font-size: 0.88rem !important;
      font-weight: 950 !important;
      line-height: 1.1 !important;
      text-decoration: none !important;
    }

    body #loadoutGrid .attachment-columns,
    body #loadoutGrid .details-columns,
    body #loadoutGrid .loadout-details-grid {
      display: grid !important;
      grid-template-columns: repeat(auto-fit, minmax(15.5rem, 1fr)) !important;
      gap: 0.72rem !important;
      align-items: stretch !important;
    }

    body #loadoutGrid .attachments-panel,
    body #loadoutGrid .perks-panel,
    body #loadoutGrid .detail-panel,
    body #loadoutGrid .loadout-panel,
    body #loadoutGrid .build-code-box {
      border: 1px solid rgba(255, 255, 255, 0.085) !important;
      border-radius: 0.62rem !important;
      background: rgba(5, 9, 14, 0.62) !important;
      box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.04) !important;
    }

    body #loadoutGrid .attachments-panel,
    body #loadoutGrid .perks-panel,
    body #loadoutGrid .detail-panel,
    body #loadoutGrid .loadout-panel {
      padding: 0.74rem !important;
    }

    body #loadoutGrid .detail-panel-title,
    body #loadoutGrid .attachments-panel h4,
    body #loadoutGrid .perks-panel h4 {
      display: flex !important;
      align-items: center !important;
      justify-content: space-between !important;
      gap: 0.5rem !important;
      margin: 0 0 0.58rem !important;
      color: #f4f7fb !important;
      font-size: 0.78rem !important;
      font-weight: 950 !important;
      letter-spacing: 0.04em !important;
      line-height: 1 !important;
      text-transform: uppercase !important;
    }

    body #loadoutGrid .premium-attachment-list,
    body #loadoutGrid .attachment-list,
    body #loadoutGrid .perk-list {
      display: grid !important;
      gap: 0.42rem !important;
      margin: 0 !important;
      padding: 0 !important;
      list-style: none !important;
    }

    body #loadoutGrid .loadout-slot,
    body #loadoutGrid .attachment-item,
    body #loadoutGrid .perk-list li {
      min-height: 2.8rem !important;
      padding: 0.55rem 0.64rem !important;
      border: 1px solid rgba(255, 255, 255, 0.075) !important;
      border-radius: 0.48rem !important;
      background: rgba(255, 255, 255, 0.035) !important;
      color: #dce6f2 !important;
    }

    body #loadoutGrid .loadout-slot strong,
    body #loadoutGrid .attachment-item strong,
    body #loadoutGrid .perk-list strong {
      color: var(--tier-card-color, #b9ff3d) !important;
      font-weight: 950 !important;
    }

    body #loadoutGrid .loadout-slot span,
    body #loadoutGrid .attachment-item span,
    body #loadoutGrid .perk-list span {
      color: #b8c2cf !important;
      font-size: 0.82rem !important;
      line-height: 1.25 !important;
    }

    body #loadoutGrid .build-code-box {
      margin-top: 0.72rem !important;
      padding: 0.72rem !important;
    }

    body #loadoutGrid .build-code-box.no-code {
      background: rgba(255, 255, 255, 0.025) !important;
    }

    @media (max-width: 760px) {
      body .${FILTER_SHELL_CLASS} {
        position: sticky !important;
        top: 0.45rem !important;
        z-index: 18 !important;
        margin: 0 0 0.72rem !important;
        padding: 0.52rem !important;
        border: 1px solid rgba(255, 255, 255, 0.1) !important;
        border-radius: 0.72rem !important;
        background: rgba(5, 8, 12, 0.94) !important;
        box-shadow: 0 0.9rem 1.8rem rgba(0, 0, 0, 0.34) !important;
        backdrop-filter: blur(10px) !important;
      }

      body .mobile-filter-toggle {
        display: grid !important;
        grid-template-columns: auto minmax(0, 1fr) auto !important;
        align-items: center !important;
        gap: 0.55rem !important;
        width: 100% !important;
        min-height: 2.6rem !important;
        padding: 0.48rem 0.62rem !important;
        border: 1px solid rgba(185, 255, 61, 0.22) !important;
        border-radius: 0.56rem !important;
        background: #101821 !important;
        color: #f5f8fc !important;
        font-weight: 950 !important;
        letter-spacing: 0 !important;
        line-height: 1 !important;
      }

      body .mobile-filter-toggle-label {
        color: #b9ff3d !important;
        font-size: 0.78rem !important;
        text-transform: uppercase !important;
      }

      body .mobile-filter-toggle-active {
        min-width: 0 !important;
        overflow: hidden !important;
        color: #f5f8fc !important;
        font-size: 0.92rem !important;
        text-overflow: ellipsis !important;
        white-space: nowrap !important;
      }

      body .mobile-filter-toggle-count {
        color: #8f9baa !important;
        font-size: 0.72rem !important;
        white-space: nowrap !important;
      }

      body .mobile-filter-toggle-icon {
        display: inline-grid !important;
        place-items: center !important;
        width: 1.45rem !important;
        height: 1.45rem !important;
        border-radius: 999px !important;
        background: rgba(185, 255, 61, 0.1) !important;
        color: #b9ff3d !important;
        transition: transform 160ms ease !important;
      }

      body .${FILTER_SHELL_CLASS}.filters-open .mobile-filter-toggle-icon {
        transform: rotate(180deg) !important;
      }

      body #filterToolbar.${FILTER_TOOLBAR_CLASS} {
        display: grid !important;
        grid-template-columns: repeat(2, minmax(0, 1fr)) !important;
        gap: 0.42rem !important;
        max-height: 0 !important;
        margin-top: 0 !important;
        overflow: hidden !important;
        opacity: 0 !important;
        pointer-events: none !important;
        transition: max-height 220ms ease, margin-top 220ms ease, opacity 160ms ease !important;
      }

      body .${FILTER_SHELL_CLASS}.filters-open #filterToolbar.${FILTER_TOOLBAR_CLASS} {
        max-height: 32rem !important;
        margin-top: 0.5rem !important;
        opacity: 1 !important;
        pointer-events: auto !important;
      }

      body #filterToolbar.${FILTER_TOOLBAR_CLASS} .filter-button {
        width: 100% !important;
        min-height: 2.18rem !important;
        padding: 0.42rem 0.34rem !important;
        font-size: 0.75rem !important;
        text-align: center !important;
        white-space: nowrap !important;
      }

      body #loadoutGrid .loadout-card.expanded .meta-card-details,
      body #loadoutGrid .loadout-card.expanded .card-details {
        padding: 0.72rem !important;
      }

      body #loadoutGrid .detail-source-row {
        grid-template-columns: repeat(2, minmax(0, 1fr)) !important;
        gap: 0.42rem !important;
      }

      body #loadoutGrid .detail-source-chip {
        min-height: 2.95rem !important;
        padding: 0.5rem !important;
      }

      body #loadoutGrid .detail-source-chip strong,
      body #loadoutGrid .detail-source-chip a {
        font-size: 0.78rem !important;
      }

      body #loadoutGrid .attachment-columns,
      body #loadoutGrid .details-columns,
      body #loadoutGrid .loadout-details-grid {
        grid-template-columns: 1fr !important;
      }
    }
  `;

  function text(value) {
    return String(value || "").replace(/\s+/g, " ").trim();
  }

  function installStyle() {
    let style = document.getElementById(STYLE_ID);
    if (!style) {
      style = document.createElement("style");
      style.id = STYLE_ID;
      document.head.append(style);
    }
    style.textContent = css;
  }

  function toolbar() {
    return document.getElementById("filterToolbar") || document.querySelector(".filter-toolbar") || document.querySelector(".filter-button[data-smart-filter]")?.parentElement || null;
  }

  function filterLabel(button) {
    const raw = text(button?.textContent);
    if (!raw) return "Alle";
    return raw.replace("Aufsätzen", "Aufsaetzen");
  }

  function countLabel() {
    const count = document.getElementById("resultCount");
    const raw = text(count?.dataset?.filterCount || count?.textContent || "");
    const match = raw.match(/^(\d+)\s+(?:von|of)\s+(\d+)/i);
    if (match) return `${match[1]}/${match[2]}`;
    return "";
  }

  function updateToggle(shell, bar) {
    const active = bar.querySelector(".filter-button.active") || bar.querySelector(".filter-button");
    const toggle = shell.querySelector(".mobile-filter-toggle");
    if (!toggle) return;
    toggle.querySelector(".mobile-filter-toggle-active").textContent = filterLabel(active);
    toggle.querySelector(".mobile-filter-toggle-count").textContent = countLabel();
    const open = shell.classList.contains("filters-open");
    const english = localStorage.getItem("loadoutLabLang") === "en" || document.documentElement.lang === "en";
    toggle.setAttribute("aria-expanded", open ? "true" : "false");
    toggle.setAttribute("aria-controls", bar.id);
    toggle.setAttribute("aria-label", english
      ? `${open ? "Close" : "Open"} meta filters`
      : `Meta-Filter ${open ? "schließen" : "öffnen"}`);
  }

  function ensureMobileFilters() {
    const bar = toolbar();
    if (!bar || bar.dataset.mobileDetailsPolished === "true") {
      const shell = bar?.closest(`.${FILTER_SHELL_CLASS}`);
      if (shell) updateToggle(shell, bar);
      return;
    }

    bar.dataset.mobileDetailsPolished = "true";
    if (!bar.id) bar.id = "filterToolbar";
    bar.classList.add(FILTER_TOOLBAR_CLASS);

    const parent = bar.parentElement;
    if (!parent) return;

    let shell = bar.closest(`.${FILTER_SHELL_CLASS}`);
    if (!shell) {
      shell = document.createElement("section");
      shell.className = FILTER_SHELL_CLASS;
      shell.setAttribute("aria-label", "Meta Filter");
      parent.insertBefore(shell, bar);
      shell.append(bar);
    }

    let toggle = shell.querySelector(".mobile-filter-toggle");
    if (!toggle) {
      toggle = document.createElement("button");
      toggle.type = "button";
      toggle.className = "mobile-filter-toggle";
      toggle.innerHTML = `
        <span class="mobile-filter-toggle-label">Filter</span>
        <span class="mobile-filter-toggle-active">Alle</span>
        <span class="mobile-filter-toggle-count"></span>
        <span class="mobile-filter-toggle-icon" aria-hidden="true">v</span>
      `;
      shell.insertBefore(toggle, bar);
      toggle.addEventListener("click", () => {
        shell.classList.toggle("filters-open");
        updateToggle(shell, bar);
      });
    }

    updateToggle(shell, bar);
  }

  function markDetails() {
    document.querySelectorAll("#loadoutGrid .loadout-card").forEach((card) => {
      const panel = card.querySelector(".meta-card-details, .card-details");
      if (!panel) return;
      card.classList.add("details-polished-card");
      panel.classList.add("details-polished-panel");
    });
  }

  function refresh() {
    installStyle();
    ensureMobileFilters();
    markDetails();
    window.__loadoutMobileDetailsPolishReady = true;
  }

  function init() {
    refresh();
    window.setTimeout(refresh, 250);
    window.setTimeout(refresh, 900);
    window.setTimeout(refresh, 1800);
    document.addEventListener("click", (event) => {
      if (event.target.closest("#filterToolbar .filter-button")) {
        const shell = document.querySelector(`.${FILTER_SHELL_CLASS}`);
        const bar = toolbar();
        if (shell && bar) {
          window.setTimeout(() => {
            if (window.matchMedia("(max-width: 760px)").matches) shell.classList.remove("filters-open");
            updateToggle(shell, bar);
          }, 180);
        }
      }
      window.setTimeout(refresh, 80);
      window.setTimeout(refresh, 360);
    }, true);

    window.addEventListener("loadoutlab:filter-change", () => {
      const shell = document.querySelector(`.${FILTER_SHELL_CLASS}`);
      const bar = toolbar();
      if (!shell || !bar) return;
      if (window.matchMedia("(max-width: 760px)").matches) shell.classList.remove("filters-open");
      updateToggle(shell, bar);
    });

    const observer = new MutationObserver(() => window.setTimeout(refresh, 80));
    observer.observe(document.body, { childList: true, subtree: true });
  }

  if (document.readyState === "loading") document.addEventListener("DOMContentLoaded", init, { once: true });
  else init();
}());

/* source: modern-refresh.js */
(function () {
  const brokenTextPattern = /(?:Ã.|Â.|â€|â‚|ðŸ|Ò[¤¼])/;
  const windows1252Bytes = new Map([
    ["€", 0x80], ["‚", 0x82], ["ƒ", 0x83], ["„", 0x84], ["…", 0x85],
    ["†", 0x86], ["‡", 0x87], ["ˆ", 0x88], ["‰", 0x89], ["Š", 0x8a],
    ["‹", 0x8b], ["Œ", 0x8c], ["Ž", 0x8e], ["‘", 0x91], ["’", 0x92],
    ["“", 0x93], ["”", 0x94], ["•", 0x95], ["–", 0x96], ["—", 0x97],
    ["˜", 0x98], ["™", 0x99], ["š", 0x9a], ["›", 0x9b], ["œ", 0x9c],
    ["ž", 0x9e], ["Ÿ", 0x9f],
  ]);
  const compactCardCss = `
    body #loadoutGrid .loadout-card {
      grid-template-columns: 10.75rem minmax(0, 1fr) 5.75rem !important;
      gap: 0.65rem 1rem !important;
      min-height: 0 !important;
      margin-bottom: 0.6rem !important;
      padding: 0.85rem 1rem 0.75rem !important;
    }
    body #loadoutGrid .loadout-card .weapon-art,
    body #loadoutGrid > .loadout-card.tier-absolute-meta .weapon-art {
      width: 100% !important;
      max-width: none !important;
      height: 6rem !important;
    }
    body #loadoutGrid .loadout-card .card-body {
      display: grid !important;
      grid-template-columns: auto minmax(0, 1fr) !important;
      align-items: end !important;
      gap: 0.35rem 0.65rem !important;
      min-height: 0 !important;
    }
    body #loadoutGrid .loadout-card .card-title-row { grid-column: 1 / -1 !important; }
    body #loadoutGrid .loadout-card .weapon-name {
      margin-top: 0.22rem !important;
      font-size: clamp(1.7rem, 2.35vw, 2.15rem) !important;
      line-height: 0.98 !important;
    }
    body #loadoutGrid .meta-score-pill { margin: 0 !important; }
    body #loadoutGrid .stat-row { align-self: center !important; margin: 0 !important; }
    body #loadoutGrid .loadout-card .rank-badge {
      width: 5.35rem !important;
      min-width: 5.35rem !important;
      height: 5.35rem !important;
      min-height: 5.35rem !important;
    }
    body #loadoutGrid .loadout-card .card-footer {
      min-height: 0 !important;
      gap: 0.5rem !important;
      padding-top: 0.65rem !important;
    }
    body #loadoutGrid .expand-button,
    body #loadoutGrid .weapon-share-button,
    body #loadoutGrid .weapon-compare-check {
      min-height: 2.4rem !important;
    }
    body #loadoutGrid .expand-button { min-width: 7rem !important; }

    body .updates-section .update-card img {
      width: 100% !important;
      height: 100% !important;
      min-height: 10.2rem !important;
      aspect-ratio: auto !important;
      object-fit: cover !important;
      background: #0b1016 !important;
    }
    body .updates-section .update-card-content {
      padding: 0.9rem 1rem 1rem !important;
    }
    body .updates-section .update-card h3 {
      font-size: clamp(1.45rem, 2vw, 1.9rem) !important;
      line-height: 1.05 !important;
    }
    body .updates-section .update-detail p {
      font-size: 0.95rem !important;
      line-height: 1.55 !important;
    }

    @media (max-width: 760px) {
      body #loadoutGrid .loadout-card {
        grid-template-columns: minmax(0, 1fr) !important;
        gap: 0.55rem !important;
        padding: 0.65rem !important;
      }
      body #loadoutGrid .loadout-card .weapon-art,
      body #loadoutGrid > .loadout-card.tier-absolute-meta .weapon-art {
        height: 7rem !important;
        padding-right: 4.5rem !important;
      }
      body #loadoutGrid .loadout-card .card-body {
        grid-template-columns: auto minmax(0, 1fr) !important;
        gap: 0.3rem 0.5rem !important;
      }
      body #loadoutGrid .loadout-card .weapon-name { font-size: 1.65rem !important; }
      body #loadoutGrid .loadout-card .rank-badge {
        top: 0.65rem !important;
        right: 0.65rem !important;
        width: 4.35rem !important;
        min-width: 4.35rem !important;
        height: 4.35rem !important;
        min-height: 4.35rem !important;
      }
      body #loadoutGrid .loadout-card .card-footer {
        display: grid !important;
        grid-template-columns: minmax(0, 1fr) auto minmax(0, 1.08fr) !important;
        align-items: stretch !important;
        gap: 0.38rem !important;
        padding-top: 0.6rem !important;
      }
      body #loadoutGrid .expand-button,
      body #loadoutGrid .weapon-share-button,
      body #loadoutGrid .weapon-compare-check {
        width: auto !important;
        min-width: 0 !important;
        min-height: 2.5rem !important;
        padding: 0.45rem 0.55rem !important;
        font-size: 0.72rem !important;
      }
      body #loadoutGrid .weapon-compare-check { justify-content: center !important; }
      body .updates-section .updates-hero {
        align-items: start !important;
        padding: 0.9rem !important;
      }
      body .updates-section .updates-hero h2 { font-size: 2rem !important; }
      body .updates-section .update-card img { height: 8rem !important; min-height: 8rem !important; }
    }
  `;

  function installCompactCardStyle() {
    let style = document.getElementById("modern-compact-card-style");
    if (!style) {
      style = document.createElement("style");
      style.id = "modern-compact-card-style";
      document.head.append(style);
    }
    style.textContent = compactCardCss;
  }

  function decodeUtf8Layer(value) {
    try {
      const bytes = Uint8Array.from(value, (character) => {
        const codePoint = character.codePointAt(0);
        return windows1252Bytes.get(character) ?? codePoint;
      });
      return new TextDecoder("utf-8", { fatal: true }).decode(bytes);
    } catch (_error) {
      return value;
    }
  }

  function cleanText(value) {
    let result = String(value || "");
    for (let pass = 0; pass < 3 && brokenTextPattern.test(result); pass += 1) {
      const decoded = decodeUtf8Layer(result);
      if (decoded === result) break;
      result = decoded;
    }
    return result
      .replaceAll("Schliessen", "Schließen")
      .replaceAll("auswaehlen", "auswählen")
      .replaceAll("Geprueft", "Geprüft")
      .replaceAll("oeffnen", "öffnen")
      .replaceAll("fuehrt", "führt");
  }

  function cleanNode(root) {
    const walker = document.createTreeWalker(root, NodeFilter.SHOW_TEXT);
    let node = walker.nextNode();
    while (node) {
      const next = cleanText(node.nodeValue);
      if (next !== node.nodeValue) node.nodeValue = next;
      node = walker.nextNode();
    }

    root.querySelectorAll?.("[aria-label], [title], [placeholder], img[alt]").forEach((element) => {
      ["aria-label", "title", "placeholder", "alt"].forEach((attribute) => {
        if (!element.hasAttribute(attribute)) return;
        const current = element.getAttribute(attribute);
        const next = cleanText(current);
        if (next !== current) element.setAttribute(attribute, next);
      });
    });
  }

  function init() {
    installCompactCardStyle();
    cleanNode(document.body);
    document.documentElement.classList.add("modern-refresh-ready");

    fetch("data/season-watch.json", { cache: "no-store" })
      .then((response) => response.ok ? response.json() : null)
      .then((data) => {
        if (!data?.season) return;
        const label = data.season.replace(/^Season 0/, "Season ");
        const updateSeasonButton = () => {
          const button = document.querySelector(".season-mode-button, [data-mode='season4-info']");
          if (button && button.textContent !== label) button.textContent = label;
        };
        updateSeasonButton();
        window.addEventListener("loadoutlab:lite-render", updateSeasonButton);
      })
      .catch(() => {});

    let scheduled = false;
    const observer = new MutationObserver(() => {
      if (scheduled) return;
      scheduled = true;
      window.requestAnimationFrame(() => {
        scheduled = false;
        cleanNode(document.body);
      });
    });
    observer.observe(document.body, { childList: true, subtree: true });
  }

  if (document.readyState === "loading") document.addEventListener("DOMContentLoaded", init, { once: true });
  else init();
}());

/* source: site-health.js */
(function () {
  const HEALTH_URL = "data/site-health.json";
  let currentData = null;

  function language() {
    return localStorage.getItem("loadoutLabLang") === "en" || document.documentElement.lang === "en" ? "en" : "de";
  }

  function formatAge(generatedAt) {
    const timestamp = new Date(generatedAt).getTime();
    const en = language() === "en";
    if (!Number.isFinite(timestamp)) return { label: en ? "Time unknown" : "Zeit unbekannt", state: "warning" };
    const hours = Math.max(0, (Date.now() - timestamp) / 3600000);
    if (hours < 2) return { label: en ? "Just checked" : "Gerade geprüft", state: "healthy" };
    if (hours < 24) return { label: en ? `Checked ${Math.max(1, Math.round(hours))}h ago` : `Vor ${Math.max(1, Math.round(hours))} Std. geprüft`, state: "healthy" };
    const days = Math.round(hours / 24);
    return { label: en ? `Checked ${days} day${days === 1 ? "" : "s"} ago` : `Vor ${days} Tag${days === 1 ? "" : "en"} geprüft`, state: days > 2 ? "error" : "warning" };
  }

  function statusItem(label, value, state) {
    return `<span class="health-item health-${state}"><i aria-hidden="true"></i><strong>${label}</strong><em>${value}</em></span>`;
  }

  function checkedAt(value) {
    const date = new Date(value);
    if (!Number.isFinite(date.getTime())) return language() === "en" ? "Unknown" : "Unbekannt";
    return new Intl.DateTimeFormat(language() === "en" ? "en-GB" : "de-DE", {
      day: "2-digit",
      month: "2-digit",
      hour: "2-digit",
      minute: "2-digit",
    }).format(date);
  }

  function nextAutoCheckLabel() {
    const now = new Date();
    const next = new Date(now);
    next.setMinutes(17, 0, 0);
    if (next <= now) next.setHours(next.getHours() + 1);
    return new Intl.DateTimeFormat(language() === "en" ? "en-GB" : "de-DE", {
      hour: "2-digit",
      minute: "2-digit",
    }).format(next);
  }

  function hasDebugMode() {
    return new URLSearchParams(window.location.search).get("debug") === "1";
  }

  function notifyRendered() {
    document.dispatchEvent(new CustomEvent("site-health-rendered"));
  }

  function sourceList(data, en) {
    const sources = Array.isArray(data.sources) && data.sources.length ? data.sources : ["WZStats", "Call of Duty Updates"];
    return `<div class="health-source-panel" aria-label="${en ? "Data sources" : "Datenquellen"}">
      <strong>${en ? "Data sources" : "Datenquellen"}</strong>
      <p>${sources.map((source) => `<span>${source}</span>`).join("")}</p>
    </div>`;
  }

  function debugPanel(data, en) {
    const failures = Array.isArray(data.sourceFailures) ? data.sourceFailures : [];
    const failureHtml = failures.length
      ? failures.map((failure) => `<li><strong>${failure.name || "Quelle"}</strong><span>${failure.error || "Fehler"}</span></li>`).join("")
      : `<li><strong>${en ? "No source errors" : "Keine Quellenfehler"}</strong><span>${en ? "All automated checks passed" : "Alle automatischen Checks bestanden"}</span></li>`;
    return `<details class="health-debug-panel" open>
      <summary>${en ? "Debug checks" : "Debug-Prüfung"}</summary>
      <ul>
        ${failureHtml}
        <li><strong>${en ? "Auto refresh" : "Auto-Update"}</strong><span>${en ? "Hourly at minute 17" : "Stündlich um Minute 17"}</span></li>
        <li><strong>${en ? "Registry" : "Register"}</strong><span>${data.completeWeapons || 0}/${data.totalWeapons || 0} ${en ? "complete" : "vollständig"}</span></li>
      </ul>
    </details>`;
  }

  function render(data) {
    currentData = data;
    const en = language() === "en";
    const heading = document.querySelector("#loadouts > .section-heading");
    if (!heading) return;
    const age = formatAge(data.generatedAt);
    const complete = Number(data.completeWeapons || 0);
    const total = Number(data.totalWeapons || 0);
    const images = Number(data.approvedImages ?? complete);
    const builds = Number(data.completeBuilds ?? complete);
    const completeState = total > 0 && complete === total ? "healthy" : "error";
    const imageState = total > 0 && images === total ? "healthy" : "error";
    const buildState = total > 0 && builds === total ? "healthy" : "error";
    const sourceState = data.sourcesVerified ? "healthy" : "warning";
    const debug = hasDebugMode();

    let bar = document.querySelector("#siteHealthBar");
    if (!bar) {
      bar = document.createElement("div");
      bar.id = "siteHealthBar";
      bar.className = "site-health-bar";
      heading.insertAdjacentElement("afterend", bar);
    }

    bar.dataset.state = [age.state, completeState, imageState, buildState, sourceState].includes("error") ? "error" :
      [age.state, completeState, imageState, buildState, sourceState].includes("warning") ? "warning" : "healthy";
    document.documentElement.classList.toggle("loadout-debug-mode", debug);
    bar.setAttribute("aria-label", en ? "Meta data status" : "Status der Meta-Daten");
    if (!debug) {
      bar.hidden = true;
      bar.innerHTML = "";
      notifyRendered();
      return;
    }

    bar.hidden = false;
    bar.innerHTML = `
      <div class="health-summary">
        <strong>${bar.dataset.state === "healthy" ? (en ? "All checks passed" : "Alles geprüft") : (en ? "Checks need attention" : "Prüfung braucht Aufmerksamkeit")}</strong>
        <span>${en ? "Next auto check" : "Nächster Auto-Check"}: ${nextAutoCheckLabel()}</span>
      </div>
      <div class="health-items">
        ${[
          statusItem(en ? "Meta" : "Meta", checkedAt(data.generatedAt), age.state),
          statusItem(en ? "Sources" : "Quellen", data.sourcesVerified ? (en ? "Checked" : "Geprüft") : `${(data.sourceFailures || []).length} ${en ? "errors" : "Fehler"}`, sourceState),
          statusItem(en ? "Images" : "Bilder", `${images}/${total}`, imageState),
          statusItem(en ? "Builds" : "Aufsätze", `${builds}/${total}`, buildState),
        ].join("")}
      </div>
      ${sourceList(data, en)}
      ${debugPanel(data, en)}`;
    notifyRendered();
  }

  function renderUnavailable() {
    render({ generatedAt: "", completeWeapons: 0, totalWeapons: 0, sourcesVerified: false });
  }

  fetch(`${HEALTH_URL}?t=${Date.now()}`, { cache: "no-store" })
    .then((response) => {
      if (!response.ok) throw new Error("health status unavailable");
      return response.json();
    })
    .then(render)
    .catch(renderUnavailable);

  document.addEventListener("click", (event) => {
    if (!event.target.closest("[data-language]")) return;
    window.setTimeout(() => { if (currentData) render(currentData); });
  });
}());

/* source: image-audit.js */
(function () {
  const REGISTRY_URL = "data/weapon-registry.json";
  const REVIEW_KEY = "loadout-lab-image-reviews-v1";
  let registry = null;
  let dialog = null;
  let brokenImages = 0;
  let reviews = new Set();
  let duplicateUrls = new Set();

  try {
    reviews = new Set(JSON.parse(localStorage.getItem(REVIEW_KEY) || "[]"));
  } catch (_) {}

  function html(value) {
    return String(value || "")
      .replace(/&/g, "&amp;")
      .replace(/</g, "&lt;")
      .replace(/>/g, "&gt;")
      .replace(/"/g, "&quot;");
  }

  function sourceKey(source) {
    if (/waffenübersicht/i.test(source)) return "guide";
    if (/blog/i.test(source)) return "blog";
    return "cdn";
  }

  function sourceLabel(key) {
    if (key === "guide") return "Waffenübersicht";
    if (key === "blog") return "Season-Blog";
    return "COD CDN";
  }

  function suspicionReasons(weapon, source, duplicate) {
    const reasons = [];
    if (!weapon.imageApproved) reasons.push("nicht freigegeben");
    if (duplicate) reasons.push("doppelte URL");
    if (source === "cdn") reasons.push("unklare Quelle");
    if (!/^https:\/\/(imgs\.callofduty\.com|www\.callofduty\.com)\//i.test(weapon.imageUrl || "")) reasons.push("keine COD-URL");
    return reasons;
  }

  function hasInitialSuspects() {
    return (registry?.weapons || []).some((weapon) => {
      const source = sourceKey(weapon.imageSource);
      return suspicionReasons(weapon, source, duplicateUrls.has(weapon.imageUrl)).length > 0;
    });
  }

  function visibleWeaponCount(selector) {
    return [...document.querySelectorAll(selector)].filter((item) => !item.hidden).length;
  }

  function card(weapon) {
    const source = sourceKey(weapon.imageSource);
    const duplicate = duplicateUrls.has(weapon.imageUrl);
    const reasons = suspicionReasons(weapon, source, duplicate);
    const suspect = reasons.length > 0;
    const meta = [
      weapon.tier ? `${weapon.tier} #${weapon.position || "?"}` : "",
      weapon.score != null ? `Score ${weapon.score}` : "",
      weapon.role || "",
    ].filter(Boolean).join(" · ");
    return `
      <article class="image-audit-card" data-name="${html(weapon.name.toLowerCase())}" data-weapon="${html(weapon.name)}" data-url="${html(weapon.imageUrl)}" data-source="${source}" data-image-state="loading" data-review-state="${reviews.has(weapon.name) ? "flagged" : "clear"}" data-duplicate-state="${duplicate ? "duplicate" : "unique"}" data-suspect-state="${suspect ? "suspect" : "clear"}">
        <a href="${html(weapon.imageUrl)}" target="_blank" rel="noreferrer" aria-label="${html(weapon.name)} Bild in Originalgröße öffnen">
          <img src="${html(weapon.imageUrl)}" alt="${html(weapon.name)}" loading="lazy" decoding="async">
        </a>
        <div>
          <h3>${html(weapon.name)}</h3>
          <p class="image-audit-meta">${html(meta)}</p>
          <span class="image-source source-${source}">${sourceLabel(source)}</span>
          ${duplicate ? `<span class="image-source source-cdn">Doppelte URL</span>` : ""}
          ${suspect ? `<span class="image-source image-suspect-reason">${html(reasons.join(" · "))}</span>` : ""}
          <strong class="image-state">Wird geprüft</strong>
          <div class="image-audit-actions">
            <a class="image-source-link" href="${html(weapon.imageUrl)}" target="_blank" rel="noreferrer">Original</a>
            <button class="image-jump-button" type="button">Karte</button>
            <button class="image-review-button" type="button">${reviews.has(weapon.name) ? "Markierung entfernen" : "Bild ist falsch"}</button>
          </div>
        </div>
      </article>`;
  }

  function updateSummary() {
    if (!dialog) return;
    const visible = [...dialog.querySelectorAll(".image-audit-card")].filter((item) => !item.hidden);
    const summary = dialog.querySelector("#imageAuditSummary");
    const flagged = reviews.size;
    const duplicates = visible.filter((item) => item.dataset.duplicateState === "duplicate").length;
    const suspects = visible.filter((item) => item.dataset.suspectState === "suspect" || item.dataset.imageState === "error").length;
    if (summary) summary.textContent = `${visible.length} angezeigt · ${suspects} verdächtig · ${brokenImages} Ladefehler · ${duplicates} doppelte URLs · ${flagged} markiert`;
    dialog.dataset.state = brokenImages || flagged || suspects ? "error" : "healthy";
    dialog.querySelector("#imageAuditSuspectCount")?.replaceChildren(document.createTextNode(String(visibleWeaponCount(".image-audit-card[data-suspect-state='suspect'], .image-audit-card[data-image-state='error']"))));
    dialog.querySelector("#imageAuditDuplicateCount")?.replaceChildren(document.createTextNode(String(visibleWeaponCount(".image-audit-card[data-duplicate-state='duplicate']"))));
    dialog.querySelector("#imageAuditFlaggedCount")?.replaceChildren(document.createTextNode(String(reviews.size)));
  }

  function applyFilters() {
    if (!dialog) return;
    const query = dialog.querySelector("#imageAuditSearch")?.value.trim().toLowerCase() || "";
    const source = dialog.querySelector("#imageAuditSource")?.value || "all";
    const review = dialog.querySelector("#imageAuditReview")?.value || "all";
    dialog.querySelectorAll(".image-audit-card").forEach((item) => {
      item.hidden = !item.dataset.name.includes(query)
        || (source !== "all" && item.dataset.source !== source)
        || (review === "flagged" && item.dataset.reviewState !== "flagged")
        || (review === "duplicate" && item.dataset.duplicateState !== "duplicate")
        || (review === "suspect" && item.dataset.suspectState !== "suspect" && item.dataset.imageState !== "error")
        || (review === "broken" && item.dataset.imageState !== "error");
    });
    updateSummary();
  }

  function toggleReview(button) {
    const item = button.closest(".image-audit-card");
    const name = item.dataset.weapon;
    if (reviews.has(name)) reviews.delete(name);
    else reviews.add(name);
    localStorage.setItem(REVIEW_KEY, JSON.stringify([...reviews]));
    item.dataset.reviewState = reviews.has(name) ? "flagged" : "clear";
    button.textContent = reviews.has(name) ? "Markierung entfernen" : "Bild ist falsch";
    applyFilters();
  }

  function jumpToWeapon(button) {
    const item = button.closest(".image-audit-card");
    const name = item?.dataset.weapon;
    if (!name) return;
    dialog?.close();
    const card = [...document.querySelectorAll("#loadoutGrid .loadout-card")]
      .find((element) => element.dataset.loadoutCard === name);
    if (!card) return;
    card.scrollIntoView({ behavior: "smooth", block: "center" });
    card.classList.add("image-audit-focus");
    window.setTimeout(() => card.classList.remove("image-audit-focus"), 1700);
  }

  function exportReviews() {
    const entries = registry.weapons
      .filter((weapon) => reviews.has(weapon.name))
      .map((weapon) => ({ name: weapon.name, imageUrl: weapon.imageUrl, imageSource: weapon.imageSource }));
    if (!entries.length) return;
    const blob = new Blob([`${JSON.stringify(entries, null, 2)}\n`], { type: "application/json" });
    const link = document.createElement("a");
    link.href = URL.createObjectURL(blob);
    link.download = "falsche-waffenbilder.json";
    link.click();
    URL.revokeObjectURL(link.href);
  }

  function bindImageChecks() {
    dialog.querySelectorAll(".image-audit-card img").forEach((image) => {
      const item = image.closest(".image-audit-card");
      const status = item.querySelector(".image-state");
      const markLoaded = () => {
        item.dataset.imageState = "ready";
        status.textContent = "Bild erreichbar";
        updateSummary();
      };
      const markBroken = () => {
        if (item.dataset.imageState !== "error") brokenImages += 1;
        item.dataset.imageState = "error";
        item.dataset.suspectState = "suspect";
        status.textContent = "Bildfehler";
        updateSummary();
      };
      if (image.complete) image.naturalWidth > 0 ? markLoaded() : markBroken();
      else {
        image.addEventListener("load", markLoaded, { once: true });
        image.addEventListener("error", markBroken, { once: true });
      }
    });
  }

  function buildDialog() {
    if (dialog) return dialog;
    const defaultReview = hasInitialSuspects() ? "suspect" : "all";
    dialog = document.createElement("dialog");
    dialog.id = "imageAuditDialog";
    dialog.className = "image-audit-dialog";
    dialog.setAttribute("aria-labelledby", "imageAuditTitle");
    dialog.innerHTML = `
      <div class="image-audit-shell">
        <header>
          <div><span>Waffenregister</span><h2 id="imageAuditTitle">Bildprüfung</h2></div>
          <button class="image-audit-close" type="button" aria-label="Bildprüfung schließen">×</button>
        </header>
        <div class="image-audit-toolbar">
          <label><span>Suche</span><input id="imageAuditSearch" type="search" placeholder="Waffe suchen"></label>
          <label><span>Quelle</span><select id="imageAuditSource"><option value="all">Alle Quellen</option><option value="guide">Waffenübersicht</option><option value="blog">Season-Blog</option><option value="cdn">COD CDN</option></select></label>
          <label><span>Prüfstatus</span><select id="imageAuditReview"><option value="suspect"${defaultReview === "suspect" ? " selected" : ""}>Verdächtige zuerst</option><option value="all"${defaultReview === "all" ? " selected" : ""}>Alle Bilder</option><option value="flagged">Nur markierte</option><option value="duplicate">Doppelte URLs</option><option value="broken">Ladefehler</option></select></label>
        </div>
        <div class="image-audit-metrics" aria-label="Bildprüfung Kennzahlen">
          <button type="button" data-audit-filter="suspect"><span id="imageAuditSuspectCount">0</span><strong>Verdächtig</strong></button>
          <button type="button" data-audit-filter="duplicate"><span id="imageAuditDuplicateCount">0</span><strong>Doppelte URLs</strong></button>
          <button type="button" data-audit-filter="flagged"><span id="imageAuditFlaggedCount">0</span><strong>Markiert</strong></button>
        </div>
        <div class="image-audit-result"><i aria-hidden="true"></i><strong id="imageAuditSummary">Bilder werden geprüft</strong><button id="imageAuditExport" type="button">Markierungen exportieren</button></div>
        <div class="image-audit-grid">${registry.weapons.map(card).join("")}</div>
      </div>`;
    document.body.append(dialog);
    dialog.querySelector(".image-audit-close").addEventListener("click", () => dialog.close());
    dialog.querySelector("#imageAuditSearch").addEventListener("input", applyFilters);
    dialog.querySelector("#imageAuditSource").addEventListener("change", applyFilters);
    dialog.querySelector("#imageAuditReview").addEventListener("change", applyFilters);
    dialog.querySelector("#imageAuditExport").addEventListener("click", exportReviews);
    dialog.querySelectorAll(".image-review-button").forEach((button) => button.addEventListener("click", () => toggleReview(button)));
    dialog.querySelectorAll(".image-jump-button").forEach((button) => button.addEventListener("click", () => jumpToWeapon(button)));
    dialog.querySelectorAll("[data-audit-filter]").forEach((button) => {
      button.addEventListener("click", () => {
        const select = dialog.querySelector("#imageAuditReview");
        select.value = button.dataset.auditFilter;
        applyFilters();
      });
    });
    dialog.addEventListener("click", (event) => {
      if (event.target === dialog) dialog.close();
    });
    dialog.addEventListener("close", () => document.body.classList.remove("image-audit-open"));
    applyFilters();
    bindImageChecks();
    return dialog;
  }

  function openAudit() {
    const audit = buildDialog();
    document.body.classList.add("image-audit-open");
    audit.showModal();
    audit.querySelector("#imageAuditSearch")?.focus();
  }

  function installButton() {
    const bar = document.querySelector("#siteHealthBar");
    if (!bar || bar.querySelector(".image-audit-button")) return false;
    const button = document.createElement("button");
    button.className = "image-audit-button";
    button.type = "button";
    button.textContent = "Bilder prüfen";
    button.addEventListener("click", openAudit);
    bar.append(button);
    return true;
  }

  function watchHealthBar() {
    installButton();
    if (document.body.dataset.imageAuditButtonWatched === "true") return;
    document.body.dataset.imageAuditButtonWatched = "true";
    new MutationObserver(installButton).observe(document.body, { childList: true, subtree: true });
  }

  window.__loadoutWeaponRegistryPromise ||= fetch(`${REGISTRY_URL}?t=${Date.now()}`, { cache: "no-store" })
    .then((response) => {
      if (!response.ok) throw new Error("weapon registry unavailable");
      return response.json();
    });

  window.__loadoutWeaponRegistryPromise
    .then((data) => {
      registry = data;
      const counts = new Map();
      registry.weapons.forEach((weapon) => counts.set(weapon.imageUrl, (counts.get(weapon.imageUrl) || 0) + 1));
      duplicateUrls = new Set([...counts].filter(([, count]) => count > 1).map(([url]) => url));
      document.addEventListener("site-health-rendered", installButton);
      watchHealthBar();
    })
    .catch(() => {});
}());

/* source: build-audit.js */
(function () {
  const REGISTRY_URL = "data/weapon-registry.json";
  let registry = null;
  let dialog = null;

  function html(value) {
    return String(value || "")
      .replace(/&/g, "&amp;")
      .replace(/</g, "&lt;")
      .replace(/>/g, "&gt;")
      .replace(/"/g, "&quot;");
  }

  function badgeLabel(weapon) {
    if (weapon.expectedAttachmentCount === 1) return "1/1 Build-Eintrag";
    return `${weapon.attachmentCount}/${weapon.expectedAttachmentCount} Aufsätze`;
  }

  function markCards() {
    const byName = new Map(registry.weapons.map((weapon) => [weapon.name.toLowerCase(), weapon]));
    document.querySelectorAll("#loadoutGrid .loadout-card").forEach((card) => {
      const name = (card.dataset.loadoutCard || card.querySelector(".weapon-name")?.textContent || "").trim().toLowerCase();
      const weapon = byName.get(name);
      if (!weapon) return;
      card.dataset.buildState = weapon.buildComplete ? "complete" : "incomplete";
      let badge = card.querySelector(".build-completeness-badge");
      if (!badge) {
        badge = document.createElement("span");
        badge.className = "build-completeness-badge";
        const target = card.querySelector(".meta-score-pill, .stat-row, .weapon-name");
        target?.insertAdjacentElement("afterend", badge);
      }
      const label = badgeLabel(weapon);
      const title = weapon.buildComplete ? "Build vollständig" : "Build unvollständig";
      if (badge.textContent !== label) badge.textContent = label;
      if (badge.title !== title) badge.title = title;
    });
  }

  function auditRow(weapon) {
    const attachments = (weapon.attachments || []).map((attachment) => `<li>${html(attachment)}</li>`).join("");
    return `
      <details class="build-audit-row" data-name="${html(weapon.name.toLowerCase())}" data-state="${weapon.buildComplete ? "complete" : "incomplete"}">
        <summary>
          <strong>${html(weapon.name)}</strong>
          <span>${badgeLabel(weapon)}</span>
          <em>${weapon.buildComplete ? "Vollständig" : "Prüfen"}</em>
        </summary>
        <ol>${attachments || "<li>Keine verifizierten Aufsätze vorhanden</li>"}</ol>
      </details>`;
  }

  function applyFilters() {
    const query = dialog.querySelector("#buildAuditSearch")?.value.trim().toLowerCase() || "";
    const state = dialog.querySelector("#buildAuditState")?.value || "all";
    const rows = [...dialog.querySelectorAll(".build-audit-row")];
    rows.forEach((row) => {
      row.hidden = !row.dataset.name.includes(query) || (state !== "all" && row.dataset.state !== state);
    });
    const visible = rows.filter((row) => !row.hidden).length;
    const incomplete = registry.weapons.filter((weapon) => !weapon.buildComplete).length;
    dialog.querySelector("#buildAuditSummary").textContent = `${visible} angezeigt · ${incomplete} unvollständig`;
    dialog.dataset.state = incomplete ? "error" : "healthy";
  }

  function buildDialog() {
    if (dialog) return dialog;
    dialog = document.createElement("dialog");
    dialog.id = "buildAuditDialog";
    dialog.className = "build-audit-dialog";
    dialog.setAttribute("aria-labelledby", "buildAuditTitle");
    dialog.innerHTML = `
      <div class="build-audit-shell">
        <header><div><span>Build-Register</span><h2 id="buildAuditTitle">Aufsatzprüfung</h2></div><button class="build-audit-close" type="button" aria-label="Aufsatzprüfung schließen">×</button></header>
        <div class="build-audit-toolbar">
          <label><span>Suche</span><input id="buildAuditSearch" type="search" placeholder="Waffe suchen"></label>
          <label><span>Status</span><select id="buildAuditState"><option value="all">Alle Builds</option><option value="incomplete">Nur unvollständige</option><option value="complete">Nur vollständige</option></select></label>
        </div>
        <div class="build-audit-result"><i aria-hidden="true"></i><strong id="buildAuditSummary"></strong></div>
        <div class="build-audit-list">${registry.weapons.map(auditRow).join("")}</div>
      </div>`;
    document.body.append(dialog);
    dialog.querySelector(".build-audit-close").addEventListener("click", () => dialog.close());
    dialog.querySelector("#buildAuditSearch").addEventListener("input", applyFilters);
    dialog.querySelector("#buildAuditState").addEventListener("change", applyFilters);
    dialog.addEventListener("click", (event) => { if (event.target === dialog) dialog.close(); });
    dialog.addEventListener("close", () => document.body.classList.remove("build-audit-open"));
    applyFilters();
    return dialog;
  }

  function openAudit() {
    const audit = buildDialog();
    document.body.classList.add("build-audit-open");
    audit.showModal();
    audit.querySelector("#buildAuditSearch")?.focus();
  }

  function installButton() {
    const bar = document.querySelector("#siteHealthBar");
    if (!bar || bar.querySelector(".build-audit-button")) return false;
    const button = document.createElement("button");
    button.className = "build-audit-button";
    button.type = "button";
    button.textContent = "Aufsätze prüfen";
    button.addEventListener("click", openAudit);
    bar.append(button);
    return true;
  }

  function watchHealthBar() {
    installButton();
    if (document.body.dataset.buildAuditButtonWatched === "true") return;
    document.body.dataset.buildAuditButtonWatched = "true";
    new MutationObserver(installButton).observe(document.body, { childList: true, subtree: true });
  }

  window.__loadoutWeaponRegistryPromise ||= fetch(`${REGISTRY_URL}?t=${Date.now()}`, { cache: "no-store" })
    .then((response) => {
      if (!response.ok) throw new Error("weapon registry unavailable");
      return response.json();
    });

  window.__loadoutWeaponRegistryPromise
    .then((data) => {
      registry = data;
      markCards();
      document.addEventListener("site-health-rendered", installButton);
      watchHealthBar();
      const grid = document.querySelector("#loadoutGrid");
      if (grid) new MutationObserver(markCards).observe(grid, { childList: true });
    })
    .catch(() => {});
}());

/* source: weapon-detail.js */
(function () {
  const REGISTRY_URL = "data/weapon-registry.json";
  let registry = null;
  let dialog = null;
  let currentId = "";
  const baseTitle = document.title;

  const copy = {
    de: { profile: "Waffenprofil", close: "Waffenprofil schließen", rank: "Rang", score: "Score", tier: "Tier", role: "Rolle", build: "Meta-Build", code: "Klassen-Code", noCode: "Kein Code verfügbar", copyCode: "Code kopieren", imageSource: "Bildquelle", updated: "Datenstand", previous: "Vorherige", share: "Teilen", next: "Nächste", copied: "Kopiert", shareText: "Meta-Build auf Loadout Lab" },
    en: { profile: "Weapon profile", close: "Close weapon profile", rank: "Rank", score: "Score", tier: "Tier", role: "Role", build: "Meta build", code: "Class code", noCode: "No code available", copyCode: "Copy code", imageSource: "Image source", updated: "Updated", previous: "Previous", share: "Share", next: "Next", copied: "Copied", shareText: "Meta build on Loadout Lab" },
  };

  function language() {
    return localStorage.getItem("loadoutLabLang") === "en" || document.documentElement.lang === "en" ? "en" : "de";
  }

  function labels() {
    return copy[language()];
  }

  function localRole(value) {
    if (language() === "en") return value;
    return { "Long Range": "Langstrecke", "Close Range": "Kurzstrecke", Sniper: "Scharfschütze", Special: "Spezial", Melee: "Nahkampf" }[value] || value;
  }

  function localClass(value) {
    if (language() === "en") return value;
    return { "Assault Rifle": "Sturmgewehr", SMG: "MP", LMG: "LMG", "Marksman Rifle": "Präzisionsgewehr", "Sniper Rifle": "Scharfschützengewehr", Pistol: "Pistole", Special: "Spezial", Melee: "Nahkampf" }[value] || value;
  }

  function html(value) {
    return String(value ?? "")
      .replace(/&/g, "&amp;")
      .replace(/</g, "&lt;")
      .replace(/>/g, "&gt;")
      .replace(/"/g, "&quot;");
  }

  function fallbackImage(weapon) {
    const card = [...document.querySelectorAll("#loadoutGrid .loadout-card")]
      .find((item) => (item.dataset.loadoutCard || "").toLowerCase() === weapon.name.toLowerCase());
    const cardFallback = card?.querySelector(".weapon-art img")?.dataset.fallback;
    if (cardFallback) return cardFallback;
    const label = html(weapon.name).replace(/&amp;/g, "&");
    return `data:image/svg+xml;charset=utf-8,${encodeURIComponent(`<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 420 236"><rect width="420" height="236" fill="#070b10"/><path d="M34 151h250l26-22h51l20 22h20v25H34z" fill="#222c38" stroke="#9dff2f" stroke-width="4"/><text x="24" y="42" fill="#9dff2f" font-family="Arial" font-size="24" font-weight="900">${label}</text></svg>`)}`;
  }

  function weaponUrl(id) {
    const url = new URL(window.location.href);
    url.searchParams.set("weapon", id);
    url.hash = "";
    return url.toString();
  }

  function setUrl(id, replace = false) {
    const url = new URL(window.location.href);
    if (id) url.searchParams.set("weapon", id);
    else url.searchParams.delete("weapon");
    history[replace ? "replaceState" : "pushState"]({ weapon: id || null }, "", url);
  }

  async function copyText(value, button) {
    try {
      await navigator.clipboard.writeText(value);
    } catch (_) {
      const input = document.createElement("textarea");
      input.value = value;
      document.body.append(input);
      input.select();
      document.execCommand("copy");
      input.remove();
    }
    const old = button.textContent;
    button.textContent = labels().copied;
    window.setTimeout(() => { button.textContent = old; }, 1400);
  }

  async function shareWeapon(button) {
    const item = registry.weapons.find((weapon) => weapon.id === currentId);
    const url = weaponUrl(currentId);
    if (navigator.share) {
      try {
        await navigator.share({ title: `${item.name} | Loadout Lab`, text: labels().shareText, url });
        return;
      } catch (error) {
        if (error?.name === "AbortError") return;
      }
    }
    await copyText(url, button);
  }

  function buildDialog() {
    if (dialog) return dialog;
    dialog = document.createElement("dialog");
    dialog.id = "weaponDetailDialog";
    dialog.className = "weapon-detail-dialog";
    dialog.setAttribute("aria-labelledby", "weaponDetailTitle");
    document.body.append(dialog);
    dialog.addEventListener("click", (event) => {
      const action = event.target.closest("[data-weapon-action]")?.dataset.weaponAction;
      if (action === "close") closeDetail();
      if (action === "previous") navigate(-1);
      if (action === "next") navigate(1);
      if (action === "share-link") shareWeapon(event.target.closest("button"));
      if (action === "copy-code") {
        const weapon = registry.weapons.find((item) => item.id === currentId);
        if (weapon?.buildCode) copyText(weapon.buildCode, event.target.closest("button"));
      }
      if (event.target === dialog) closeDetail();
    });
    dialog.addEventListener("close", () => {
      document.body.classList.remove("weapon-detail-open");
      document.title = baseTitle;
      if (new URL(window.location.href).searchParams.has("weapon")) setUrl("", true);
    });
    return dialog;
  }

  function render(weapon) {
    const text = labels();
    const fallback = fallbackImage(weapon);
    const rawAttachments = (Array.isArray(weapon.attachments) ? weapon.attachments : [])
      .map((value) => String(value || "").trim())
      .filter((value) => value && !/^(Pick-Rate|Quelle|Rolle):/i.test(value));
    const hasPendingAttachments = rawAttachments.length === 0 || rawAttachments.some((value) => /Aufsätze werden aktualisiert/i.test(value));
    const attachments = hasPendingAttachments
      ? `<li><span>--</span><strong>Aufsätze werden aktualisiert</strong></li>`
      : rawAttachments.map((value) => {
          const parts = value.split(":");
          const slot = parts.length > 1 ? parts.shift().trim() : "Aufsatz";
          const name = parts.join(":").trim() || value;
          return `<li><span>${html(slot)}</span><strong>${html(name)}</strong></li>`;
        }).join("");
    const rank = weapon.position ? `#${weapon.position}` : "–";
    const score = Number.isFinite(Number(weapon.score)) ? weapon.score : "–";
    dialog.innerHTML = `
      <article class="weapon-detail-shell tier-${html(String(weapon.tier || "").toLowerCase())}">
        <header>
          <span>${text.profile}</span>
          <button type="button" data-weapon-action="close" aria-label="${text.close}">×</button>
        </header>
        <div class="weapon-detail-hero">
          <div class="weapon-detail-image"><img src="${html(weapon.imageUrl)}" alt="${html(weapon.name)}"${fallback ? ` data-fallback="${html(fallback)}" onerror="this.onerror=null;this.src=this.dataset.fallback"` : ""} decoding="async" fetchpriority="high"></div>
          <div class="weapon-detail-title">
            <span>${html(localClass(weapon.weaponClass))}</span>
            <h2 id="weaponDetailTitle">${html(weapon.name)}</h2>
            <div class="weapon-detail-stats">
              <strong><small>${text.rank}</small>${rank}</strong>
              <strong><small>${text.score}</small>${score}<em>/100</em></strong>
              <strong><small>${text.tier}</small>${html(weapon.tierLabel || weapon.tier)}</strong>
              <strong><small>${text.role}</small>${html(localRole(weapon.role))}</strong>
            </div>
          </div>
        </div>
        <div class="weapon-detail-content">
          <section>
            <div class="weapon-detail-section-title"><span>${text.build}</span><strong>${hasPendingAttachments ? "Update" : `${rawAttachments.length}/${weapon.expectedAttachmentCount || 5}`}</strong></div>
            <ol class="weapon-detail-attachments">${attachments}</ol>
          </section>
          <aside>
            <span>${text.code}</span>
            <strong>${html(weapon.buildCode || text.noCode)}</strong>
            ${weapon.buildCode ? `<button type="button" data-weapon-action="copy-code">${text.copyCode}</button>` : ""}
            <dl>
              <div><dt>${text.imageSource}</dt><dd>${html(weapon.imageSource)}</dd></div>
              <div><dt>${text.updated}</dt><dd>${html(weapon.sourceUpdatedLabel)}</dd></div>
            </dl>
          </aside>
        </div>
        <footer>
          <button type="button" data-weapon-action="previous">${text.previous}</button>
          <button type="button" data-weapon-action="share-link">${text.share}</button>
          <button type="button" data-weapon-action="next">${text.next}</button>
        </footer>
      </article>`;
  }

  function openDetail(id, updateUrl = true) {
    const weapon = registry?.weapons.find((item) => item.id === id);
    if (!weapon) return false;
    currentId = id;
    buildDialog();
    render(weapon);
    document.body.classList.add("weapon-detail-open");
    if (!dialog.open) dialog.showModal();
    if (updateUrl) setUrl(id);
    document.title = `${weapon.name} Build | Loadout Lab`;
    dialog.querySelector("[data-weapon-action='close']")?.focus();
    return true;
  }

  function closeDetail() {
    if (dialog?.open) dialog.close();
    else setUrl("", true);
  }

  function navigate(direction) {
    const index = registry.weapons.findIndex((weapon) => weapon.id === currentId);
    const next = (index + direction + registry.weapons.length) % registry.weapons.length;
    openDetail(registry.weapons[next].id, false);
    setUrl(registry.weapons[next].id, true);
  }

  function installShareButtons() {
    const byName = new Map(registry.weapons.map((weapon) => [weapon.name.toLowerCase(), weapon]));
    document.querySelectorAll("#loadoutGrid .loadout-card").forEach((card) => {
      const existing = card.querySelector(".weapon-share-button");
      if (existing) {
        existing.textContent = language() === "en" ? "Share" : "Teilen";
        return;
      }
      const name = (card.dataset.loadoutCard || card.querySelector(".weapon-name")?.textContent || "").trim().toLowerCase();
      const weapon = byName.get(name);
      const footer = card.querySelector(".card-footer");
      if (!weapon || !footer) return;
      const button = document.createElement("button");
      button.className = "weapon-share-button";
      button.type = "button";
      button.textContent = language() === "en" ? "Share" : "Teilen";
      button.setAttribute("aria-label", `${weapon.name} Waffenprofil öffnen`);
      button.addEventListener("click", (event) => {
        event.preventDefault();
        event.stopPropagation();
        openDetail(weapon.id);
      });
      footer.append(button);
    });
  }

  window.__loadoutWeaponRegistryPromise ||= fetch(`${REGISTRY_URL}?t=${Date.now()}`, { cache: "no-store" })
    .then((response) => {
      if (!response.ok) throw new Error("weapon registry unavailable");
      return response.json();
    });

  window.__loadoutWeaponRegistryPromise
    .then((data) => {
      registry = data;
      installShareButtons();
      const grid = document.querySelector("#loadoutGrid");
      if (grid) new MutationObserver(installShareButtons).observe(grid, { childList: true });
      const requested = new URL(window.location.href).searchParams.get("weapon");
      if (requested && !openDetail(requested, false)) setUrl("", true);
      document.addEventListener("click", (event) => {
        if (!event.target.closest("[data-language]")) return;
        window.setTimeout(() => {
          installShareButtons();
          const current = registry.weapons.find((item) => item.id === currentId);
          if (dialog?.open && current) render(current);
        });
      });
      window.addEventListener("popstate", () => {
        const id = new URL(window.location.href).searchParams.get("weapon");
        if (id) openDetail(id, false);
        else if (dialog?.open) dialog.close();
      });
    })
    .catch(() => {});
}());

/* source: weapon-compare.js */
(function () {
  const REGISTRY_URL = "data/weapon-registry.json";
  const selected = new Set();
  let registry = null;
  let tray = null;
  let dialog = null;

  const copy = {
    de: { selected: "ausgewählt", empty: "Keine Waffen ausgewählt", clear: "Leeren", compare: "Vergleichen", direct: "Direktvergleich", close: "Vergleich schließen", gap: "Score-Abstand", points: "Punkte", point: "Punkt", rank: "Rang", score: "Score", tier: "Tier", role: "Rolle", versus: "vs.", winner: "Besserer Score", tie: "Gleichstand", same: "gleich", different: "anders", attachments: "Aufsätze" },
    en: { selected: "selected", empty: "No weapons selected", clear: "Clear", compare: "Compare", direct: "Direct comparison", close: "Close comparison", gap: "Score gap", points: "points", point: "point", rank: "Rank", score: "Score", tier: "Tier", role: "Role", versus: "vs.", winner: "Higher score", tie: "Tie", same: "same", different: "different", attachments: "Attachments" },
  };

  function language() {
    return localStorage.getItem("loadoutLabLang") === "en" || document.documentElement.lang === "en" ? "en" : "de";
  }

  function labels() {
    return copy[language()];
  }

  function localRole(value) {
    if (language() === "en") return value;
    return { "Long Range": "Langstrecke", "Close Range": "Kurzstrecke", Sniper: "Scharfschütze", Special: "Spezial", Melee: "Nahkampf" }[value] || value;
  }

  function localClass(value) {
    if (language() === "en") return value;
    return { "Assault Rifle": "Sturmgewehr", SMG: "MP", LMG: "LMG", "Marksman Rifle": "Präzisionsgewehr", "Sniper Rifle": "Scharfschützengewehr", Pistol: "Pistole", Special: "Spezial", Melee: "Nahkampf" }[value] || value;
  }

  function html(value) {
    return String(value ?? "")
      .replace(/&/g, "&amp;")
      .replace(/</g, "&lt;")
      .replace(/>/g, "&gt;")
      .replace(/"/g, "&quot;");
  }

  function weapon(id) {
    return registry.weapons.find((item) => item.id === id);
  }

  function fallbackImage(item) {
    const card = [...document.querySelectorAll("#loadoutGrid .loadout-card")]
      .find((candidate) => (candidate.dataset.loadoutCard || "").toLowerCase() === item.name.toLowerCase());
    const cardFallback = card?.querySelector(".weapon-art img")?.dataset.fallback;
    if (cardFallback) return cardFallback;
    const label = html(item.name).replace(/&amp;/g, "&");
    return `data:image/svg+xml;charset=utf-8,${encodeURIComponent(`<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 420 236"><rect width="420" height="236" fill="#070b10"/><path d="M34 151h250l26-22h51l20 22h20v25H34z" fill="#222c38" stroke="#9dff2f" stroke-width="4"/><text x="24" y="42" fill="#9dff2f" font-family="Arial" font-size="24" font-weight="900">${label}</text></svg>`)}`;
  }

  function syncTray() {
    if (!tray) return;
    const text = labels();
    const items = [...selected].map(weapon).filter(Boolean);
    tray.hidden = !items.length;
    tray.querySelector("strong").textContent = items.length
      ? items.map((item) => item.name).join(` ${text.versus} `)
      : text.empty;
    tray.querySelector("span").textContent = `${items.length}/2 ${text.selected}`;
    tray.querySelector("[data-compare-clear]").textContent = text.clear;
    tray.querySelector("[data-compare-open]").textContent = text.compare;
    tray.querySelector("[data-compare-open]").disabled = items.length !== 2;
    document.querySelectorAll(".weapon-compare-check input").forEach((input) => {
      input.checked = selected.has(input.value);
      input.disabled = selected.size >= 2 && !input.checked;
    });
  }

  function normalizeAttachment(value) {
    return String(value || "")
      .toLowerCase()
      .replace(/\([^)]*\)/g, "")
      .replace(/^[^:]+:\s*/, "")
      .replace(/\s+/g, " ")
      .trim();
  }

  function attachmentRows(item, sharedAttachments) {
    const text = labels();
    return item.attachments.map((attachment) => {
      const state = sharedAttachments.has(normalizeAttachment(attachment)) ? "same" : "different";
      return `<li data-attachment-state="${state}"><span>${state === "same" ? text.same : text.different}</span><strong>${html(attachment)}</strong></li>`;
    }).join("");
  }

  function comparisonColumn(item, context) {
    const text = labels();
    const fallback = fallbackImage(item);
    const score = Number(item.score || 0);
    const scoreState = context.tie ? "tie" : score === context.highScore ? "winner" : "lower";
    const attachments = attachmentRows(item, context.sharedAttachments);
    return `
      <section class="weapon-compare-column" data-score-state="${scoreState}">
        <img src="${html(item.imageUrl)}" alt="${html(item.name)}"${fallback ? ` data-fallback="${html(fallback)}" onerror="this.onerror=null;this.src=this.dataset.fallback"` : ""} loading="lazy" decoding="async">
        <div class="compare-title-row"><span>${html(localClass(item.weaponClass))}</span>${scoreState === "winner" ? `<em>${text.winner}</em>` : scoreState === "tie" ? `<em>${text.tie}</em>` : ""}</div>
        <h3>${html(item.name)}</h3>
        <dl>
          <div><dt>${text.rank}</dt><dd>#${html(item.position)}</dd></div>
          <div data-stat-state="${scoreState}"><dt>${text.score}</dt><dd>${html(item.score)}/100</dd></div>
          <div><dt>${text.tier}</dt><dd>${html(item.tierLabel)}</dd></div>
          <div><dt>${text.role}</dt><dd>${html(localRole(item.role))}</dd></div>
        </dl>
        <div class="compare-attachment-title">${text.attachments}</div>
        <ol>${attachments}</ol>
      </section>`;
  }

  function openComparison() {
    const items = [...selected].map(weapon).filter(Boolean);
    if (items.length !== 2) return;
    const text = labels();
    if (!dialog) {
      dialog = document.createElement("dialog");
      dialog.id = "weaponCompareDialog";
      dialog.className = "weapon-compare-dialog-v2";
      dialog.setAttribute("aria-labelledby", "weaponCompareTitle");
      document.body.append(dialog);
      dialog.addEventListener("click", (event) => {
        if (event.target === dialog || event.target.closest("[data-compare-close]")) dialog.close();
      });
      dialog.addEventListener("close", () => document.body.classList.remove("weapon-compare-open"));
    }
    const difference = Math.abs(Number(items[0].score) - Number(items[1].score));
    const highScore = Math.max(Number(items[0].score || 0), Number(items[1].score || 0));
    const tie = Number(items[0].score || 0) === Number(items[1].score || 0);
    const firstAttachments = new Set(items[0].attachments.map(normalizeAttachment));
    const sharedAttachments = new Set(items[1].attachments.map(normalizeAttachment).filter((attachment) => firstAttachments.has(attachment)));
    const context = { highScore, tie, sharedAttachments };
    dialog.innerHTML = `
      <div class="weapon-compare-shell">
        <header><div><span>${text.direct}</span><h2 id="weaponCompareTitle">${html(items[0].name)} ${text.versus} ${html(items[1].name)}</h2></div><button type="button" data-compare-close aria-label="${text.close}">×</button></header>
        <p class="weapon-compare-summary">${text.gap}: <strong>${difference} ${difference === 1 ? text.point : text.points}</strong></p>
        <div class="weapon-compare-columns">${items.map((item) => comparisonColumn(item, context)).join("")}</div>
      </div>`;
    document.body.classList.add("weapon-compare-open");
    if (!dialog.open) dialog.showModal();
    dialog.querySelector("[data-compare-close]")?.focus();
  }

  function buildTray() {
    tray = document.createElement("div");
    tray.className = "weapon-compare-tray-v2";
    tray.hidden = true;
    tray.innerHTML = `
      <div><span aria-live="polite"></span><strong></strong></div>
      <button type="button" data-compare-clear></button>
      <button type="button" data-compare-open disabled></button>`;
    document.body.append(tray);
    tray.querySelector("[data-compare-clear]").addEventListener("click", () => {
      selected.clear();
      syncTray();
    });
    tray.querySelector("[data-compare-open]").addEventListener("click", openComparison);
  }

  function toggle(id, checked) {
    if (checked && selected.size < 2) selected.add(id);
    else selected.delete(id);
    syncTray();
  }

  function handleCompareClick(event) {
    const label = event.target.closest(".weapon-compare-check");
    if (!label) return;
    const input = label.querySelector("input");
    if (!input) return;
    event.preventDefault();
    event.stopPropagation();
    if (input.disabled && !input.checked) return;
    toggle(input.value, !input.checked);
  }

  function bindCheck(label, item) {
    if (!label || label.dataset.compareBound === "true") return;
    label.dataset.compareBound = "true";
    const input = label.querySelector("input");
    if (!input) return;
    input.addEventListener("change", (event) => toggle(item.id, event.target.checked));
    label.addEventListener("pointerdown", (event) => {
      if (event.target === input) return;
      event.preventDefault();
      event.stopPropagation();
      if (input.disabled && !input.checked) return;
      toggle(item.id, !input.checked);
    });
  }

  function installChecks() {
    const byName = new Map(registry.weapons.map((item) => [item.name.toLowerCase(), item]));
    document.querySelectorAll("#loadoutGrid .loadout-card").forEach((card) => {
      const name = (card.dataset.loadoutCard || card.querySelector(".weapon-name")?.textContent || "").trim().toLowerCase();
      const item = byName.get(name);
      const footer = card.querySelector(".card-footer");
      if (!item || !footer) return;
      const existing = card.querySelector(".weapon-compare-check");
      if (existing) {
        bindCheck(existing, item);
        return;
      }
      const label = document.createElement("label");
      label.className = "weapon-compare-check";
      label.innerHTML = `<input type="checkbox" value="${html(item.id)}"><span>${labels().compare}</span>`;
      bindCheck(label, item);
      footer.append(label);
    });
    syncTray();
  }

  window.__loadoutWeaponRegistryPromise ||= fetch(`${REGISTRY_URL}?t=${Date.now()}`, { cache: "no-store" })
    .then((response) => {
      if (!response.ok) throw new Error("weapon registry unavailable");
      return response.json();
    });

  window.__loadoutWeaponRegistryPromise
    .then((data) => {
      registry = data;
      buildTray();
      document.addEventListener("click", handleCompareClick, true);
      installChecks();
      const grid = document.querySelector("#loadoutGrid");
      if (grid) new MutationObserver(installChecks).observe(grid, { childList: true });
      document.addEventListener("click", (event) => {
        if (!event.target.closest("[data-language]")) return;
        window.setTimeout(() => {
          document.querySelectorAll(".weapon-compare-check span").forEach((node) => { node.textContent = labels().compare; });
          syncTray();
          if (dialog?.open) openComparison();
        });
      });
    })
    .catch(() => {});
}());
